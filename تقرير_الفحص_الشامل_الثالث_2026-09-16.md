# تقرير الفحص الشامل الثالث — بيت الورد

**التاريخ:** 2026-09-16 · **نوع الفحص:** فحص جديد كامل، لا إعادة تدوير للتقارير السابقة

> **منهج هذا التقرير:** لم أثق بسجلّي الخاص. تحقّقتُ **مستقلّاً** من وجود كل إصلاح سابق
> في الكود، ثم بحثتُ عن أعطال **جديدة** بمسوحات لم أُجرِها من قبل (شروط ميتة، وعود غير
> منتظَرة، ابتلاع صامت للأخطاء، تسرّب مؤقّتات ومستمعين، طبقات التخزين، إشارات القيود
> المحاسبية). ووجدتُ **عطلاً حرجاً واحداً لم يظهر في أي تقرير سابق** — وأصلحتُه.

---

## 0. النتيجة: **٨١٪** (كانت ٧٢٪)

| المحور | السابق | الآن | التغيّر |
|---|---|---|---|
| الحسابات المالية | ٩٢٪ | ٩٢٪ | ثابت — تحقّق حيّ للبيع والمرتجع والتقسيم والآجل |
| حسابات الوردية والدرج | ٧٠٪ | **٨٥٪** | صيغة واحدة مشتركة + ١٢ اختباراً |
| المزامنة | ٨٠٪ | **٨٢٪** | مؤقّت لكل مفتاح؛ يبقى غياب `serverTimestamp` |
| الأمان والصلاحيات | ٥٥٪ | **٧٠٪** | القواعد صارت جاهزة للأدوار؛ البريد المشترك باقٍ |
| النسخ الاحتياطية | ٤٥٪ | **٨٥٪** | تجزئة + استبقاء + واجهة استرجاع + تنبيه فشل |
| الطباعة | ٥٠٪ | **٧٥٪** | التوجيه ١١/١١ + TSPL؛ QZ محجوب على HTTPS |
| نظافة الكود | ٦٥٪ | **٧٨٪** | كود ميت حُذف، تناقضات وُحّدت |
| الاختبارات | ٦٠٪ | **٧٠٪** | ٩٩ ← **١١١** اختباراً؛ `AppContext` ما زال ناقص التغطية |

**البناء:** ١٩٥١ وحدة بنجاح · **الاختبارات: ١١١/١١١** · ٤٨٬٧٤٧ سطراً في ٧٦ ملفاً.

---

## 1. 🚨 العطل الحرج الجديد — التصفير كان يُحيي المحذوف

**هذا أخطر ما في التقرير، ولم يظهر في أي فحص سابق. وهو يُبطل جوابي السابق لك عن حذف
البيانات المالية.**

### السلسلة كاملةً

1. **`saveAndSync` تكتب في مكانين**: IndexedDB أولاً ثم localStorage
   (`AppContext.jsx:1224`). هذا صحيح ومقصود.
2. **لكن دوال التصفير التسع عشرة لا تكتب إلا في localStorage**:
   `localStorage.setItem('naif_pos_v3_invoices', '[]')` + حذف سحابي.
   **ولا واحدة منها تمسّ IndexedDB** (تحقّقتُ: صفر استدعاء `idbSet` داخلها).
3. **وعند الإقلاع التالي** تعمل `hydrateLargeCollections` بشرط واحد:
   ```js
   setInvoices(prev => (idbInvoices.length > prev.length ? idbInvoices : prev));
   ```
   أي **«الأطول هو الأصحّ»**. و localStorage صار `[]` بينما IndexedDB فيها المئات.
4. **واللقطة السحابية الفارغة لا تُنقذ**: `syncEngine.js:258` تعود مبكراً عند
   `snap.empty` — عمداً وبحقّ (مجموعة فارغة ≠ «احذف كل شيء»، §5.3).

**النتيجة:** مالكٌ يُصفّر حساباته، ثم يعيد تحميل الصفحة **فيجد الفواتير كما كانت**.
والتقارير المحلية و Z-Report تُحسب على بيانات كان يظنّها محذوفة.

> **تصحيح واجب:** قلتُ لك سابقاً إن التصفير «حذف سحابي حقيقي» وإنك تستطيع محو بياناتك
> التجريبية. **السحابة كانت تُمسح فعلاً — والجهاز لا.** الجواب كان ناقصاً.

### الإصلاح

**(أ) `purgeLocalKey(key, emptyValue, stamp)`** — دالة واحدة مشتركة تمسح
**localStorage و IndexedDB معاً** وتكتب ختم التصفير في الاثنين. أُدرجت في
**٢١ موضع تصفير** (بما فيها التصفير الكامل والسنة المالية). و«المسح من مكان واحد لا
يكفي حين يُقرأ من مكانين».

**(ب) الاسترجاع صار يحترم ختم التصفير** — لم يعد الطول دليلاً:
أي نسخة في IndexedDB ختمها `<=` آخر تصفير **تُهمَل وتُمسح**، فلا تتكرّر المحاولة كل
إقلاع. والتعادل يفوز فيه التصفير.

**تحقّق:** `tests/reset_purge.test.mjs` — **١٢/١٢** — يستخرج `purgeLocalKey` من
`AppContext.jsx` حرفياً وينفّذها على مخزنين وهميين. يغطّي **السيناريو الذي كان يُحيي
المحذوف** نصّاً، والبيع بعد التصفير (يجب أن يُحترم)، والخرائط التي تُفرَّغ `{}` لا `[]`،
وحالة تعادل الختم.

---

## 2. ✅ تحقّق مستقلّ من الإصلاحات السابقة — ١٢/١٢ موجودة

لم أعتمد على سجلّي. فحصتُ الكود مباشرةً:

| الإصلاح | الحالة |
|---|---|
| قفل الضغط المزدوج (`isCompletingRef`) | ✅ موجود |
| `lazyWithReload` (النشر لا يكسر التبويبات) | ✅ موجود |
| `resetErrorHandler` (لا نجاح كاذب) | ✅ موجود |
| `recordFailedLoginAttempt` | ✅ موجود |
| `computeExpectedCash` (صيغة واحدة) | ✅ موجود |
| تجزئة النسخ الاحتياطية (`chunks`) | ✅ موجود |
| `CloudBackupsPanel` (واجهة الاسترجاع) | ✅ موجود |
| `tsplLabel.js` | ✅ موجود |
| مؤقّت مزامنة لكل مفتاح | ✅ موجود |
| أدوار `firestore.rules` | ✅ موجود |
| `settings_reset_accounts` | ✅ موجود |
| `usePermission.js` محذوف | ✅ محذوف |

---

## 3. 🟡 أعطال جديدة أخرى — بخطورة دقيقة لا مبالَغة

### ٣.١ ٢٢ استدعاء طباعة بلا `await` — ٧ منها داخل `try/catch` لا يلتقط شيئاً

`printHtmlDirectly` غير متزامنة. سبعة مستدعين يلفّونها بـ `try/catch` ثم `window.print()`
كاحتياط — **والـ catch لا يعمل أبداً** مع الوعود غير المنتظَرة.

**لكن الخطورة أقلّ مما تبدو، وأقولها بدقّة:** فحصتُ الدالة فوجدتُها **لا ترفض إطلاقاً** —
تلتقط كل شيء داخلياً و`resolve(false)` عند الفشل. فالمسار الاحتياطي **كود ميت يُعطي
طمأنينة كاذبة**، لا عطلاً يقع اليوم.

**العطل الحقيقي الباقي:** `printHtmlDirectly` تُرجع `false` عند الفشل و**لا أحد يفحص
القيمة المُرجَعة** — فالطباعة تفشل صامتة ولا يعلم الكاشير.
**الخطورة: متوسطة.** المواضع: `ReceiptModal:108` · `ShiftHeaderModal:268` ·
`TreasuryDropModal:100` · `CurrentShiftDrawerTab:644,1257` · `ShiftsHistoryTab:47` ·
`SettingsScreen:2463`.

### ٣.٢ IndexedDB تُقرأ لمجموعتين فقط

`getSaved` (مصدر الحالة عند الإقلاع) يقرأ **localStorage وحده**. و IndexedDB تُقرأ فقط
في `hydrateLargeCollections` ولمفتاحين: `invoices` و `shifts_history`.

فتعليق `safeLocalStorageSet` الذي يقول عند امتلاء الحصة
«Relying on IndexedDB storage» **غير دقيق**: لا شيء يعتمد عليها في بقية المفاتيح.
**الخطورة: منخفضة** — لأن السحابة هي المرجع عند الإقلاع (`✦ السحابة هي المرجع` في ١٠
مواضع)، فتصحّح أي بيانات محلية بائتة. لكن **جهاز بلا إنترنت لحظة امتلاء الحصة** يعرض
بيانات ناقصة بلا أي تحذير (٢٠٣ استدعاء خام مقابل ١١ آمناً).

> **تصحيح لتقريري الأول:** وصفتُ ترحيل IndexedDB بأنه «تجميلي». الوصف الأدقّ:
> **الكتابة سليمة ومزدوجة، والقراءة ناقصة** — تُقرأ لمفتاحين من ستة عشر.

### ٣.٣ خزينة المدير تُخفي الدرج السالب

`AppContext.jsx:5180`:
```js
return sum + Math.max(0, startCash + cashSales + cashIn - cashOut);
```
درج كاشير سالب (سُحبت عهدته ثم صُرف منه مرتجع) يُحتسب **صفراً** لا سالباً، فمجموع
«نقد الكاشيرين» في شاشة المدير **يزيد عن الحقيقة**.

وهذا **يناقض قراراً اتُّخذ في نفس الجلسة**: `computeExpectedCash` تُرجع السالب كما هو،
ومكتوب في اختبارها أن «السالب حقيقة محاسبية لا خطأ يُخفى بـ `Math.max(0, …)`».

**لم أُصلحه** — قد يكون `Math.max` مقصوداً لتفادي أن يُطرح درج سالب من إجمالي العهدة.
**هذا قرار محاسبي للمالك**، وتوثيق المشروع ينصّ على عدم تغيير قرارات من هذا النوع بلا إذن.

---

## 4. ✅ ما فُحص ووُجد سليماً

- **قيود الخزينة**: المصروف يُخزَّن `-numAmount` والإيراد `+numAmount`
  (`AppContext.jsx:3840`)، فـ `managerVaultCash = Σ amount` صحيح رياضياً، و
  `totalManagerExpenses` تستعمل `Math.abs` للعرض فقط. **لا عطل.**
- **لا تسرّب مؤقّتات**: كل `setInterval` له `clearInterval` مقابل.
- **لا تسرّب مستمعي أحداث**: كل `addEventListener` له `removeEventListener`.
- **لا شروط ميتة باقية** (`? x : x`) — بحثتُ بنمط regex عن كلها.
- **لا أسرار في حزمة الإنتاج**: صفر تطابق لـ `pin/password/secret/token` بقيمة صريحة.
- **الحسابات الأساسية** تحقّقت حيّاً في الجلسة: البيع (`TDNU-0004`) والمرتجع (المخزون
  ٢←٤) والشبكة والتقسيم (٧٢٫٥+٧٢٫٥=١٤٥) والآجل (رصيد العميل ٠←١٠).

---

## 5. ⏸️ مؤجّلان بقرار واعٍ — والسبب واحد

| البند | لماذا أُجّل |
|---|---|
| **عدّادات الورديات كقيم مطلقة** | الإصلاح الصحيح ضمّ `user_shifts` لـ `INCREMENT_OWNED` وتحويل كل كتّاب `cashSales`/`cashIn`/`cashOut` إلى `adjustFields` |
| **`serverTimestamp`** | المزامنة تحتكم لساعة الجهاز (١٣ `Date.now()` مقابل صفر `serverTimestamp`) |

كلاهما **تغيير واسع في مسار نقدي لا يمكن التحقّق منه حيّاً** بلا تسجيل دخول وجهازين.
وتنفيذ تغيير كهذا بلا تحقّق أخطر من العطل نفسه. ويخفّف من أثر الأول أن
`computeExpectedCash` صارت تُعيد الحساب من السجلات الأصلية لا من العدّادات.

---

## 6. 🔐 ما ينتظر قراراً منك

**١. بريد Firebase لكل جهاز — أكبر ثغرة باقية.**
القواعد **جاهزة** الآن (`isSupervisor`/`isAccountant`/`isCashier` و `canSeeFinancials`
و `canManageInventory`، منشورة). لكنها **بلا أثر** حتى تُنشئ `f1`…`f4` في Firebase
Authentication وتُسجّل كل جهاز ببريده. اليوم كل جهاز يدخل ببريد المدير فيمرّ من
`isAdmin()`، أي أن كل كاشير مديرٌ في السحابة.
⚠️ **أثر جانبي مقصود**: بعد التحويل لن تُرفع النسخ الاحتياطية إلا من جهازك.

**٢. الدرج السالب في خزينة المدير** (بند ٣٫٣) — قرار محاسبي.

---

## 7. ❌ ما لم يُتحقّق منه تشغيلياً

- شاشة النسخ السحابية الجديدة، ومفتاح TSPL، وتبويب أداء الموظفين — **تحتاج تسجيل دخول**.
- TSPL على طابعة حقيقية — يحتاج QZ مثبَّتاً وشهادته موثوقة.
- QZ على HTTPS **محجوب أصلاً** بـ Mixed Content (`ws://` يُحظر، و`wss://` بلا شهادة).
- دورة تشغيلية متبقّية: شراء من مورد وصرف · مصروف بكل الطرق · الخزينة · التقارير.

---

## 8. خطة مرتّبة

**اليوم**
1. **أعد تشغيل التصفير** إن كنت قد صفّرت سابقاً — النسخة القديمة كانت تُبقي البيانات
   في IndexedDB. النسخة الحالية تمسحها فعلاً.
2. تحقّق بصرياً من شاشة النسخ السحابية ومفتاح TSPL.

**هذا الأسبوع**
3. أنشئ `f1`…`f4` في Firebase وسجّل كل جهاز ببريده.
4. افحص القيمة المُرجَعة من `printHtmlDirectly` في المواضع السبعة وأظهر خطأً عند `false`.

**لاحقاً**
5. `user_shifts` إلى `INCREMENT_OWNED` (يحتاج بيئة اختبار بجهازين).
6. `serverTimestamp` بدل ساعة الجهاز.
7. تغطية اختبارية حقيقية لـ `AppContext` (ما زال ٦٧٩٢ سطراً بلا اختبار مباشر).
