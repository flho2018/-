# تقرير الفحص الشامل الثاني — بيت الورد

**التاريخ:** 2026-09-16 · **النطاق:** القوائم، الحسابات، المزامنة، المستخدمون، Firebase، الإعدادات، النسخ الاحتياطية، الطباعة، التكرار

> **ما هو هذا التقرير:** فحص ساكن للكود (٤٨٬٠٢٦ سطراً في ٧٦ ملفاً) + فحص حيّ للتطبيق
> المنشور من داخله + قراءة الحالة الفعلية من `localStorage` + فحص رؤوس النشر بـ `curl`.
> **ما ليس فيه:** لم تُنفَّذ عمليات بيع أو إرجاع حقيقية للتحقّق من الأرقام تشغيلياً.

---

## 0. النتيجة: **٧٢٪ صالح للعمل**

| المحور | النسبة | الحكم |
|---|---|---|
| الحسابات المالية (ضريبة، خصم، تقسيم) | ٩٢٪ | المنطق الأساسي سليم ومطابق لـ ZATCA |
| حسابات الوردية والدرج | ٧٠٪ | صحيحة اليوم لكن بصيغتين متوازيتين تتعايشان بالصدفة |
| المزامنة متعدّدة الأجهزة | ٨٠٪ | معمارية ناضجة، وثغرة واحدة في عدّادات الورديات |
| الأمان والصلاحيات | ٥٥٪ | القواعد جيّدة، لكن بريد واحد مشترك يُبطل أثرها |
| النسخ الاحتياطية | ٤٥٪ | تعمل الآن، وتتوقّف حتماً بعد بضعة أشهر |
| الطباعة | ٥٠٪ | نصف المسارات موجَّهة ونصفها لا |
| نظافة الكود (تكرار/ميت) | ٦٥٪ | تكرار حقيقي في ثلاثة مواضع مالية |
| الاختبارات | ٦٠٪ | ٤٨ اختباراً، لكن `AppContext` (٦٧٩٢ سطراً) بلا تغطية |

**لماذا ٧٢٪ وليس أعلى:** البرنامج يبيع ويحاسب صحيحاً اليوم. الخصم عن المئة ليس في
منطق البيع، بل في **ثلاثة أشياء تنفجر لاحقاً**: النسخة الاحتياطية التي ستتوقّف صامتة،
والبريد المشترك الذي يجعل كل كاشير مديراً في السحابة، والصيغ المالية المكرّرة التي
ستفترق أول ما يُعدّل أحدها.

---

## 1. الطرق الحسابية

### ✅ سليم ومُتحقَّق منه

- **الضريبة** (`AppContext.jsx:1597` `getCartTotals`) — الحالتان صحيحتان رياضياً:
  شاملة (`total / 1.15` ثم الفرق) وغير شاملة (`base × 0.15`). والخصم النسبي يُحسب على
  الصافي بعد خصومات الأصناف لا على الإجمالي — وهذا هو الصحيح محاسبياً.
- **التقسيم** — يُرفض حفظ فاتورة مجموع دفعاتها ≠ إجماليها بفارق > ٠٫٠٠٥
  (`CheckoutModal.jsx:374`). قبلها كانت السماحية ٠٫٠٢ فكان تقرير وسائل الدفع يفترق
  عن إجمالي المبيعات دائماً.
- **عمولة التسوية** (`helpers.js:774`) — نسبة + ثابت لكل عملية + ضريبة على العمولة. صحيح.
- **محرّك أداء الموظفين** (`staffPerformance.js`) — ١٩ اختباراً، والمرتجع يدخل فترة
  وقوعه لا فترة الفاتورة.
- **النقدية المتوقّعة** — `startCash + cashSales − refunds + cashIn − cashOut − expenses − purchases`،
  ولا ازدواج: المصروف مستثنى من `cashOut` لأنه يُخصم من قائمة المصروفات، والمرتجع
  مستثنى لأنه يُخصم من `userCashRefunds`. مدروس فعلاً.

### ⚠️ صحيح اليوم، هشّ غداً

**١. صيغتان متوازيتان للنقدية.** هناك طريقتان مختلفتان لحساب نقد الدرج:

- **(أ) إعادة حساب من السجلات** — `ShiftHeaderModal.jsx:107` و `CurrentShiftDrawerTab.jsx:447`.
- **(ب) عدّادات تراكمية على الوردية** — `Dashboard.jsx:102`، `ManagerTreasuryTab.jsx:1581`،
  `helpers.js:300` (رسالة واتساب)، `AppContext.jsx:5118` (خزينة المدير).

تعطيان اليوم نفس الرقم، لكن بالصدفة: كل عملية تُحدَّث في المكانين يدوياً. أي عملية
جديدة يُنسى أحد طرفيها تجعل لوحة التحكّم تقول رقماً وتقرير Z يقول آخر. **والصيغة (أ)
مكرّرة حرفياً في ملفين** — تعديل أحدهما بلا الآخر يفتح الخلاف فوراً.

**٢. `shift.drawerExpenses` حقل ميت.** يُقرأ في `AppContext.jsx:2188` و `:5119` ولا يُكتب
في أي مكان، فقيمته صفر دائماً. النتيجة صحيحة **بالمصادفة** (المصروف محسوب أصلاً في
`cashOut`)؛ لو بدأ أحد بكتابته لخُصمت المصروفات مرتين في خزينة المدير.

**٣. فاتورة المشتريات النقدية لا تُنشئ سطراً في حركات الدرج.** `AppContext.jsx:3344`
يزيد `shift.cashOut` فقط. الكاشير يرى نقديته نقصت ولا يجد ما يفسّرها — وهي نفس
المشكلة التي عولجت للمصروفات بتعليق صريح في `:3724`، وبقيت في المشتريات.

**٤. تصنيف المرتجع يختلف بين شاشتين.**

- `helpers.js:716 buildPaymentBreakdown` (التقارير): يتجاهل الفاتورة المرتجعة كلياً.
- `useShiftMetrics.js classifyInvoicePayments` (الوردية/الدرج): يحتسب **الجزء النقدي**
  من الفاتورة المرتجعة ويتجاهل الشبكة والآجل.

للدرج هذا صحيح (النقد خرج فعلاً ويُخصم لاحقاً كمرتجع)، لكن نفس الكائن يُعرض أيضاً
كـ«مبيعات حسب الوسيلة»، فيظهر نقد فاتورة مرتجعة كمبيعات في شاشة ولا يظهر في أخرى.

**٥. `calculateInvoicePaymentBreakdown` يثبّت أسماء الوسائل في الكود** (`tamara`, `ninja`,
`visa`). أي وسيلة جديدة تضيفها من الإعدادات تسقط في سلّة `card`.

---

## 2. المزامنة

### ✅ ما هو سليم

- الحقول التراكمية (`stock`, `balance`) تُزامَن بـ `increment` لا بالقيمة المطلقة.
- لا حذف ضمني: غياب سجل من مصفوفة جهاز لا يحذفه.
- النسخة الأقدم لا تُكتب فوق الأحدث (مقارنة `updatedAt`).
- مجموعة سحابية فارغة لا تُفسَّر «امسح كل شيء».
- لقطة من نفس `_src` تُتجاهل، واللقطة الأولى استثناء صريح.
- ذاكرة Firestore الرسمية (`persistentLocalCache` + `MultipleTabManager`) مفعّلة.

### ⚠️ الثغرات

**١. عدّادات الورديات تُزامَن كقيم مطلقة.** `INCREMENT_OWNED` يغطّي
`products/customers/suppliers` فقط. أما `user_shifts` فحقوله التراكمية
(`cashSales`, `cashIn`, `cashOut`) تُكتب كقيمة كاملة. إذا حدّث جهازان نفس الوردية
(المدير يغلق وردية كاشير من جهازه بينما الكاشير يبيع) **يفوز آخر كاتب ويضيع تحديث
الآخر** — وهي نقود.

**٢. مؤقّت تأجيل واحد لكل المفاتيح** (`syncEngine.js:596`). كل حفظ جديد يُلغي المؤقّت،
فتحت الضغط المتواصل قد لا يُفرَّغ شيء حتى تهدأ الحركة ٤٠٠ مللي ثانية.

**٣. لا `serverTimestamp` إطلاقاً** — صفر استخدام في `syncEngine.js` مقابل ١٣ استخداماً
لـ `Date.now()`. جهاز ساعته متأخّرة نصف ساعة سيخسر كل تعارض، أو يفوز بنسخة قديمة.

---

## 3. المستخدمون وسجلاتهم — وهل نحتاج بريداً لكل دور؟

### الحالة الفعلية المرصودة حيّاً

خمسة مستخدمي POS (نايف/مشرف، روان/كاشير، فاطمة/كاشير، نورة/محاسبة، مدير المتجر/admin)،
كلهم بتجزئة `v2$…` سليمة ولا أحد بكلمة سرّ صريحة. الجهاز المفحوص كان داخلاً ببريد
**`fl.ho2018@gmail.com`** — بريد المدير — بينما المستخدم العامل عليه كاشير.

### ⛔ الإجابة: نعم، تحتاج بريداً مستقلاً لكل جهاز/دور — وهي أهمّ ثغرة في النظام

قواعد Firestore تعرف هويتك **بالبريد وحده** (`isAdmin()` = `email() == 'fl.ho2018@gmail.com'`).
ودور المستخدم في البرنامج (من رمز PIN) **لا علاقة له بالسحابة إطلاقاً**.

فإذا دخل جهاز الكاشير ببريد المدير:

| ما تمنعه الواجهة عن الكاشير | ما تسمح به السحابة فعلياً |
|---|---|
| حذف فاتورة | ✅ مسموح |
| تعديل سعر البيع أو التكلفة | ✅ مسموح |
| تعديل مستخدم أو رقم دخوله | ✅ مسموح |
| قراءة سجل التدقيق | ✅ مسموح |
| قراءة وحذف النسخ الاحتياطية | ✅ مسموح |

الصلاحيات في هذا البرنامج **تُفرَض في المتصفّح وحده**. من يفتح أدوات المطوّر على جهاز
كاشير داخل ببريد المدير يتجاوزها كلها بسطر واحد. القواعد المكتوبة بعناية في
`firestore.rules` تصبح بلا أثر.

**الخطة الموصى بها:**

1. الحسابات موجودة أصلاً في `authUsers.js` (`f1`…`f4`) — أنشئها في Firebase Authentication
   إن لم تكن منشأة، وسجّل كل جهاز ببريده: جهاز الكاشير بـ `f3`، الثاني بـ `f4`،
   المحاسبة بـ `f2`، المشرف بـ `f1`، وجهازك وحدك بـ `fl.ho2018`.
2. `firestore.rules` تحتاج تفصيلاً للأدوار: حالياً كل من في `isStaff()` سواء. أضف
   `isAccountant()` و `isSupervisor()` واربط بها التقارير والمشتريات والمصروفات.
3. ⚠️ **أثر جانبي يجب معرفته قبل التنفيذ:** `pos_users` و `pos_backups` كتابتهما
   `isAdmin()` فقط. بمجرد تحويل جهاز لبريد كاشير: لن تُزامَن تعديلات المستخدمين منه،
   ولن تُرفع النسخ الاحتياطية التلقائية منه (وهذا مقصود وصحيح — لكن تأكّد أن جهازك
   يعمل يومياً وإلا انقطعت النسخ).

### سجلات المستخدمين

- ✅ يُسجَّل: الوقت، المعرّف، الاسم، الدور، طريقة الدخول (PIN/NFC/تبديل)، مواصفات الجهاز.
- ❌ **لا تُسجَّل محاولات الدخول الفاشلة إطلاقاً** — `status: 'success'` ثابت في الكود
  (`AppContext.jsx:5900`)، والدالة لا تُستدعى إلا عند النجاح. فسجل الأمان لا يُظهر
  محاولة تخمين رقم أبداً.
- ❌ `roleLabel` يصنّف غير المدير كلهم «كاشير مبيعات 👤» — المشرف والمحاسبة يظهران
  كاشيرين في سجل الدخول (`AppContext.jsx:5896`).
- ⚠️ السجل يُقصّ محلياً عند ٣٠٠ دخول و١٠٠٠ قيد تدقيق. السحابة تحتفظ بالكل، لكن الشاشة
  لا تعرض ما بعدها.
- ⚠️ **كائن الصلاحيات لقطة جامدة** (`permissions.js:330`): من عنده `permissions` يُفحص
  داخله فقط ولا يُرجَع لقالب دوره. فأي صلاحية جديدة تُضاف للنظام مستقبلاً **تُمنَع
  صامتة عن كل الموظفين الحاليين** حتى تُعاد حفظ بطاقاتهم يدوياً.

---

## 4. Firebase

- ✅ القواعد محكمة ومكتوبة بأسلوب «تثبيت الحقول الحسّاسة» المناسب لمحرّك يكتب المستند
  كاملاً. `pos_audit_logs` تعديله وحذفه `false` — سجل تدقيق حقيقي.
- ✅ `pos_spoilage_logs` صارت لها قاعدة ومنشورة (كانت الكتابة تفشل بـ `permission-denied`).
- ✅ مفتاح `apiKey` في الحزمة أمر طبيعي في Firebase للويب (ليس سرّاً؛ حمايته المُحيلات
  والقواعد) — لا إجراء مطلوب.
- ⛔ **كل الأمان معلّق على خمسة عناوين بريد مثبّتة في نصّ القواعد.** إضافة موظف تتطلّب
  تعديل القواعد ونشرها. ولا تمييز بين مشرف ومحاسب وكاشير — كلهم `isStaff()`.
- ⚠️ **رؤوس ذاكرة النشر كانت معطوبة** (أُصلحت اليوم): `no-cache` كانت على `/index.html`
  وحده والجذر `/` لا يطابقه، فكان كل جهاز يبقى على النسخة القديمة حتى ساعة بعد كل نشر.

---

## 5. النسخ الاحتياطية

- ✅ تُرفع فعلاً إلى `pos_backups` يومياً من جهاز المدير (تحقّقتُ سابقاً برفع ٥٠ ك.ب).
- ✅ في السحابة لا في المتصفّح — نسخة تعيش داخل ما تحميه ليست نسخة.

### ⛔ قنبلة موقوتة: النسخة تتوقّف حتماً خلال أشهر

`syncEngine.js:811` يكتب النسخة كلها في **مستند Firestore واحد بلا تجزئة**. والحدّ
الصلب لمستند Firestore **١ ميجابايت**. نسختك اليوم ٥٠ ك.ب بـ٧١ صنفاً و٥ فواتير. الفاتورة
الواحدة ببنودها وتكلفتها ودفعاتها ≈ ١–٢ ك.ب، فعند حوالي **٥٠٠–١٠٠٠ فاتورة** يتجاوز
المستند الحدّ ويرمي `INVALID_ARGUMENT`.

وما يجعلها خطيرة أنها **تفشل صامتة**: الكود يكتفي بـ `console.warn`، ولا ينبّه المالك،
ولا يكتب الختم — فيعيد المحاولة ويفشل كل يوم إلى الأبد. تظن أنك محمي وأنت بلا نسخة
منذ شهور. **هذا أخطر ما في التقرير.**

### ⚠️ أعطال أخرى

- **لا سياسة استبقاء**: النسخ تتراكم بلا حدّ ولا حذف تلقائي للقديم.
- **`listBackups` ليست خفيفة رغم تعليقها.** `getDocs` على المجموعة كلها يُنزّل كل
  مستند **بكامل بياناته** ثم يحذف الحقل في المتصفّح. فتح شاشة النسخ بعد سنة = تنزيل
  مئات الميجابايت وتكلفة قراءات Firestore مقابل قائمة أسماء.
- **الاسترجاع بلا معاينة**: `importBackup` بوضع `replace` يستبدل كل شيء دفعةً.

---

## 6. الطباعة

- ✅ نقطة طباعة واحدة `printHtmlDirectly` + طبقة QZ مع ارتداد آمن للطباعة العادية.
- ✅ **مسارات موجَّهة** (تحترم إعداد الطابعات): ملصقات الباركود، تقرير Z، سندات الترحيل
  والاستلام والإيداع وتغذية الدرج، وفاتورة البيع التلقائية بعد الدفع.

### ⛔ مسارات غير موجَّهة — الإعداد موجود ولا أثر له

١١ استدعاءً مباشراً يتجاوز التوجيه ويذهب لطابعة ويندوز الافتراضية:

| الملف | ما يُطبع |
|---|---|
| `ReceiptModal.jsx:108` | **إعادة طباعة الفاتورة يدوياً** |
| `CustomersScreen.jsx:153, 375` | سند قبض عميل، كشف حساب |
| `InvoicesScreen.jsx:512` | سند قبض |
| `PaymentsAndVatTab.jsx:139, 371` | إقرار ZATCA، التسوية المالية |
| `StaffAndShiftsTab.jsx:238, 334, 580` | تقارير الأداء و Z |
| `ManagerTreasuryTab.jsx:388` | تقرير الخزينة |

النتيجة العملية: **نفس الفاتورة تخرج من طابعة مختلفة حسب طريقة طباعتها** — تلقائياً
بعد الدفع تحترم الإعداد، ويدوياً من نافذة الإيصال لا تحترمه.

### ⚠️ أخرى

- **`printRawViaQz` مكتوبة ولا تُستدعى** — فطباعة الباركود ما زالت HTML لا TSPL، وهذا
  جوهر شكوى «الباركود غير احترافي»: المقاس بالملّيمتر لا يُضبط عبر HTML.
- الملصقات تُطبع بـ `window.print()` عند فشل الدالة (`ProductsScreen.jsx:291`) — يطبع
  الصفحة كلها لا الملصق.
- نافذة الطباعة الأصلية تُجمّد الواجهة حتى تُغلق يدوياً.

---

## 7. القوائم والشاشات

١١ شاشة رئيسية، ١٠ تبويبات تقارير، ١٤ تبويب إعدادات. البنية منطقية والتحميل كسول
(code-splitting) فالحزمة الأولى خفيفة.

### ⚠️ ملاحظات

1. **`userReports` قائمة مكرّرة.** بند «تحليلات الكاشيرات» في القائمة الجانبية يفتح
   `<ReportsScreen defaultTab="staff" />` — أي **نفس التبويب** الموجود داخل التقارير
   باسم «👥 تقرير أداء الكاشيرات». مدخلان لشاشة واحدة.
2. **تبويب «التصفير» محميّ بالصلاحية الخطأ.** أخطر شاشة في النظام (`ResetAccountsTab`،
   ١١٦٩ سطراً) تظهر لمن يملك `settings_cloud_sync_backup` — أي صلاحية النسخ الاحتياطي،
   لا صلاحية تدمير. تستحق مفتاحاً خاصاً بها.
3. **`canAccessModule` تسمح عند المفتاح المجهول** (`permissions.js:410`). شاشة جديدة
   تُضاف بلا تسجيلها في الخريطة تُفتح للجميع افتراضياً — الافتراض الآمن هو المنع.
4. تبويب «المظهر» بلا أي فحص صلاحية.

---

## 8. المكرر وغير الضروري

### كود ميت مؤكَّد

| الملف/الدالة | الحجم | الحالة |
|---|---|---|
| `src/hooks/usePermission.js` | ١٢٦ سطراً | **لا يستورده أحد إطلاقاً** — ملف كامل ميت |
| `helpers.js: generateBarcode` | — | مولّد الباركود العشوائي القديم، حلّ محلّه التسلسلي |
| `qzPrint.js: printRawViaQz` | — | مكتوبة ولا تُستدعى (مسار TSPL) |
| `qzPrint.js: isQzConnected, disconnectQz, getDefaultPrinter, PRINT_PURPOSES` | — | غير مستعملة |
| `shareHelper.js: captureElementImage, captureHtmlImage, imageToPdfBlob, downloadBlob, openWhatsApp` | ٥ من ٨ | غير مستعملة |
| `idbStorage.js: openIDB, idbRemove, idbGetAllKeys` | — | غير مستعملة |
| `security.js: sha256` · `audit.js: AUDIT_LABELS` · `themes.js: FONT_SIZES` | — | غير مستعملة |
| `shift.drawerExpenses` | — | حقل يُقرأ ولا يُكتب |
| `HOLD_BILLS_ENABLED = false` | — | كل منطق الفواتير المعلقة معطّل وباقٍ في الملف |

### منطق مكرّر (الأخطر)

1. **ثلاث نسخ من تصنيف وسائل الدفع**: `calculateInvoicePaymentBreakdown` +
   `buildPaymentBreakdown` (helpers.js) + `classifyInvoicePayments` (useShiftMetrics.js).
   الثلاثة تفعل الشيء نفسه بقواعد مرتجع مختلفة.
2. **صيغة النقدية المتوقّعة مكرّرة حرفياً** في `ShiftHeaderModal.jsx:107` و
   `CurrentShiftDrawerTab.jsx:447`.
3. **مساران لبناء HTML الفاتورة**: `printInvoiceDirectly` (موجَّه) و `buildReceiptHtml`
   داخل `ReceiptModal` (غير موجَّه).
4. **`AppContext.jsx` = ٦٧٩٢ سطراً** — ١٤٪ من الكود كلّه في ملف واحد، وهو الملف الذي
   يحذّر توثيق المشروع نفسه من أن تعديله أثناء التشغيل يكسر الواجهة.

---

## 9. أخطر خمسة — بالترتيب

| # | العطل | الأثر | الموضع |
|---|---|---|---|
| ١ | النسخة الاحتياطية في مستند واحد بحدّ ١ م.ب | تتوقّف صامتة خلال أشهر — بلا نسخة وأنت تظنّ العكس | `syncEngine.js:811` |
| ٢ | بريد Firebase مشترك بين الأجهزة | كل كاشير مديرٌ في السحابة؛ القواعد بلا أثر | `firestore.rules:30` |
| ٣ | لا قفل ضدّ الضغط المزدوج على «تأكيد الدفع» | فاتورتان ومخزون مخصوم مرتين بلمسة مزدوجة | `CheckoutModal.jsx:973` |
| ٤ | التوجيه غائب عن ١١ مسار طباعة | إعداد الطابعات بلا مفعول في نصف الحالات | `printHelper.js` وما يستدعيها |
| ٥ | عدّادات الورديات تُزامَن كقيم مطلقة | ضياع نقد عند تحرير وردية من جهازين | `syncEngine.js:69` |

> ملاحظة مقارنة: الاسترجاع **محميّ** بقفل (`refundingIdsRef`)، والبيع لا. الحماية موجودة
> في المكان الأقل تكراراً وغائبة عن الأكثر تكراراً.

---

## 10. خطة مرتّبة بالأولوية

**اليوم**

1. قفل الضغط المزدوج على زر تأكيد الدفع. (سطر واحد، يمنع فواتير مكرّرة)
2. تنبيه ظاهر للمالك عند فشل النسخة الاحتياطية بدل `console.warn`.

**هذا الأسبوع**

3. تجزئة النسخة الاحتياطية على عدة مستندات + سياسة استبقاء + جعل `listBackups` خفيفة فعلاً.
4. تمرير `{ purpose, storeInfo }` في المسارات الإحدى عشر الناقصة.
5. توحيد صيغة النقدية المتوقّعة في دالة واحدة مشتركة، وحذف `drawerExpenses`.

**هذا الشهر**

6. بريد Firebase مستقل لكل جهاز + تفصيل `firestore.rules` للأدوار.
7. تسجيل محاولات الدخول الفاشلة.
8. إضافة `user_shifts` لعائلة `increment` أو تحويل عدّاداتها إلى إعادة حساب.
9. حذف الكود الميت (يبدأ بـ `usePermission.js`).
10. `printRawViaQz` بـ TSPL لملصقات الباركود — علاج جذر شكوى «غير احترافي».
