# تقرير الفحص الشامل — نظام «بيت الورد» لنقاط البيع
**التاريخ:** ١٣ سبتمبر ٢٠٢٦ · **النطاق:** فحص للقراءة فقط لكامل الكود (`D:\FL-HO2018`) · **المنهجية:** خمسة محاور متخصصة (الحسابات، الأمان والصلاحيات، محرّك المزامنة، التحقق من المدخلات والحقن، قواعد Firestore) + تحقق يدوي من النتائج الحرجة.

> ⚠️ **تنبيه:** هذا التقرير يصف ثغرات في نظام إنتاج حقيقي. لا تُشارك الملف علناً. احفظه خارج أي مستودع عام.

---

## ١. الملخّص التنفيذي

النظام مبنيّ بعناية محاسبية وأمنية عالية في جوهره (قيود مزدوجة، تجزئة PIN بـ PBKDF2، منع الحذف الضمني، تثبيت حقول الفاتورة، حماية الضغط المزدوج). لكن الفحص كشف **فجوة جوهرية واحدة تُضعف كل الطبقات**: الصلاحيات الدقيقة (٤٨ مفتاحاً) والحماية الحسّاسة **غير مفروضة على الخادم فعلياً** — إما لأن القواعد تفرّق فقط بين «مدير» و«موظف»، أو لأن تثبيت الحقول يشير إلى **أسماء حقول خاطئة** فلا يحمي شيئاً. أي حساب موظف يستطيع الكتابة مباشرة إلى Firestore متجاوزاً كل حرّاس الواجهة.

### عدّاد النتائج حسب الخطورة

| الخطورة | العدد | الأمثلة الأبرز |
|---|---|---|
| 🔴 CRITICAL | ٤ | حماية سعر/تكلفة المنتج معطّلة في القواعد · اجتياز مسار + ربط `0.0.0.0` في الخادم المحلي · فقد مخزون/رصيد عند المزامنة المتزامنة · حقن HTML عبر اسم المنتج في الملصق |
| 🟠 HIGH | ١١ | الصلاحيات الدقيقة غير مفروضة خادمياً · `status`/`isCreditSettled` غير مثبّتة · أرصدة العملاء/الموردين غير محمية · حرّاس التنفيذ في العميل فقط · خطأ برمجي في تسجيل المشتريات النقدية · طول TLV/الرقم الضريبي الاحتياطي · حقن صيَغ Excel |
| 🟡 MEDIUM | ~١٤ | مبالغة ربح المنتجات (الضريبة/الخصم) · مرتجع يعدّل تقارير سابقة · نجاح كاذب قبل تأكيد الحفظ · presets متضاربة · PIN افتراضي ضعيف · PIN صريح في بيانات البذرة |
| ⚪ LOW | ~١٢ | تصادم معرّف الفاتورة · نواقص تغطية اختبارات القواعد · `.gitignore` ينقصه `*.pem` · تعارض projectId · إلخ |

**أُصلح في هذه الجلسة:** خطأ `permission-denied` في سجل التدقيق (توحيد المسار عبر `serverTimestamp`)، ترقية `xlsx` المهجورة، حذف ملفات `.bak`، ونشر قواعد Firestore.

---

## ٢. سير الحسابات (Accounting Flow) — كما هو فعلاً في الكود

**البيع** (`checkout`, `AppContext.jsx:1602`): تُحسب الإجماليات في `getCartTotals()` (`:1450`): إجمالي الأصناف (سعر×كمية) ← ناقص خصومات الأصناف ← ناقص الخصم العام ← ثم الضريبة (شاملة ١٥٪ افتراضياً، تُستخرَج رياضياً). تُثبَّت الفاتورة مع `costAtSale` لكل صنف، ثم أثر مزدوج: (١) خصم المخزون بالفرق (`adjustStock`)؛ (٢) إن آجلاً تُزاد `customer.balance`؛ (٣) تُحدَّث حقول الوردية (`cashSales/cardSales/creditSales`)؛ (٤) تُحفظ الفاتورة.

**الدرج والوردية:** كل بيع نقدي يدخل درج الكاشير؛ المصروفات/المشتريات النقدية والمرتجع النقدي والترحيل للخزينة تخرج منه. عند `closeShift` (`:3653`) يُعاد احتساب كل شيء **من الفواتير الفعلية لا من الحقول الحيّة**:
`expected = startCash + النقد المُباع − المرتجع النقدي + الوارد − الصادر − المصروفات − المشتريات`، ثم `difference = actual − expected` (تقرير Z).

**الخزينة:** الكاشير يُرحّل نقداً (`treasury_drop`) أو يُسلّم عهدته عند الإقفال (`shift_handover`) فتدخل دفتر الخزينة. مبيعات الشبكة/التطبيقات تبقى «معلّقة» حتى `reconcilePosSettlement` (تسجّل الإجمالي والعمولة والصافي المودع). الإيداع البنكي يُخرج من كاش المدير (سالب) إلى البنك — قيد مزدوج متوازن.

**التقارير:** `ReportsScreen`/`Dashboard`/`FinancialOverviewTab` تصفّي الفواتير بالفترة، تستبعد المرتجعة، وتحسب COGS من `costAtSale × qty`، والربح الإجمالي = المبيعات − الضريبة − التكلفة − المصروفات.

> **الحكم:** الهيكل المحاسبي سليم ومتوازن في المسار الأساسي. الأخطاء منحصرة في (أ) تفصيل «ربح المنتجات» في التقارير، (ب) معالجة المرتجع بأثر رجعي، (ج) خطأ برمجي واحد في المشتريات النقدية، (د) عدم تسجيل فرق العهدة.

---

## ٣. الأخطاء الحرجة 🔴 CRITICAL

### C-1 · حماية سعر/تكلفة المنتج في القواعد معطّلة تماماً (تحقّقت يدوياً)
- **الملف:** `firestore.rules:64` مقابل `AppContext.jsx:2245-2246`, `2310-2311`.
- القاعدة تثبّت `same('price') && same('cost')`، لكن مستند المنتج يستخدم `sellingPrice` و`costPrice`. وتعريف `same()` (`firestore.rules:45`) يعتبر **الحقل الغائب في الطرفين «لم يتغيّر»** → `same('price')` و`same('cost')` تُرجعان `true` دائماً، والحقلان الحقيقيان **بلا أي حماية**.
- **الاستغلال:** أي كاشير (`isStaff`) يكتب `pos_products/<id>` مباشرة من أدوات المطور ويغيّر سعر البيع أو التكلفة (مع إبقاء `barcode` كما هو).
- **الإصلاح:** `same('sellingPrice') && same('costPrice') && same('barcode')` (واحذف `createdAt` غير الموجود).

### C-2 · الخادم المحلي: اجتياز مسار + استماع على كل الواجهات (الإصلاح غير مطبَّق)
- **الملفات:** `create_new_pc_package.cjs` (الخادم المولَّد `local_server.js`, ~سطر ٤٥/٧٠) و`تشغيل_البرنامج_محليا.bat` (~سطر ٤٢/٦٢).
- `path.join(baseDir, reqPath)` بلا تطبيع/تحقق احتواء → `GET /../../../../Windows/win.ini` يُخدَم. و`server.listen(PORT, '0.0.0.0')` يُعرّض المتجر كاملاً لكل جهاز على الشبكة.
- **الإصلاح:** `const resolved = path.normalize(path.join(baseDir, reqPath)); if(!resolved.startsWith(baseDir)) return 403;` + `server.listen(PORT, '127.0.0.1')`. **مهم:** تأكّد أن النسخة المصححة هي التي تُوزَّع فعلاً على الأجهزة الجديدة.

### C-3 · حقن HTML عبر اسم المنتج في ملصق الباركود
- **الملف:** `printHelper.js:426` (وأيضاً `storeName` :425 و`footerText` :436).
- اسم المنتج مُهرَّب في الإيصال (`esc(itemName)` :132) لكنه **غير مهرَّب** في الملصق، ويُقحَم في `<title>` (:454، :524)، ويُنفَّذ عبر `document.write` داخل `iframe` (:64). اسم يُدخله الكاشير مثل `<img src=x onerror=...>` → تنفيذ/تشويه. مسار عبر-صلاحيات (الكاشير أدنى صلاحية).
- **الإصلاح:** `esc(prodName)`, `esc(storeName)`, `esc(footerText)`.

### C-4 · المزامنة: الكتابة الكاملة تمحو خصم المخزون/الرصيد المتزامن
- **الملفات:** `syncEngine.js:471-502` (`adjustFields`), `AppContext.jsx:2306-2317` (`updateProduct`).
- البيع يخصم المخزون بـ `increment` **دون رفع `updatedAt`**، وتعديل بيانات الصنف يكتب المصفوفة كاملةً بقيمة `stock` من ذاكرة الجهاز المُحرِّر **دون `updatedAt`**. حارس «لا نكتب الأقدم فوق الأحدث» (`:659-680`) يقارن `updatedAt` فقط؛ فإذا كان الطرفان بلا ختم يُلغى الحارس → آخر كاتب يفوز.
- **السيناريو:** الجهاز A يبيع (١٠→٧)، والجهاز B يعدّل السعر (يرى ١٠) ويحفظ → يكتب `stock:10` فوق السحابة → **خصم البيع ضاع**. نفس الشيء على `balance` العملاء/الموردين.
- **الإصلاح:** استبعاد `stock`/`balance` من الكتابة الكاملة (اعتماد `increment` مصدراً وحيداً لهما)، ورفع `updatedAt` في كل تعديل.

---

## ٤. الأخطاء العالية 🟠 HIGH

### H-1 · الصلاحيات الدقيقة (٤٨ مفتاحاً) غير مفروضة على الخادم
- `firestore.rules:29-41` تعرف `isAdmin`/`isStaff` فقط. كل صلاحية واجهة (مرتجع، خصم، سندات، مشتريات، مصروفات، تعديل سعر) يتجاوزها أي موظف مصادَق بكتابة مباشرة. **هذا يضخّم خطورة كل حارس واجهة.**
- **الإصلاح (المرحلة الثانية):** إمّا Cloud Function وسيطة تتحقق من الصلاحية، أو تخزين دور/صلاحيات المستخدم في custom claims وفحصها في القواعد.

### H-2 · حقول الفاتورة `status`/`isCreditSettled`/`paymentMethod` غير مثبّتة (تحقّقت يدوياً)
- `firestore.rules:84-87` يثبّت `total/subtotal/items/invoiceNumber/cashierId` (وكلها حقول حقيقية ✅)، لكن `status` (`AppContext.jsx:1757`) غير مثبّت.
- **الاستغلال:** كاشير ممنوع من الاسترجاع يكتب `status:'refunded'`، أو `isCreditSettled:true` على فاتورة آجلة فيُسقط دَيناً **بلا سند قبض ولا أثر نقدي**.
- **الإصلاح:** أضِف `same('status')` وامنع تعديل حالة السداد إلا للمدير أو مع سند مقابل. (ملاحظة: `same('createdAt')` بلا أثر لأن الفاتورة تستخدم `date`.)

### H-3 · أرصدة العملاء والموردين غير محمية في القواعد
- `firestore.rules:94-103` (`create, update: if isStaff()` بلا تثبيت). كاشير يصفّر رصيد دَينه كعميل أو يعدّل أرصدة الموردين.
- **الإصلاح:** حصر تعديل `balance` في المدير أو في مجموعة حركات (ledger) منفصلة بدل حقل قابل للكتابة الحرّة.

### H-4 · إنشاء الفاتورة/القيود المالية بلا تحقق
- `firestore.rules:83` (`create: if isStaff()`) بلا تحقق من `total`/`items`/`cashierId`. ونفسه لـ `pos_expenses`/`pos_drawer_tx`/`pos_receipts` عند الإنشاء.
- **الإصلاح:** `allow create: if isStaff() && request.resource.data.total is number && request.resource.data.total >= 0;`

### H-5 · حرّاس التنفيذ في العميل فقط (مخالفة النموذج #1)
- الحرّاس في معالِج المكوّن لا في دالة السياق: `refundInvoice` (`AppContext.jsx:1974`), `addProduct/updateProduct/deleteProduct` (`:2241/2259/2323`), `addCustomerPayment` (`:2486`), `addSupplierPayment` (`:3120`), `addPurchaseInvoice/deletePurchaseInvoice` (`:2923/3049`).
- قارن بـ `addExpense`/`deleteExpense`/`deleteShiftRecord` — تحرس **داخلياً** بشكل صحيح.
- **الإصلاح:** انقل `checkUserPermission(...)` إلى أول كل دالة تنفيذ.

### H-6 · خطأ برمجي: تسجيل المشتريات النقدية قد يُسقط العملية
- `AppContext.jsx:3020`: `const cur = prev[currentUserId] || myExpenseShift;` — `myExpenseShift` معرَّف داخل `addExpense` لا داخل `addPurchaseInvoice` → **ReferenceError** لمستخدم له `activeShift` بلا مدخل في `userShifts[currentUserId]`.
- **الإصلاح:** `prev[currentUserId] || activeShift`.

### H-7 · ZATCA: طول TLV أحادي البايت بلا حدّ
- `helpers.js:86`: اسم متجر عربي ≥ ١٢٨ حرفاً (≥ ٢٥٦ بايت) يلتفّ الطول → **QR فاسد صامتاً**. (الاسم الحالي قصير، فالأثر مؤجَّل لكنه فخّ.)
- **الإصلاح:** قصّ `sellerName` إلى حدّ آمن قبل `getTLV`.

### H-8 · ZATCA: رقم ضريبي احتياطي تجريبي
- `helpers.js:93`: `... || '300000000000003'` رقم اختبار معروف → QR يبدو صحيحاً وغير مطابق قانونياً عند غياب الرقم الحقيقي. **الإصلاح:** الفشل الصريح/منع الطباعة الضريبية بدل حقن رقم تجريبي.

### H-9 · طباعة: عشرات الحقول غير مهرَّبة
- سندات الخزينة/البنك/تغذية الدرج/تسليم الوردية: `recipient`/`authorizedBy`/`reason`/`bankName`/`slipNumber`/`managerName`/`sourceLabel` (أرقام الأسطر في `printHelper.js`: ٩٦٦، ٩٧١، ٩٨٣، ١١٨٦، ١١٩٠، ١١٩٤، ١٢٧٨-١٢٩٨، ١٠٨٠-١١٠٢)، وأسماء وسائل الدفع (`:151, :303, :652`)، وإعدادات المتجر/التسميات القابلة للتعديل.
- **الإصلاح:** لفّ كل حقل نصّي مصدره بيانات مستخدم بـ `esc()`، أو ابنِ الـ DOM بـ `textContent`.

### H-10 · حقن صيَغ/CSV عند تصدير Excel
- `excelHelper.js:10-22`: `p.name`/`p.notes`/التصنيف تُكتب بلا تحييد البادئات `= + - @` → صنف اسمه `=HYPERLINK(...)` يصبح صيغة تنفيذية عند الفتح.
- **الإصلاح:** بادئة `'` أمام أي قيمة تبدأ بـ `= + - @` أو تصدير الخلايا كـ `t:'s'`.

### H-11 · كتابات المزامنة غير مُعمَّرة + الإقلاع يستبدل المحلي بالسحابي
- `syncEngine.js:115` (`pendingSaves` في الذاكرة فقط)، `AppContext.jsx:741` (`filteredPrev` يُحسب ثم **يُهمَل** عند الإقلاع). انهيار/إعادة تحميل داخل نافذة الـ400ms أو بوابة الثانيتين يفقد التعديل.
- **الإصلاح:** عمِّر `pendingSaves` في `localStorage` وأعِد فلشها قبل السحب، وادمج `filteredPrev` مع السحابة بدل استبداله.

---

## ٥. الأخطاء المتوسطة 🟡 MEDIUM

**التقارير والحسابات:**
- **M-1 · مبالغة ربح المنتجات (يتضمّن الضريبة):** `ReportsScreen.jsx:116-139`, `Dashboard.jsx:55` — صنف ١١٥ (شامل ١٥) تكلفته ٥٠ يُظهر ربح ٦٥ بدل ٥٠. الإصلاح: `netRev = itemNetRevenue / (1 + taxRate/100)`.
- **M-2 · ربح/إيراد المنتجات يتجاهل الخصم العام:** `ReportsScreen.jsx:125,137` — توزيع الخصم العام تناسبياً على الأصناف.
- **M-3 · المرتجع يعدّل تقارير فترات سابقة بأثر رجعي:** `ReportsScreen.jsx:43`, `Dashboard.jsx:28` — استرجاع فاتورة مارس في أبريل يخفض مبيعات مارس. الإصلاح: قيد مرتجع سالب في فترة الاسترجاع.
- **M-4 · تكرار خصم `transferSales` و`bankSales` عند عكس المرتجع:** `AppContext.jsx:2166-2167` — اعتمد حقلاً واحداً.
- **M-5 · نجاح كاذب قبل تأكيد الكتابة السحابية:** `AppContext.jsx:1890` (بلا `await`) — تظهر «فاتورة جديدة 🧾» وتُمسح السلة حتى لو فشلت الكتابة. الإصلاح: انتظر النتيجة وأظهر فشلاً صريحاً.
- **M-6 · عهدة مُستلَمة أقل من المُعلَنة بلا قيد فرق:** `AppContext.jsx:4311, 4355` — سجّل قيد عجز/فائض صريحاً.

**المزامنة:**
- **M-7 · تصادم معرّف الفاتورة `inv-${Date.now()}`:** `AppContext.jsx:1713` — أضِف لاحقة عشوائية.
- **M-8 · بوابة التهيئة (٢ ثانية) تُسقِط أول تعديل debounced:** `AppContext.jsx:1104` — استبدلها بعلامة «اكتمل أول pull».
- **M-9 · التصفير لا يطبَّق على سجلات بلا ختم زمني:** `syncEngine.js:337-343` — اضمن ختماً في كل سجل.

**الأمان:**
- **M-10 · presets متضاربة:** `permissions.js` مقابل `initialData.js` (مثال: `pos_discount`, `invoices_refund` مختلفان للكاشير) — وحّد المصدر.
- **M-11 · preset المحاسب واسع جداً:** `permissions.js:275-286` يمنح صلاحيات حذف مالية.
- **M-12 · حدّ محاولات PIN في العميل فقط:** `security.js:281` (`localStorage` بمفتاح `global`) — مسح التخزين يصفّره. الإصلاح: عدّاد على الخادم.
- **M-13 · PIN افتراضي ضعيف:** `addUser` يجعله `1234` بلا `validatePinStrength` (`AppContext.jsx:5223`)، وبيانات البذرة تحمل `9999/1234/...`.
- **M-14 · PIN صريح في بيانات البذرة يُزامَن إلى `pos_users`:** `initialData.js` تحمل `pin` نصياً → يُرفع صريحاً عند أول تشغيل. الإصلاح: ازرع `pinHash` أو هجّرها hash-on-load.
- **M-15 · التكلفة `cost` مقروءة لأي موظف:** Firestore لا يحجب الحقول عند القراءة (`firestore.rules:61`) — الحل: مجموعة عرض `pos_products_public` بلا التكلفة.
- **M-16 · هوية التدقيق مشتركة:** البريد مشترك و`userId` من العميل قابل للتزوير (`audit.js:134-135`) — الحل: حساب Firebase مستقل لكل موظف.

---

## ٦. الأخطاء المنخفضة ⚪ LOW (مختصرة)
- تعديل الوردية المقفلة ممكن (لكنه مُحوكَم بسبب إلزامي + audit) — `AppContext.jsx:4193`.
- `pos_login_logs` يسمح `update` غير مقيّد — `firestore.rules:209`.
- اختبارات القواعد لا تغطّي ٨ مجموعات (categories/customers/suppliers/purchases/drawer_tx/receipts/held_bills/user_shifts).
- تعارض `projectId` في الاختبار (`flower-house-test` مقابل `flower-house-8888d`) — أثر إنتاجي صفر.
- `.gitignore` ينقصه `*.pem`.
- تصادم `toDocId` (`a.b` و`a_b`) — `syncEngine.js:77`.
- `active_shift` يُكتب في كل بيعة ولا يُستهلَك — `syncEngine.js:37`.
- تعارض توثيقي: التعليق يقول ٦٠٠٠٠ تكرار و`ITERATIONS=40000` (`security.js`).
- حلقات كتابة غير مُجمَّعة (`reconcileOnce`, `adjustStock`) — يُفضَّل `writeBatch`.
- حقن سمة `src` للشعار/QR — `printHelper.js:232, 315`.

---

## ٧. ما تم التحقق منه وهو سليم ✅
- **قراءة الكمية** `qty ?? quantity ?? 1` مطبَّقة باتساق (لا يوجد خطأ الكمية=١).
- **`costAtSale`** يُثبَّت لحظة البيع ويُقدَّم في كل التقارير.
- **حماية الضغط المزدوج** على البيع (نافذة ٢ث) و**الاسترجاع المزدوج** (`refundingIdsRef` + فحص `status`).
- **`expected` في تقرير Z** يُعاد من الفواتير الفعلية بلا ازدواج.
- **القيد المزدوج** للترحيل والإيداع والتغذية متوازن.
- **`verifyPin`** يرفض الفارغ والنص الصريح والصيغة القديمة، ومقارنة ثابتة الزمن.
- **حارس التصفير** يتطلب رمز مدير صحيح (PBKDF2) — غير قابل للتجاوز.
- **حمايات استيراد Excel** (prototype pollution، السالب، الأرقام الضخمة، الاسم الفارغ) مطبَّقة وسليمة.
- **«لا حذف ضمني»** في المزامنة، وحماية اللقطة الفارغة/الصدى/الأولى، وحدود الدفعات ٤٥٠.
- **اكتمال القواعد:** كل مجموعة يكتبها التطبيق لها قاعدة + catch-all رفض؛ لا مجموعة تقع في الرفض الافتراضي.
- **الباب الخلفي `9999`** محذوف ومرفوض في التشغيل الحقيقي.

---

## ٨. التوصيات وخطة الأولويات

### فوري (هذا الأسبوع)
1. **C-1:** صحّح أسماء الحقول في قاعدة المنتجات → `sellingPrice`/`costPrice`، ثم أعِد النشر.
2. **C-2:** طبّق إصلاح اجتياز المسار والربط على `127.0.0.1` في **النسخة المُوزَّعة** من الخادم المحلي.
3. **C-3 + H-9:** لفّ كل حقول الطباعة المستخدَمة بـ `esc()` (ابدأ باسم المنتج في الملصق).
4. **H-2 + H-3 + H-4:** ثبّت `status`/حالة السداد في قاعدة الفواتير، واحمِ أرصدة العملاء/الموردين، وأضِف تحقق الإنشاء.
5. **H-6:** أصلح `myExpenseShift` → `activeShift` (خطأ برمجي قد يُسقط عملية).

### قصير المدى (هذا الشهر)
6. **C-4 + H-11:** أصلح فقد المخزون/الرصيد المتزامن (استبعاد `stock`/`balance` من الكتابة الكاملة + رفع `updatedAt` دائماً + تعمير `pendingSaves`).
7. **H-5:** انقل حرّاس `checkUserPermission` إلى داخل دوال التنفيذ.
8. **M-1..M-4:** أصلح دقّة ربح المنتجات (الضريبة + الخصم) ومعالجة المرتجع في التقارير.
9. **M-13 + M-14:** ألغِ الـ PIN الصريح من البذرة وطبّق `validatePinStrength` في `addUser`.
10. **H-7 + H-8:** عالج طول TLV والرقم الضريبي الاحتياطي، و**H-10** حقن Excel.

### متوسط المدى (المرحلة الثانية — تتطلب خطة Blaze)
11. **H-1 + M-15:** فرض الصلاحيات الدقيقة خادمياً (custom claims أو Cloud Function)، ومجموعة `pos_products_public` بلا التكلفة.
12. **تحقق سعر الفاتورة على الخادم** عند الإنشاء مقابل `pos_products` (الفجوة الأكبر المذكورة في وثيقة الحالة).
13. **M-16:** حساب Firebase مستقل لكل موظف (يجعل التدقيق موثوقاً ويبسّط الصلاحيات).

### صحّة الاختبارات
14. أضِف اختبارات قواعد للمجموعات الثماني غير المغطّاة، ووحّد `projectId`، وأضِف `*.pem` إلى `.gitignore`.

---

*انتهى التقرير. جميع أرقام الأسطر مأخوذة من حالة الكود بتاريخ ١٣ سبتمبر ٢٠٢٦، والنتائج الحرجة مُتحقَّق منها يدوياً.*
