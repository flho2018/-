# تقرير الفحص الشامل الرابع — بيت الورد
**التاريخ:** 2026-09-16 · **النطاق:** الحسابات · المخزون · المزامنة · الورديات · المرتجع · الآجل · الخزينة · الصلاحيات

---

## ⚠️ اقرأ هذا أولاً — منهج الفحص وحدوده

هذا التقرير يفرّق بصرامة بين ثلاثة مستويات، لأن الخلط بينها هو سبب ثقتك المهدورة في التقارير السابقة:

| الرمز | المعنى |
|---|---|
| 🔬 **مُثبَت تنفيذياً** | شُغِّل الكود الحقيقي وأُخرجت منه أرقام. دليل لا رأي. |
| 📖 **مُتتبَّع في الكود** | قُرئ المسار سطراً سطراً ووُثّق بـ `ملف:سطر`. لم يُشغَّل. |
| ❌ **لم يُتحقّق** | لم يُفحص تشغيلياً — ومكتوب صراحةً لماذا. |

**ما لم يُفحص في هذه الجولة:** لم يُفتح البرنامج داخل المتصفّح (الدخول يحتاج بريداً وكلمة مرور ورمز PIN لا أملكها ولا يجوز لي إدخالها). فكل ما يلي مصدره **تشغيل الكود خارج المتصفّح** و**قراءة المصدر**، لا النقر في الواجهة.

**ما سبق أن تحقّق حيّاً** (من جلسات سابقة، موثّق في `CLAUDE.md`): البيع النقدي، المرتجع وأثره على المخزون، التقسيم، الآجل، ترقيم الفواتير. هذه فحوص حقيقية وقعت فعلاً — لكنها كلها **على جهاز واحد وكاشير واحد**، وهناك بالضبط تكمن الأعطال التي وجدناها اليوم.

---

## 🔴 العطل المسؤول عن «١٠٠ صارت ٥٠» — مُثبَت

### ما وجدناه

الرصيد الافتتاحي المرحَّل يأتي من متغيّر اسمه `lastUserClosedShift` = «آخر وردية مغلقة لهذا الكاشير». وهو مكتوب **ثلاث مرات حرفياً** في ثلاثة ملفات:

- `src/components/pos/PosRegister.jsx:141`
- `src/components/cashier/ShiftHeaderModal.jsx:134`
- `src/components/cashier/tabs/CurrentShiftDrawerTab.jsx:47`

والثلاثة تستعمل:

```js
return (shiftsHistory || []).find(s => s.status === 'closed' && (... مطابقة الكاشير ...));
```

`‎.find()` تُرجع **أول عنصر مطابق في المصفوفة** — **بلا أي ترتيب زمني**. فهي لا تُرجع «آخر» وردية مغلقة، بل **أول ما يصادفه في ترتيب المصفوفة**.

### لماذا ينكسر الترتيب فعلاً

محلياً `closeShift` يُدرج الوردية الجديدة في المقدمة (`AppContext.jsx:4452` — `[closed, ...prev]`)، فالأحدث أولاً وتعمل الدالة بالصدفة. لكن:

1. المصفوفة تُعاد بناؤها من Firestore مرتَّبةً بحقل `_idx` (`syncEngine.js:463`).
2. `_idx` هو **موضع السجل في مصفوفة الجهاز الكاتب لحظة الكتابة** (`syncEngine.js:700`).
3. ومقارنة «هل تغيّر السجل؟» تُجرى بعد `stripMeta` التي **تحذف `_idx`** (`syncEngine.js:108`) — فالوردية القديمة تبقى في السحابة بـ `_idx: 0` ولا يُعاد ترقيمها عند إزاحتها.
4. والوردية الجديدة تُكتب أيضاً `_idx: 0`.

⇒ **مفتاحا فرز متساويان**، فيؤول الترتيب إلى ترتيب معرّفات المستندات (`shift-<الطابع الزمني>-…` تصاعدياً) ⇒ **الأقدم أولاً**.

⇒ بعد أول مزامنة أو إعادة تحميل، «آخر وردية مغلقة» تصير **أقدم وردية**، ويُرحَّل رصيدها.

### الدليل التنفيذي 🔬

`tests/shift_rollover.test.mjs` (جديد) يستخرج المنطق **حرفياً** من الملفات الثلاثة وينفّذه على حالتك: فاطمة أغلقت ورديتين، الأولى بـ ٥٠ والثانية بـ ١٠٠.

```
— PosRegister.jsx —
  ✅ الأحدث أولاً ← يرجع ١٠٠
  ❌ الأقدم أولاً ← رجع 50    ← عين المشكلة التي رأيتَها
  ❌ لا يلتقط وردية شخص آخر يحمل نفس الاسم — رجع 777

— ShiftHeaderModal.jsx —        (نفس النتيجة)
— CurrentShiftDrawerTab.jsx —   (نفس النتيجة)

— الصيغة المقترحة (ترتيب بـ closedAt) —
  ✅ ٦/٦ في كل الترتيبات
```

**الرقم ٥٠ خرج من الكود نفسه، لا من تخميني.**

### عطل ثانٍ في نفس الدالة

شرط المطابقة `s.cashierName === currentName` يعمل **حتى لو اختلف `userId`**. فموظفتان تحملان نفس الاسم المعروض ⇒ يُرحَّل رصيد وردية **شخص آخر** (الاختبار أرجع `777`).

### مسارات أخرى تُنتج نفس العرَض (📖 مُتتبَّعة)

| # | المسار | الدليل | كيف تميّزه في بياناتك |
|---|---|---|---|
| أ | زر «فتح وردية» في تبويب الدرج يستدعي **`openNewShift(0)` بلا نافذة إدخال** حين توجد وردية مفتوحة، ثم تختار «إغلاقها وفتح جديدة» ⇒ الجديدة تبدأ بصفر | `CurrentShiftDrawerTab.jsx:620-628` | الوردية الثانية `startCash = 0` |
| ب | **عهدة معلّقة تدهس الرقم المكتوب صامتةً**: `effectiveStartCash = myFloats.length > 0 ? myFloatTotal : startCash` | `AppContext.jsx:4612-4614` | وجود `drawer_tx` بـ `subType:'drawer_funding'` و`countedInStartCash:true` |
| ج | **`.find()` بلا ترتيب** (أعلاه) | ثلاثة ملفات | 🔬 مُثبَت |
| د | الإغلاق الآلي يُغلق بـ `actualCash ?? startCash ?? 0` — ووردية مفتوحة **لا تملك `actualCash`** فتُغلق برصيدها الافتتاحي لا بالنقد المعدود | `AppContext.jsx:4599` | ملاحظة `إغلاق آلي لبدء وردية جديدة` |

⚠️ **وحقيقة جذرية يجب أن تعرفها:** **لا يوجد ترحيل تلقائي للرصيد إطلاقاً في النظام.** `closeShift` لا يكتب أي شيء يقرأه `openNewShift`. ما تراه «ترحيلاً» هو **مجرد تعبئة مسبقة لحقل نصّي في الواجهة** — وأي شيء يغيّر الحقل أو يتجاوزه يُضيّع الرقم بلا أثر.

### ⚠️ وازدواج محاسبي لم يُوفَّق بينه

الوردية المغلقة تُختم `handoverStatus: 'pending'` بمبلغ `handoverAmount` بانتظار **استلام الإدارة** (`AppContext.jsx:4432`)، وتُحتسب في `pendingHandoversTotal` (`:5364`). فإن رُحّل **نفس المبلغ** رصيداً افتتاحياً للوردية التالية، صار المبلغ محسوباً **مرتين** في خزينة المتجر: مرة كعهدة معلّقة على الوردية القديمة، ومرة كرصيد افتتاحي في الجديدة. **الترحيل والتسليم تصميمان متناقضان.** هذا يفسّر رقم `585.00` المتضخّم في شاشة الخزينة.

---

## 🔴 عطل حرج ثانٍ — بيع جهازين لنفس المستخدم يُضيّع نقداً

📖 `user_shifts` **ليست في** `INCREMENT_OWNED` (`syncEngine.js:69`)، فعدّاداتها تُكتب **قيماً مطلقة** بـ `batch.set` **بلا merge** (`syncEngine.js:799`)، والكاتب يقرأ-يعدّل-يكتب (`AppContext.jsx:2136-2148`).

**السيناريو بالأرقام:**
1. جهازان، نفس الكاشير، كلاهما يرى `cashSales = 0`.
2. جهاز أ يبيع ٥٠ نقداً ⇒ `cashSales = 50`, `updatedAt = T1`.
3. جهاز ب (لقطته لم تصل) يبيع ٥٠ ⇒ يحسب `0 + 50 = 50`, `updatedAt = T2 > T1` ⇒ **يستبدل المستند**.
4. حارس `updatedAt` يسمح لأن T2 أحدث.

**النتيجة: في الدرج ١٠٠ ريالاً والوردية تقول ٥٠.** والفاتورتان موجودتان (لكل فاتورة مستند مستقل) فالتقارير تناقض الوردية.

نفس المسار يصيب `cashIn` (`:3202`) و`cashOut` (`:3519, 3603, 3691`) و`cardSales` (`:3269`).

> ⚠️ **هذا احتمال قائم لحالتك تحديداً** — الفواتير في لقطتك تحمل رمزَي جهازين مختلفين (`2RQ2` و`Y5UK`).

---

## 🔴 عطل حرج ثالث — بيانات محذوفة تعود من القبر

📖 `AppContext.jsx:1246`:
```js
setter(prev => rows.length > prev.length ? rows : prev)   // «الأطول هو الأصحّ»
```
حارس ختم التصفير يحمي **التصفير** فقط، لا **الحذف الفردي**:

1. المدير يحذف ١٠ فواتير من جهاز أ.
2. جهاز ب يقلع: `localStorage` فيه ١٦٠، والسحابة ١٥٠.
3. قراءة IndexedDB غير المتزامنة تنتهي بعدها: `160 > 150` ⇒ **تعود العشر**.
4. المؤثّر التالي يرفعها للسحابة ⇒ **تعود لكل الأجهزة**.

هذا مصدر «بيانات تنجو من الحذف» ومصدر تضخّم التقارير.

📖 **ومسار رابع:** التصفير يكتب ختمه في localStorage **جهاز المصفِّر وحده** (`syncEngine.js:779`). جهاز غائب وقت التصفير لا يصله الختم إطلاقاً ⇒ يبقى يعرض المحذوف ويُحييه كل إقلاع.

---

## 📦 المخزون

### ✅ ما وجدناه سليماً (📖 بدليل)
- البيع والمرتجع والتسوية اليدوية والتالف: كلها **بالفرق** (`increment`) لا بالقيمة المطلقة — تحترم القاعدة §5.1 وآمنة للأجهزة المتزامنة.
- `stockBaseline` يعمل كما وُصف: الفرق يُقاس على ما رآه المستخدم (`AppContext.jsx:2644-2648`).
- `isService` مستثنى صحيحاً من الخصم والإعادة.
- تصفير الجرد يستعمل فروقاً سالبة لا `stock: 0` مطلقاً.
- حارس تجاوز المخزون موجود في موضعين (تحذيري لا مانع — قرار مقصود موثّق).

### 🔴 أعطال
| # | العطل | الملف:السطر | الأثر |
|---|---|---|---|
| م-١ | **الكتابة الكاملة ترسل `stock` مطلقاً** إن لم تصل لقطة المنتجات بعد (`isUpdate = false` ⇒ لا تجريد ⇒ `set` بلا merge) | `syncEngine.js:727, 742, 799` | جهاز يقلع على شبكة بطيئة ويضيف صنفاً ⇒ **يمحو بيع جهاز آخر** |
| م-٢ | **استلام المشتريات ينسى `keepWritten`** — الاستدعاء الوحيد في المشروع الذي يخالف | `AppContext.jsx:3455-3460` | نافذة يُكتب فيها المخزون مطلقاً |
| م-٣ | **ازدواج المرتجع بين جهازين يضاعف إعادة المخزون** — الحارسان محليان فقط | `AppContext.jsx:2282, 2291` | فاتورة ×٣ تُرجع مرتين ⇒ **+٦** والنقد يخرج مرتين |
| م-٤ | **استيراد إكسل يكتب الكميات مطلقةً** — والسلوك غير محدَّد: إما تُهمَل الكمية صامتة أو تدهس بيعاً | `AppContext.jsx:2725-2733` | |
| م-٥ | **القصّ المحلي `Math.max(0,…)` مقابل `increment` غير مقصوص** | `AppContext.jsx:2071` | الشاشة ٠ والسحابة **−٢** ⇒ جرد لاحق ينتج عجزاً صامتاً |
| م-٦ | **`updateProduct` لا يكتب `updatedAt`** | `AppContext.jsx:2651-2661` | الكمية تتغيّر والسعر/الاسم لا — صامتاً |
| م-٧ | **لا دعم للمرتجع الجزئي إطلاقاً** — `inv.items` تُعاد كاملة دائماً | `AppContext.jsx:2339` | |

---

## 💰 الحسابات المالية

### ✅ سليم (🔬 مُشغَّل فعلياً)
- الضريبة: الشاملة `100 → 86.96 + 13.04`، وغير الشاملة `99.99 → 114.99`، والمعطّلة `0`.
- الخصم النسبي يُحسب **بعد** خصومات الأصناف (١٠٠ − ١٠ ثم ١٠٪ ⇒ خصم ١٩).
- `safeNum` يقصر السعر التالف على سطره ولا يُنتج `NaN` في الإجمالي.
- رصيد العميل يُكتب **بالفرق** في البيع والسداد والمرتجع.
- FIFO لتسوية الديون: الترتيب صحيح والتراكم صحيح.
- حارس التقسيم: مجموع الدفعات = الإجمالي ضمن هللة.
- ZATCA TLV: الوسوم الخمسة بالترتيب الصحيح.
- قيود الخزينة: `Σ amount` متّسق (الإيداع سالب والتسليم موجب).

### 🔴 أعطال مؤكّدة
| # | العطل | الملف:السطر | مثال رقمي |
|---|---|---|---|
| ح-١ | **مرتجع نقدي بين كاشيرين يُخرج نقداً من درج لا يُخصم منه** — الحركة باسم البائع والحساب باسم المنفّذ | `AppContext.jsx:2506` مقابل `4336` | نايف باع ١٠٠، سعد استرجع ⇒ درج نايف **+١٠٠ وهمية**، درج سعد **−١٠٠ وهمية** |
| ح-٢ | **سند قبض بالشبكة يُسجَّل كمبيعات** — تحصيل دين ليس بيعاً | `AppContext.jsx:3261` | فاتورة آجلة ١٠٠ + تحصيلها ⇒ المبيعات **٢٠٠** لبيعة واحدة |
| ح-٣ | **الزيادة في السداد تُبتلع** — `Math.max(0, …)` | `AppContext.jsx:3002` | دين ١٠٠ ودفع ١٥٠ ⇒ الدرج +١٥٠ والرصيد −١٠٠، و**٥٠ تختفي** |
| ح-٤ | **رصيد العميل: المحلي يُقصّ والسحابي لا** | `:2368` مقابل `:2375` | استرجاع فاتورة آجلة مسدَّدة ⇒ محلياً ٠ وسحابياً **−١٠٠** |
| ح-٥ | **خصم عام سالب يرفع الإجمالي** | `AppContext.jsx:1737` | سلة ١٠٠ + خصم `−50` ⇒ **الإجمالي ١٥٠** |
| ح-٦ | **خصم صنف بلا سقف** | `AppContext.jsx:1727` | سطر ١٠٠ بخصم ٥٠٠ ⇒ الإجمالي ٠ لكن الفاتورة تحفظ `discount: 500` |
| ح-٧ | **ضريبة ١٥٪ تُفرض عند إعداد تالف** | `AppContext.jsx:1749` | `taxRate: 'abc'` ⇒ **١٠٠ تصير ١١٥** |
| ح-٨ | **ربح الموظفين يتجاهل الخصم والضريبة** — والبونص يُحتسب عليه | `staffPerformance.js:175` | نفس البيعة: **٤٠** هنا مقابل **١٠٫٤٣** في التقارير المالية |

### 🔴 خمس صيغ متناقضة لتصنيف وسائل الدفع (🔬 مُشغَّلة على نفس الفاتورة)
| الفاتورة | `calculateInvoicePaymentBreakdown` | `buildPaymentBreakdown` | `classifyInvoicePayments` |
|---|---|---|---|
| نقدي ١٠٠ مسترجع | cash ١٠٠ | **٠** | cash ١٠٠ |
| **تمارا ١٠٠** | tamara ١٠٠ | tamara ١٠٠ | **card ١٠٠** |
| مقسم قديم ٦٠/٤٠ | ٦٠/٤٠ | **`split: 100`** (وسيلة وهمية) | ٦٠/٤٠ |
| مقسم بمصفوفة فارغة | ٦٠/٤٠ | **٠ — يضيع المبلغ كله** | ٦٠/٤٠ |

### 🟡 الأرقام العشرية تصل المال فعلاً
🔬 ثلاث فواتير ١٠٫١٠ ⇒ `cashSales = 30.299999999999997`. والفحص `difference !== 0` (`helpers.js:335`) يجعل درجاً **مطابقاً تماماً** يطبع «⚠️ الفارق: +0.00 (زيادة)» — ويغذّي تقييم انضباط الموظف.

---

## 🔐 الصلاحيات والأمان

> ⚠️ **الحقيقة التي تُبطل كل ما بعدها:** `isAdmin()` في `firestore.rules:30` = بريدك وحده، وكل الأجهزة تدخل به. فكل قواعد الأدوار **بلا أثر اليوم**، وكل جهاز في المتجر **مديرٌ كامل في السحابة**.

| # | الثغرة | الملف:السطر | الخطورة |
|---|---|---|---|
| ص-١ | **`switchUser` يتخطّى الرمز السري كلياً** — تعيّن أي مستخدم بلا PIN وبلا فحص صلاحية، وزرّها معروض في الواجهة | `AppContext.jsx:6166-6183` | 🔴 |
| ص-٢ | **تصعيد إلى مدير من `localStorage`** — تغيير `"role"` وإعادة التحميل يفتح كل الشاشات | `permissions.js:336` | 🔴 |
| ص-٣ | **كسر PIN بلا اتصال** — `pinHash` مقروء لكل موظف، والفضاء **١٠٬٠٠٠ احتمال** فقط، والحدّ من المحاولات في `localStorage` يُمحى بسطر | `firestore.rules:228`, `security.js:281` | 🔴 |
| ص-٤ | **بطاقة NFC بلا أي حدّ محاولات** (بخلاف مسار PIN) | `PinLockModal.jsx:50-64` | 🔴 |
| ص-٥ | **إدارة المستخدمين بلا فحص صلاحية في المنطق** — الحماية إخفاء تبويب فقط | `AppContext.jsx:5977, 6006, 6085` | 🟠 |
| ص-٦ | **وردية زميلك قابلة للكتابة** — لا شرط ملكية | `firestore.rules:198-202` | 🟠 |
| ص-٧ | **المرتجع بلا أي قاعدة سحابية** — `status` و`paymentMethod` حرّان | `firestore.rules:114-117` | 🔴 |
| ص-٨ | **الخروج لا ينفّذ `signOut`** ولا يفرّغ السلة — جلسة Firebase تبقى حيّة بصلاحية مدير | `AppContext.jsx:6512-6521` | 🟠 |
| ص-٩ | **سجل التدقيق لا يغطّي أخطر ما يُفعل**: إضافة/تعديل/حذف مستخدم، تغيير الصلاحيات، استرجاع نسخة، تعديل الإعدادات، الخصم وتعديل السعر | `audit.js:42-45` (ثوابت غير مستعملة) | 🟠 |

### ✅ سليم
- سجل التدقيق **السحابي** غير قابل للتزوير ولا للمسح (`firestore.rules:253-255` — يفرض البريد والوقت من الخادم).
- لا أسرار في الحزمة المنشورة: `grep 'pin:"[0-9]{4}"'` ← **لا نتيجة**. و`.env` غير متعقَّب في git.
- تجزئة PIN سليمة بنيوياً: ملح عشوائي مستقل، مقارنة ثابتة الزمن، رفض أي صيغة نصّية.
- الفاتورة لا تُعدَّل مالياً سحابياً، والوردية المغلقة مقفلة، ودفتر الخزينة لا يُعدَّل.
- المفتاح الغائب يرجع لقالب الدور، والشاشة غير المسجّلة **تُمنع** — الإصلاحان الموثّقان موجودان فعلاً.

---

## 🔄 المزامنة والحفظ

### ✅ سليم
- **العمل بلا إنترنت لا يضيع**: `persistentLocalCache` مفعّلة — الكتابة تُصفّ على القرص وتُرسَل عند العودة.
- **الحذف الضمني ممنوع فعلاً** (§5.3): لا عمليات حذف إلا داخل `if (isReset)`.
- **`increment` صحيح** للمخزون والأرصدة، والتجريد عند التحديث يعمل.
- **مؤقّت تأجيل لكل مفتاح + سقف ثانيتين** — لا تجويع تحت الضغط.
- **كبت الصدى دقيق**، واللقطة الأولى تُسلَّم دائماً.
- **تجزئة النسخ الاحتياطية سليمة**: القطع قبل المستند الوصفي، ورفض الناقص صراحةً.

### 🔴 أعطال
| # | العطل | الدليل |
|---|---|---|
| ز-١ | **تسع مفاتيح تُكتب في IndexedDB ولا تُقرأ منها أبداً** (`products`, `users`, `user_shifts`, `store_info` …) ⇒ عند امتلاء localStorage يقلع الجهاز على نسخة بائتة والنسخة السليمة موجودة ولا تُقرأ | `AppContext.jsx:1265-1271` مقابل `:59` |
| ز-٢ | **`active_shift` مستند سحابي واحد عالمي** لمفهوم شخصي — كل كاشير يكتب في نفس المستند، ولا قارئ له إطلاقاً (كتابة ميتة مُضلِّلة) | `syncEngine.js:37`, `AppContext.jsx:2134` |
| ز-٣ | **`cleanUserShifts` يُعيد ترقيم المفاتيح** ⇒ مستندان لنفس الشخص، ومجموع «نقد الكاشيرين» يعدّه مرتين. ويُسقط ورديات المستخدمين المعطّلين **صامتاً** | `AppContext.jsx:202-219` |
| ز-٤ | **استرجاع نسخة أقدم من آخر تصفير ينجح على الشاشة ولا يصل السحابة** — وأول لقطة تمسحه | `syncEngine.js:699` |
| ز-٥ | **كتابة `store_info` تفشل صامتة لغير المدير** — القاعدة تسمح بـ `active_shift` فقط في `pos_meta` | `firestore.rules:239-240` |
| ز-٦ | **انحراف الساعة**: جهاز ساعته متأخّرة **لا تُرفع تعديلاته إطلاقاً وتُبتلع بصمت**. و`clockSkew.js` **يُنذر ولا يمنع** | `syncEngine.js:717-737` |

---

## 📊 نسبة صلاحية البرنامج للعمل

النسبة الواحدة مضلِّلة، لأن البرنامج **يعمل بشكل جيّد في وضع ويكسر في وضع آخر**:

| الوضع التشغيلي | النسبة | التفسير |
|---|---|---|
| **جهاز واحد · كاشير واحد · بإنترنت** | **‎~٨٢٪** | دورة البيع والمرتجع والآجل والمخزون تعمل فعلاً (مُتحقَّق حيّاً سابقاً). الباقي: أعطال الخصم السالب والضريبة التالفة والصيغ المتناقضة في التقارير |
| **عدة أجهزة · عدة كاشيرين** (وضعك الفعلي) | **‎~٥٥٪** | ثلاثة أعطال تُضيّع نقداً أو تُحيي محذوفاً، وكلها في مسار المال |
| **الأمان متعدّد المستخدمين** | **‎~٤٠٪** | تخطّي الرمز السري، والتصعيد من المتصفّح، وكل الأجهزة على بريد المدير |
| **سلامة البيانات وصمودها** | **‎~٦٠٪** | النسخ الاحتياطية سليمة بنيوياً، لكن الحذف والتصفير لا يصمدان عبر الأجهزة |
| **جاهزية المطبوعات** | **‎~٧٠٪** | TSPL مبنيّ ولم يُختبر على طابعة، وQZ محجوب على HTTPS بلا شهادة |

### 🎯 **التقدير الإجمالي الصادق: ‎٦٢٪**

**بلغة صاحب المتجر:** البرنامج **يبيع ويطبع ويحسب صحيحاً في اليوم العادي على جهاز واحد**. لكنه **ليس جاهزاً لتشغيل عدة كاشيرين على عدة أجهزة بمال حقيقي** — وهذا بالضبط ما تخطّط له.

---

## 🛠️ المقترحات بالأولوية

### أولوية قصوى — قبل أي تشغيل بمال حقيقي
1. **إصلاح `lastUserClosedShift`** — فرز بـ `closedAt` بدل `.find()`، ومطابقة بالمعرّف لا بالاسم. **الصيغة الصحيحة مكتوبة ومختبَرة بالفعل** في `tests/shift_rollover.test.mjs` (٦/٦ ناجح). ثلاثة ملفات ⇒ **يجب توحيدها في دالة واحدة مشتركة** وإلا عاد العطل من الباب الثالث.
2. **ضمّ `user_shifts` إلى `INCREMENT_OWNED`** وتحويل كل كتّاب العدّادات إلى `adjustFields`. ⚠️ تغيير واسع في مسار نقدي — **يحتاج جهازين للتحقّق**، لكن بقاءه يعني ضياع نقد مؤكَّد.
3. **إلغاء «الأطول يفوز»** (`AppContext.jsx:1246`) واستبدالها بدمج بالمعرّف وختم التصفير.
4. **تقييد `switchUser` بالرمز السري** أو حذف الزر.
5. **إنشاء `f1`…`f4` في Firebase** — بدونه كل طبقة الصلاحيات السحابية ديكور.

### أولوية عالية
6. توحيد صيغ تصنيف وسائل الدفع في **دالة واحدة** (خمس صيغ اليوم).
7. حراس القيم في `getCartTotals`: خصم غير سالب، خصم لا يتجاوز السطر، ونسبة ضريبة تالفة ⇒ صفر لا ١٥٪.
8. تصحيح وردية المرتجع النقدي (ح-١) وسند القبض بالشبكة (ح-٢) والزيادة في السداد (ح-٣).
9. حارس ضغط مزدوج على **فتح وإغلاق الوردية** (موجود في الدفع وغائب هنا).
10. `keepWritten` في استلام المشتريات (سطر واحد).

### أولوية متوسطة
11. قراءة IndexedDB للمفاتيح التسعة الباقية.
12. حسم تناقض **الترحيل مقابل التسليم** (ازدواج العهدة المعلّقة).
13. تدوير المبالغ على هللتين عند تحديث العدّادات.
14. توسيع سجل التدقيق للمستخدمين والصلاحيات والاسترجاع والخصم.
15. `signOut` حقيقي عند الخروج.

---

## ✅ خلاصة بثلاثة أسطر

1. **شكّك كان في محلّه**: «١٠٠ صارت ٥٠» عطل حقيقي في الكود، **مُثبَت تنفيذياً**، لا خطأ منك ولا سوء قراءة.
2. **«تكرار فاطمة» ليس عطلاً** — ورديتان حقيقيتان من اختبار الإغلاق/الفتح. لكن **الرقم المرحَّل بينهما** كان خطأً.
3. **الأرقام الأساسية سليمة** (الضريبة والخصم والمخزون والآجل مُشغَّلة ونجحت)، والخلل يتركّز في **التزامن بين الأجهزة** و**الصلاحيات** — وهما بالضبط ما لم يفحصه أحد من قبل لأنه يحتاج أكثر من جهاز.
