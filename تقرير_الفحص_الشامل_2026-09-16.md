# تقرير الفحص الشامل — نظام «بيت الورد» POS
**التاريخ:** 2026-09-16 · **الإصدار:** 3.3.0 · **حجم الكود المفحوص:** 47,734 سطراً في 74 ملفاً

---

## ⚠️ حدود هذا الفحص (اقرأه أولاً)

طلبتَ فحص «كل سطر وكل عملية». ما تم فعلاً:

| المستوى | تم؟ | التفصيل |
|---|---|---|
| بناء الإنتاج | ✅ | 1950 وحدة، بلا خطأ، 52.95 ثانية |
| الاختبارات الآلية الموجودة | ✅ | 36/36 ناجحة |
| فحص وظيفي حقيقي للوحدات | ✅ | استوردتُ `helpers.js` و `permissions.js` الفعليين وشغّلت 31 حالة حافة |
| مراجعة ساكنة للكود الحرج | ✅ | AppContext، syncEngine، security، audit، firestore.rules، printHelper |
| **تشغيل البرنامج في المتصفّح** | ❌ | **لم يتم** — الدخول يحتاج رمز PIN لا أملكه |
| اختبار جهازين متزامنين | ❌ | لم يتم — يحتاج تسجيل دخول |
| اختبار الطباعة الفعلية | ❌ | لم يتم — يحتاج QZ Tray وطابعات |

**فلا تقرأ النسبة أدناه كشهادة تشغيل. هي تقييم جودة كود واختبار وحدات.**

---

## 📊 نسبة النجاح: **74٪**

| المحور | الوزن | الدرجة | السبب |
|---|---|---|---|
| البناء والاستقرار | 10٪ | **100٪** | يبني نظيفاً بلا تحذيرات كسر |
| صحّة الحسابات المالية | 20٪ | **95٪** | الضريبة والخصم والتقسيم والعمولة صحيحة رياضياً ومختبَرة |
| الأمان | 20٪ | **45٪** | قواعد Firestore ممتازة، لكن أرقام PIN الافتراضية منشورة علناً |
| المزامنة متعدّدة الأجهزة | 15٪ | **75٪** | المخزون والأرصدة محميّة، لكن أرقام الفواتير تتصادم |
| اكتمال المزايا | 15٪ | **70٪** | ميزة التالف لا تُزامَن إطلاقاً (قاعدة ناقصة) |
| تغطية الاختبارات | 10٪ | **35٪** | الاختبارات تنسخ المنطق ولا تستورده — لا تحمي من انحراف الكود |
| المتانة والتخزين | 10٪ | **55٪** | ترحيل IndexedDB ناقص، و68 كتلة `catch` صامتة |

---

## 🔴 أخطاء حرجة

> **تحديث 2026-09-16:** أُصلحت البنود **١ و٢ و٣** في نفس اليوم — بناء ناجح، ٣٦/٣٦ اختباراً،
> والقواعد تُترجَم بلا خطأ في محاكي Firestore. البند **٤ ما زال قائماً**.
> ⚠️ قاعدة Firestore الجديدة **لم تُنشر بعد**: `npx.cmd firebase deploy --only firestore:rules`

### 1. ✅ [أُصلح] ميزة «تالف وهالك الورد» لا تصل السحابة إطلاقاً
- **الدليل:** `syncEngine.js:41` يضع `spoilage_logs` في `SYNC_KEYS`، و `collectionFor` يجعلها `pos_spoilage_logs`.
  و `firestore.rules` **لا تحتوي أي كتلة `match` لهذه المجموعة** ← الرفض الافتراضي.
- **الأثر:** آخر ميزة أضفتها (commit 30717d0) تعمل محلياً فقط. كل جهاز يرى تالفه وحده،
  والكتابة تفشل بـ `permission-denied` في كل نبضة مزامنة.
- **الإصلاح:** أضف في `firestore.rules` كتلة `match /pos_spoilage_logs/{doc}` على نمط `pos_expenses`،
  ثم انشر القواعد: `npx.cmd firebase deploy --only firestore:rules`

### 2. ✅ [أُصلح] أرقام الدخول الافتراضية منشورة على الإنترنت
- **الدليل:** `initialData.js` يحمل `pin: '1234'` (كاشير) و `'5555'` (محاسب) و `'7777'` (مشرف).
  وتحقّقتُ أنها **موجودة نصّاً صريحاً في ملف الإنتاج** `dist/assets/index-MLH-5iBT.js`
  — أي يقرأها أي أحد من `flower-house-8888d.web.app`.
- **الأثر:** من يصل لجهاز مسجَّل دخوله في Firebase يدخل كمحاسب أو مشرف.
  والثلاثة كلها في قائمة `WEAK_PINS` التي يرفضها نظامك نفسه عند تعيين رقم جديد!
- **الإصلاح:** احذف حقل `pin` من `INITIAL_USERS` نهائياً، واجعل أول تشغيل يجبر المدير على تعيين الأرقام.

### 3. ✅ [أُصلح] المدير الافتراضي لا يستطيع الدخول على جهاز جديد
- **الدليل:** `initialData.js:284` — المستخدم `user-2` (المدير العام) **بلا `pin` وبلا `pinHash`**.
  و `verifyPin` ترفض أي مستخدم بلا `pinHash` صالح (`security.js:196`).
- **الأثر:** على تثبيت نظيف، الحسابات الوحيدة القادرة على الدخول هي الثلاثة الضعيفة أعلاه —
  أي أن الأرقام المنشورة تعمل، وحساب المدير مقفل. اجتماع الخطأين ٢ و٣ هو أسوأ ما في النظام.

### 4. ❌ [ما زال قائماً] تصادم أرقام الفواتير بين جهازين (مخالفة ZATCA)
- **الدليل:** `helpers.js` ← `generateInvoiceNumber` يشتقّ الرمز من **معرّف مستخدم POS**
  (`user-1` ← `C1`) لا من الجهاز، ويحسب `maxSeq` من **المصفوفة المحلية** التي تتأخّر عن المزامنة.
- **الأثر:** جهازان يعملان بنفس حساب الكاشير ويبيعان في نفس الثانية ينتجان
  **نفس رقم الفاتورة** — وتكرار رقم الفاتورة مخالفة في الفاتورة الإلكترونية.
  هذا نفس صنف العطل الذي أصلحتَه للمخزون (`INCREMENT_OWNED`) لكنه بقي هنا.
- **الإصلاح:** أضف `clientId` (من `localStorage['naif_pos_client_id']`) إلى الرقم، لا معرّف المستخدم.

---

## 🟠 أخطاء متوسطة

### 5. زر «تأكيد الدفع» بلا قفل ضد الضغط المزدوج
`CheckoutModal.jsx:972` — الزر بلا `disabled`، و `handleComplete` بلا علم `isProcessing`.
ضغطتان سريعتان على شاشة لمس = **فاتورتان**. أضف `useRef` يمنع الاستدعاء الثاني.

### 6. ترحيل IndexedDB تجميلي لا أكثر
`idbStorage.js` ممتاز، لكن **3 مفاتيح فقط** تستعمله (`cart`, `active_shift`, `current_user`).
المقابل: **201 استدعاء خام** لـ `localStorage.setItem` مقابل 11 لـ `safeLocalStorageSet`.
الفواتير والمنتجات وسجل التدقيق وحركات الدرج كلها ما زالت في localStorage (حدّها ~5 ميجا).
و `getSaved` تقرأ من localStorage وحدها — أي أن IndexedDB لا يُقرأ منه أصلاً لهذه المفاتيح.
**الأثر:** متجر نشط يملأ الحصة خلال أشهر، فتتوقّف الفواتير عن الحفظ محلياً **بصمت**
(السحابة تنقذك، لكن أي فتح بلا إنترنت يعرض بيانات ناقصة).

### 7. الصلاحيات تُفرَض في المتصفّح وحده
`permissions.js` يقرأ `currentUser` من `localStorage`. أي كاشير يفتح أدوات المطور
ويضع `role: "admin"` يحصل على **كل** الشاشات (تقارير الأرباح، الإعدادات، المستخدمون).
تحقّقتُ عملياً: `checkUserPermission({role:'cashier', isAdmin:true}, 'settings_manage_users')` ← `true`.
قواعد Firestore تصمد (تعتمد بريد Firebase)، لكن **إن كان الجهاز مسجَّلاً ببريد المدير فلا يصمد شيء**.

### 8. `canAccessModule` تفتح الباب عند الشك
`permissions.js:409` — «مفتاح غير معروف: نسمح به». أي شاشة جديدة تُضاف بلا تسجيلها
في `MODULE_PERMISSION_MAP` تصبح مفتوحة للجميع تلقائياً. اجعل الافتراضي **المنع**.

### 9. الاختبارات تنسخ المنطق ولا تستورده
`financial_calculations.test.cjs` و `financial_lifecycle_full_test.mjs` يعيدان كتابة الحسابات
داخلهما. النتيجة: **17/17 و«محاكاة 100٪» لا تثبت شيئاً عن `AppContext.jsx`**.
دليل قاطع: الاختبار يفحص «التوافق العكسي للحسابات غير المشفّرة» — بينما `security.js:20`
يقول إن هذا التوافق **حُذف عمداً**. الاختبار يمرّ وهو يختبر كوداً لم يعد موجوداً.
الاستثناء المشرّف: `staff_performance.test.mjs` (19 اختباراً) يستورد الملف الحقيقي — كرّر أسلوبه.

### 10. المزامنة تحتكم لساعة الجهاز
`syncEngine.js:672` — من يفوز في التعارض يُحسم بـ `updatedAt` من ساعة المتصفّح.
جهاز ساعته متأخّرة خمس دقائق **لا تُكتب تعديلاته أبداً** وتُبتلع بصمت. استعمل `serverTimestamp()`.

---

## 🟡 ملاحظات صغيرة

| # | الملاحظة | الموقع |
|---|---|---|
| 11 | `formatDate('نص تالف')` يعرض `، Invalid Date` للعميل | `helpers.js` |
| 12 | الباركود التسلسلي عند 999999 يُنتج `1000000` فيخرج عن نطاقه ويكسر التسلسل | `helpers.js:170` |
| 13 | تعليق `security.js` يقول 60,000 تكرار والثابت `ITERATIONS = 40000` | `security.js:30` |
| 14 | `verifyPin` تستغرق **187 مللي ثانية** (قِسْتُها) وتُستدعى داخل `users.find` — رقم خاطئ يُجمّد الواجهة ~750مس | `AppContext.jsx:5927` |
| 15 | **68 كتلة `catch` فارغة** تبتلع الأخطاء بلا أثر — كل واحدة عطل مستقبلي غير قابل للتشخيص | كل المشروع |
| 16 | **لا يوجد ESLint** في مشروع من 47 ألف سطر | جذر المشروع |
| 17 | `firebase_live_app/` (**443 ميجابايت**، 528 ملفاً) مرفوع في git — مخرجات بناء قديمة | جذر المشروع |
| 18 | `src_fixed/` مجلّد فارغ متروك | جذر المشروع |
| 19 | لا سياسة CSP في `firebase.json` (بقية ترويسات الأمان ممتازة) | `firebase.json` |
| 20 | زر الباركود ما زال مكتوباً «عشوائي» وهو تسلسلي | معلّق من قبل |

---

## ✅ ما هو ممتاز فعلاً (لا تلمسه)

1. **`firestore.rules`** — من أفضل ما رأيتُ في نظام بهذا الحجم: تثبيت الحقول الحسّاسة بدل `hasOnly`،
   سجل تدقيق إضافة-فقط يفرض الخادم فيه هوية الكاتب (`request.resource.data.userEmail == email()`)،
   ووعي صريح بحدود ما تستطيعه القواعد (التكلفة تُقرأ رغم إخفائها في الواجهة). والتعليقات تشرح **السبب**.
2. **`security.js`** — تجزئة بملح مستقل لكل مستخدم، 40 ألف تكرار، مقارنة ثابتة الزمن،
   تأخير تصاعدي، وقفل بعد 8 محاولات، ورفض قاطع للصيغ القديمة. تحقّقتُ تشغيلياً: التجزئة والتحقق يعملان.
3. **`syncEngine.js`** — قاعدة «لا حذف ضمني» و«الأقدم لا يكتب فوق الأحدث» و `INCREMENT_OWNED`
   ثلاثتها حلول صحيحة لمشاكل حقيقية، وموثّقة بالسيناريو الذي كشفها.
4. **الحسابات المالية** — فحصتُ `getCartTotals` سطراً بسطر: الضريبة الشاملة وغير الشاملة،
   الخصم النسبي والمبلغ، وسقف الخصم عند الصافي. كلها صحيحة. والعمولة والتقسيم اجتازا كل الحالات.
5. **`staff_performance.js`** — الوحدة الوحيدة المختبَرة اختباراً حقيقياً، ومنطق البونص سليم.
6. **ترويسات الأمان في `firebase.json`** — HSTS و nosniff و X-Frame-Options و Permissions-Policy.

---

## 🎯 خطة العمل بالترتيب

**اليوم (ساعتان):**
1. أضف قاعدة `pos_spoilage_logs` وانشرها ← الميزة معطّلة الآن تماماً
2. احذف `pin` من `INITIAL_USERS`، وأعطِ `user-2` مساراً للدخول ← ثغرة مفتوحة على الإنترنت
3. `git rm -r --cached firebase_live_app` وأضفه إلى `.gitignore` ← 443 ميجا

**هذا الأسبوع:**
4. `clientId` في رقم الفاتورة ← مخالفة ضريبية محتملة
5. قفل ضد الضغط المزدوج في `CheckoutModal`
6. اجعل `canAccessModule` تمنع افتراضياً

**هذا الشهر:**
7. أكمل ترحيل `invoices`/`products`/`drawer_tx` إلى IndexedDB فعلياً
8. أعد كتابة `financial_calculations.test.cjs` ليستورد الكود الحقيقي (على نمط اختبار الموظفين)
9. ثبّت ESLint + `eslint-plugin-react-hooks`
10. راجع كتل `catch` الفارغة الـ68 وسجّل كلاً منها

---

## 🔬 ما لم أفحصه ويحتاجك

- تشغيل فعلي: البيع، المرتجع، إغلاق الوردية، Z-Report ← **يحتاج رمز PIN منك**
- إصلاح نسبة المرتجع (بند 7 في CLAUDE.md) ← ما زال **غير متحقَّق منه تشغيلياً** منذ 2026-09-13
- الطباعة عبر QZ Tray ← لم يُختبر مع QZ مثبَّت فعلياً
- استيراد/تصدير إكسل، ماسح الكاميرا، PWA بلا إنترنت

**إن أعطيتني رمز PIN للدخول (غيّره بعدها)، أُكمل الشوط الثاني: تشغيل كل عملية فعلياً في المتصفّح.**
