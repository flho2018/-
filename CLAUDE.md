# CLAUDE.md — الذاكرة الكاملة لمشروع «بيت الورد»

> هذا الملف هو **المرجع الوحيد الكامل** للمشروع. كُتب ليُغني عن أي شرح شفهي من المستخدم،
> وليبقى صالحاً عند الانتقال إلى منصّة ذكاء اصطناعي أخرى، أو عند تغيير الحساب من مشترك
> إلى شخصي، أو عند بدء جلسة جديدة بلا سياق سابق.
>
> **اقرأ هذا الملف بالكامل قبل أي عمل على المشروع.**

---

## ⚠️ تعليمات إلزامية للذكاء الاصطناعي (AI INSTRUCTIONS — MANDATORY)

### البند الصارم الأول — تحديث الحالة

> **يجب على الذكاء الاصطناعي تحديث قسم (الوضع الحالي Current Development State) داخل هذا
> الملف فوراً بعد إتمام أي خطوة، أو تعديل كود، أو حل مشكلة، دون الحاجة لطلب المستخدم في
> كل مرة.**

هذا البند غير قابل للتأجيل أو التفاوض. التحديث يتم **في نفس الدور** الذي أُنجز فيه العمل،
لا في دور لاحق ولا عند انتهاء المهمة الكبيرة. وإذا نُفّذت عدة خطوات في دور واحد، تُسجَّل
كلها.

**ما يُسجَّل في كل تحديث:**
- ماذا تغيّر (الملف والسطر بصيغة `path/file.js:123`)
- لماذا تغيّر (المشكلة الحقيقية لا وصف الكود)
- كيف تُحقّق منه (بناء ناجح؟ اختبار؟ تحقّق تشغيلي في المتصفّح؟ أم لم يُتحقّق بعد؟)
- ما بقي معلّقاً بسببه

### بنود إلزامية أخرى

1. **لا تدّعِ تحقّقاً لم يحدث.** ميّز دائماً بين: «بُني بنجاح» و«اجتاز اختباراً آلياً»
   و«تحقّقتُ منه تشغيلياً في المتصفّح». الثلاثة مستويات مختلفة ولا يُستبدل أحدها بالآخر.
2. **لا تحذف بيانات مالية حقيقية** (فواتير، سندات، ورديات) دون إذن صريح لكل عملية.
   الحذف الخام من Firestore يترك الوردية والخزينة غير متسقتين.
3. **لا تُدخل كلمات مرور أو أرقام PIN.** ممنوع منعاً باتاً. اطلب من المستخدم إدخالها.
4. **اكتب التعليقات بالعربية** بنفس أسلوب المشروع: اشرح **المشكلة الحقيقية** التي يحلّها
   الكود لا ما يفعله الكود حرفياً. انظر أمثلة في `syncEngine.js` و `AppContext.jsx`.
5. **بعد أي تعديل: ابنِ.** `npm.cmd run build` — ولا تُسلّم عملاً لم يُبنَ.

---

## 1. هوية المشروع

| | |
|---|---|
| **الاسم** | بيت الورد للزهور والهدايا — نظام نقاط بيع (POS) وكاشير |
| **المسار** | `D:\FL-HO2018` |
| **اللغة/الاتجاه** | عربي بالكامل، RTL |
| **المستخدم** | صاحب المتجر (مدير عام) — `fl.ho2018@gmail.com` |
| **الحالة** | **تحت الإنشاء** — ليس في إنتاج نهائي، وبيانات الاختبار مقبولة |
| **Git** | ❌ لا يوجد مستودع git في هذا المجلد |

### طبيعة العمل
متجر زهور وهدايا. البيع نقداً وشبكة وتحويل وتمارا ونينجا وفيزا و**آجل** و**تقسيم**
(عدة وسائل في فاتورة واحدة). عدة أجهزة كاشير تعمل **في نفس الوقت** على نفس البيانات،
وهذا هو مصدر أصعب المشاكل في المشروع.

---

## 2. التقنيات

```
React 18.3          واجهة
Vite 5.4            بناء وخادم تطوير
Tailwind 3.4        تنسيق
Firebase 12.18      Auth + Firestore + Hosting
qz-tray 2.3         توجيه الطابعات (أُضيف 2026-09-13)
jsbarcode           توليد الباركود
qrcode              رمز ZATCA الضريبي
xlsx (SheetJS)      استيراد/تصدير إكسل
html5-qrcode        مسح الباركود بالكاميرا
react-simple-keyboard  لوحة مفاتيح افتراضية للشاشات اللمسية
lucide-react        الأيقونات
vite-plugin-pwa     تطبيق ويب تقدّمي
```

**لا يوجد Electron.** البرنامج تطبيق ويب/PWA خالص. سكربت `package` مجرّد ضغط للمشروع.

---

## 3. قيود التشغيل ⚠️

### PowerShell يمنع npm.ps1
سياسة التنفيذ على جهاز المستخدم تمنع `npm.ps1` و `npx.ps1`. **استعمل دائماً `.cmd`**:

```powershell
cd D:\FL-HO2018; npm.cmd run build
cd D:\FL-HO2018; npx.cmd firebase deploy --only hosting
```

كتابة `npm` المجرّدة تفشل بخطأ سياسة تنفيذ — لا بخطأ في المشروع.

### أوامر PowerShell
- لا يوجد `&&` — استعمل `;` أو `if ($?) { }`
- لا `head`/`tail`/`which` — استعمل `Select-Object -First/-Last` و `Get-Command`

### النشر
```powershell
cd D:\FL-HO2018; npm.cmd run build
cd D:\FL-HO2018; npx.cmd firebase deploy --only hosting
```
- مشروع Firebase: `flower-house-8888d`
- الموقع: https://flower-house-8888d.web.app
- مجلّد النشر: `dist`

### خادم التطوير
- المنفذ **3000** (`vite.config.js`) — ويقرأ `process.env.PORT` إن وُجد.
- ⚠️ **مفتاح Firebase مقيَّد بمُحيلات (HTTP referrers)**. منفذ غير 3000 يعطي:
  `auth/requests-from-referer-http://localhost:XXXX/-are-blocked`
  فلا تشغّل خادم التطوير على منفذ عشوائي إن أردت تسجيل الدخول.

---

## 4. خريطة الملفات

### القلب
| الملف | الحجم | المسؤولية |
|---|---|---|
| `src/context/AppContext.jsx` | **308 ك.ب** | الحالة المركزية كاملة: المنتجات، الفواتير، العملاء، الموردون، الورديات، الخزينة، الإشعارات، النسخ الاحتياطي. **أكبر ملف وأخطره.** |
| `src/utils/syncEngine.js` | 41 ك.ب | محرّك المزامنة متعدّد الأجهزة عبر Firestore |
| `src/utils/printHelper.js` | 69 ك.ب | بناء HTML لكل المطبوعات + نقطة الطباعة الوحيدة |
| `src/utils/helpers.js` | 35 ك.ب | حسابات مشتركة، ZATCA TLV، الباركود التسلسلي |
| `src/utils/permissions.js` | 22 ك.ب | الصلاحيات التفصيلية لكل دور |
| `src/utils/staffPerformance.js` | 16 ك.ب | **جديد** — محرّك أداء الموظفين والبونص |
| `src/utils/qzPrint.js` | 12 ك.ب | **جديد** — توجيه الطابعات عبر QZ Tray |
| `src/utils/security.js` | 14 ك.ب | تجزئة PIN وبطاقات NFC |
| `src/utils/initialData.js` | 16 ك.ب | البيانات والإعدادات الافتراضية |
| `src/utils/audit.js` | 6.7 ك.ب | سجل التدقيق (إضافة فقط، لا يُحذف) |
| `src/utils/authUsers.js` | 0.9 ك.ب | خريطة بريد Firebase ← دور النظام |

### الشاشات الرئيسية
```
src/components/
  pos/PosRegister.jsx           شاشة الكاشير والبيع
  products/ProductsScreen.jsx   المخزون والأصناف
  invoices/InvoicesScreen.jsx   الفواتير والاسترجاع
  customers/CustomersScreen.jsx العملاء والديون والآجل
  suppliers/                    الموردون والمشتريات
  expenses/                     المصروفات
  cashier/                      الخزينة والورديات + tabs/
  reports/                      التقارير + tabs/
  settings/SettingsScreen.jsx   الإعدادات (149 ك.ب) + tabs
  dashboard/Dashboard.jsx       لوحة التحكّم
  common/VirtualKeyboardWrapper.jsx  لوحة المفاتيح الافتراضية
```

### تبويبات التقارير
```
reports/tabs/
  StaffAndShiftsTab.jsx      94 ك.ب — أداء الموظفين (قيد إعادة البناء)
  PaymentsAndVatTab.jsx      58 ك.ب — الدفع والضريبة
  FinancialOverviewTab.jsx   33 ك.ب
  ShiftsHistoryLog.jsx       28 ك.ب
  AuditLogTab.jsx            12 ك.ب
  TopProductsTab.jsx          6 ك.ب
```

### تبويبات الإعدادات
```
settings/
  ResetAccountsTab.jsx        69 ك.ب — التصفير
  UsersSettingsTab.jsx        56 ك.ب — المستخدمون والصلاحيات و PIN
  PaymentsSettingsTab.jsx     45 ك.ب — وسائل الدفع والعمولات البنكية
  BackupSettingsTab.jsx       41 ك.ب — النسخ الاحتياطي
  BarcodeSettingsTab.jsx      33 ك.ب — إعدادات الباركود
  PrintersSettingsPanel.jsx   جديد — توجيه الطابعات (QZ)
  ThemesSettingsTab.jsx       16 ك.ب
  PosSettingsTab.jsx          11 ك.ب
  MenuSettingsTab.jsx         10 ك.ب
```

---

## 5. قواعد ثابتة لا تُكسر (INVARIANTS)

هذه قواعد دُفع ثمنها بأخطاء حقيقية. **لا تخالفها دون فهم كامل.**

### 5.1 الحقول التراكمية تُزامَن بالفرق لا بالقيمة
```js
INCREMENT_OWNED = { products: ['stock'], customers: ['balance'], suppliers: ['balance'] }
```
- البيع يرسل `increment(-3)` لا «اجعل المخزون ٤».
- عند تحديث مستند قائم، المحرّك **يُجرِّد** هذه الحقول من الكتابة الكاملة ويكتب الباقي
  بـ `merge` — `syncEngine.js:693`.
- **السبب**: جهازان يبيعان نفس الصنف في نفس اللحظة؛ الكتابة المطلقة تُضيع خصم أحدهما.

### 5.2 الأساس الذي يُقاس عليه الفرق هو ما رآه المستخدم
عند حساب فرق مخزون أو رصيد، قِس مقابل **القيمة التي كانت أمام المستخدم لحظة فتح النموذج**
(`stockBaseline`)، لا مقابل القيمة الحيّة. وإلا أعاد الحفظُ كلَّ بيعٍ تمّ على جهاز آخر
أثناء فتح النافذة. انظر `AppContext.jsx` → `updateProduct`.

### 5.3 لا حذف ضمني إطلاقاً
غياب سجل من مصفوفة جهاز **لا يعني** حذفه. الحذف لا يحدث إلا بأمر صريح
(`deleteRecord`/`deleteRecords`) أو تصفير مقصود. `syncEngine.js:704`.

### 5.4 النسخة الأقدم لا تُكتب فوق الأحدث
مقارنة `updatedAt`؛ ونسخة بلا ختم لا تُكتب فوق نسخة مختومة. `syncEngine.js:668`.

### 5.5 الحالة المتقدّمة لا تُلغى
فاتورة `refunded` أو `isCreditSettled` أعلى رتبةً من `completed` ولا ترتدّ.
`AppContext.jsx:540`.

### 5.6 كل الطباعة من نقطة واحدة
`printHelper.js` → `printHtmlDirectly(html, title, routing)`. أي طباعة جديدة تمرّ من هنا
كي ترث توجيه الطابعات تلقائياً.

### 5.7 سجل التدقيق إضافة فقط
`audit.js` — لا يُعدَّل ولا يُحذف. كل عملية حسّاسة (مرتجع، تسوية مخزون، تعديل أسعار)
تُسجَّل باسم من نفّذها والقيمة قبل وبعد والسبب.

---

## 6. المزامنة متعدّدة الأجهزة (الأهم)

- `clientId` يُحفظ في `localStorage['naif_pos_client_id']` ويُقرأ **مرة واحدة عند الإقلاع**.
- لقطة واردة من نفس `_src` تُتجاهل — `syncEngine.js:240`.
- **لمحاكاة جهازين على نفس المتصفّح**: بدّل `naif_pos_client_id` في `localStorage` **بين
  إقلاع التبويبين**، فيحمل كلٌّ منهما معرّفاً مختلفاً في ذاكرته. (تبويبان بنفس المعرّف
  يتجاهل كلٌّ تغييرات الآخر فيبطل الاختبار.)
- المجموعات في Firestore: `pos_products`, `pos_customers`, `pos_invoices`, `pos_receipts`,
  `pos_backups`, `pos_suppliers`, `user_shifts`, `shifts_history`, `drawer_tx`, …

### قراءة Firestore مباشرةً من المتصفّح (للتشخيص)
```js
const fb = await import('/src/utils/firebase.js');
const fs = await import('/node_modules/.vite/deps/firebase_firestore.js?v=<hash>');
const d = await fs.getDoc(fs.doc(fb.db, 'pos_products', '<id>'));
d.data();
```
(الاستيراد المجرّد `'firebase/firestore'` لا يعمل في الطرفية — استعمل مسار Vite.)

---

## 7. الصلاحيات والأدوار

`src/utils/authUsers.js` يربط بريد Firebase بدور النظام:
```
fl.ho2018@gmail.com  → admin       (المدير العام)
f1@flower-house.com  → supervisor
f2@flower-house.com  → accountant
f3@flower-house.com  → cashier
f4@flower-house.com  → cashier
```

**طبقتا دخول**: (١) حساب Firebase للجهاز، (٢) **رمز PIN شخصي** لتحديد من يعمل الآن.
المستخدمون في `naif_pos_v3_users` بحقل `pinHash` (مُجزّأ، `security.js`).

المستخدمون الحاليون: `user-1` كاشير، `user-2` مدير، `user-3` محاسب، `user-4` مشرف.

---

## 8. الطباعة

### الوضع الحالي
- كل المطبوعات HTML عبر `printHtmlDirectly` في إطار مخفي.
- `--kiosk-printing` (سكربت `تشغيل_الكاشير_طباعة_مباشرة.bat`) يطبع صامتاً **على طابعة
  ويندوز الافتراضية وحدها** — فتذهب الملصقات لطابعة الفواتير.
- **المتصفّح لا يسمح باختيار طابعة بالاسم** — قيد أمني في كل المتصفحات.

### الحل المعتمد: QZ Tray
`src/utils/qzPrint.js` + `src/components/settings/PrintersSettingsPanel.jsx`

ثلاث وجهات مستقلّة في `storeInfo.printers`:
```js
printers: {
  useQz: false,
  invoice: { mode: 'default', name: '', host: '', port: 9100 },
  report:  { mode: 'follow_invoice', ... },
  barcode: { mode: 'name', ... }
}
```
الأوضاع: `default` (افتراضية النظام) | `name` (طابعة مثبَّتة بالاسم) |
`network` (IP ومنفذ مباشرةً بلا تعريف — لأن طابعة الفواتير أحياناً على الشبكة) |
`follow_invoice` (للتقارير).

**إن لم يكن QZ مثبَّتاً يرجع البرنامج للطباعة العادية ولا يتعطّل.**

### أجهزة المستخدم
طابعة حرارية 80مم + طابعة A4 + طابعة ملصقات باركود بمقاسات مختلفة.

---

## 9. الاختبارات

```powershell
cd D:\FL-HO2018; npm.cmd test            # الكل
cd D:\FL-HO2018; npm.cmd run test:staff  # محرّك الأداء فقط
```

**المجموع الحالي: ١٣٤/١٣٤ اختباراً.**

| الملف | العدد | التغطية |
|---|---|---|
| `tests/financial_real.test.mjs` | ٤٠ | **الكود الحيّ**: `getCartTotals` و `generateZatcaTLV` بالاستخراج الحرفي، و `security.js` بالاستيراد المباشر. كشف عطل `NaN` في إجمالي الفاتورة. |
| `tests/reset_purge.test.mjs` | ١٢ | يستخرج `purgeLocalKey` من `AppContext.jsx` حرفياً — التصفير يمسح localStorage و IndexedDB معاً، والاسترجاع يحترم ختم التصفير. |
| ~~`financial_calculations.test.cjs`~~ | ~~١٧~~ | ⛔ **أُخرج من `npm test`**: كان ينسخ الدوال ولا يستوردها، فلا يثبت شيئاً عن الكود الحيّ. بقي للرجوع لحالات ديون FIFO. |
| `tests/staff_performance.test.mjs` | ١٩ | يستورد `staffPerformance.js` الحقيقي مباشرةً. |
| `tests/pin_setup_gate.test.mjs` | ١٢ | يستخرج `pinSetupTarget` من `AppContext.jsx` **حرفياً** وينفّذه — تصعيد الصلاحيات والحبس خارج النظام. |
| `tests/expected_cash.test.mjs` | ١٢ | يستخرج `computeExpectedCash` و `sumDrawerCashIn/Out` من `useShiftMetrics.js` حرفياً — استثناءات منع الخصم المزدوج. |
| `tests/tspl_label.test.mjs` | ٣٩ | يستورد `tsplLabel.js` الحقيقي — المقاس بالنقاط، الترميز، رفض العربية، حدود النسخ، بوابة التشغيل. |

**نمطان للاختبار في هذا المشروع:**
1. **استيراد مباشر** — للوحدات الخالية من الاستيرادات (`staffPerformance.js`, `tsplLabel.js`).
2. **استخراج النصّ حرفياً** ثم `new Function` — للمنطق المدفون داخل ملفات ضخمة لها
   استيرادات (`AppContext.jsx`, `useShiftMetrics.js`). ميزته أن الاختبار **لا يمكن أن
   ينجح على منطق منسوخ متخلّف عن المصدر**، وهو عيب الملف الأول أعلاه.

**درس**: لكتابة اختبار يستورد كوداً حقيقياً، اجعل الوحدة خالية من الاستيرادات واكتب
الاختبار بامتداد `.mjs`.

---

## 10. عوائق معروفة أثناء التطوير

| العائق | التفصيل |
|---|---|
| **تسجيل الدخول** | الذكاء الاصطناعي لا يستطيع إدخال PIN. أي تحقّق داخل التطبيق يحتاج المستخدم. |
| **نافذة الطباعة** | بعد تأكيد الفاتورة تفتح نافذة طباعة أصلية **تُجمّد لوحة المتصفّح كلها** حتى تُغلق يدوياً. |
| **HMR يكسر AppContext** | تعديل `AppContext.jsx` أثناء التشغيل يسبّب `removeChild NotFoundError`. **أعد التحميل** بعد تعديله. |
| **قيد المُحيلات** | خادم تطوير على منفذ غير 3000 يمنع تسجيل الدخول. |
| **`window.prompt`** | تسوية الكمية تفتح `prompt` يُجمّد الصفحة — اعترضه في الاختبار الآلي. |

### ما يمكن اختباره بلا تسجيل دخول
- شاشة الدخول نفسها (تكفي لاختبار لوحة المفاتيح الافتراضية)
- الدوال الخالصة عبر وحدة Vite حيّة:
  ```js
  const m = await import('/src/utils/helpers.js');
  m.generateSequentialBarcode(products);
  ```

---

## 11. الوضع الحالي (Current Development State)

> ⚠️ **هذا القسم إلزامي التحديث بعد كل خطوة أو تعديل أو إصلاح — دون طلب المستخدم.**

**آخر تحديث:** 2026-09-16

### 🔍 فحص شامل — 2026-09-16 (التقرير الكامل: `تقرير_الفحص_الشامل_2026-09-16.md`)
**ما تم:** بناء ناجح (١٩٥٠ وحدة)، ٣٦/٣٦ اختباراً ناجحاً، فحص وظيفي حقيقي لـ `helpers.js`
و `permissions.js` (٣١ حالة حافة باستيراد الوحدات الفعلية)، ومراجعة ساكنة لـ AppContext
و syncEngine و security و audit و firestore.rules. **النتيجة: ٧٤٪.**
❌ **لم يُشغَّل البرنامج في المتصفّح** — الدخول يحتاج PIN.

**٤ أعطال حرجة مكتشفة — أُصلح منها ٣ (انظر أدناه):**
1. ✅ **`pos_spoilage_logs` بلا قاعدة في `firestore.rules`** ← ميزة التالف (commit 30717d0)
   لا تُزامَن إطلاقاً؛ كل كتابة تفشل بـ `permission-denied`. `syncEngine.js:41`.
2. ✅ **أرقام PIN الافتراضية (`1234`/`5555`/`7777`) في `initialData.js` وفي حزمة الإنتاج
   المنشورة** — تحقّقتُ: `dist/assets/index-MLH-5iBT.js` كان يحوي `pin:"1234"` نصاً صريحاً.
3. ✅ **المدير `user-2` بلا `pin` ولا `pinHash`** ← لا يستطيع الدخول على تثبيت نظيف،
   بينما الحسابات الضعيفة المنشورة تعمل.
4. ⚠️ **تصادم أرقام الفواتير بين جهازين** — `generateInvoiceNumber` يشتقّ الرمز من معرّف
   مستخدم POS لا من `clientId`، و `maxSeq` من المصفوفة المحلية المتأخّرة. مخالفة ZATCA.
   **كُتب الإصلاح ولم يُبنَ ولم يُختبر** — انظر أدناه.

### 🔧 إصلاح الأعطال ١ و٢ و٣ — 2026-09-16

**(١) قاعدة التالف** — `firestore.rules`: أُضيفت كتلة `match /pos_spoilage_logs/{doc}`
(قراءة وإنشاء للموظف بشرط `qty is number && qty > 0`، والتعديل يثبّت `qty` و
`totalCostLoss` و `productId` و `date`، والحذف للمدير).
**تحقّق:** حمّلها محاكي Firestore بلا خطأ ترجمة عبر `emulators:exec`.
⚠️ **لم تُنشر بعد** — تحتاج `npx.cmd firebase deploy --only firestore:rules`.

**(٢) حذف الأرقام الصريحة** — `initialData.js`: حُذفت `pin: '1234'` و `'5555'` و `'7777'`
من `INITIAL_USERS`، مع تعليق يشرح سبب المنع. وأُصلح كذلك `AppContext.jsx` → `addUser`
الذي كان يمنح **`'1234'` صامتاً** لكل مستخدم يُضاف بلا رقم؛ صار من يُضاف بلا رقم يبقى
بلا `pinHash` فلا يدخل حتى يُعيَّن رقمه صراحةً.
**تحقّق:** `grep 'pin:"[0-9]*"' dist/assets/*.js` ← **لا نتيجة** (كان يُخرج ثلاثة).

**(٣) تهيئة أول رقم** — بعد حذف الأرقام لا يملك أحد تجزئة على تثبيت نظيف، فأُضيف:
- `AppContext.jsx`: `needsPinSetup` (صحيح حين لا مستخدم مفعّل يملك `pinHash` صالحاً)
  و `setupInitialPin(pin)` — تفحص قوة الرقم، وتعيد الفحص **لحظة التنفيذ** لا التصيير
  (لقطة مزامنة قد تصل بين العرض والضغط)، وتعيّنه لصاحب دور `admin`، وتقيّده في سجل التدقيق.
- `PinLockModal.jsx`: شاشة تهيئة بحقلَي رقم وتأكيد. حمايتها **حساب Firebase** لا رقم —
  لا تُعرض إلا بعد دخول ناجح بالبريد (`App.jsx:294`)، وتختفي نهائياً بوجود أي تجزئة
  فلا تصلح لإعادة تعيين رقم منسيّ. وعُطِّل الالتقاط العام للوحة المفاتيح أثناءها، وإلا
  ذهب كل رقم يُكتب في الحقل إلى محاولة دخول أيضاً فاستُهلكت محاولات الحدّ من التخمين.

**حالة التحقّق:** `npm.cmd run build` ← **ناجح**، و `npm.cmd test` ← **٣٦/٣٦**، وقواعد
Firestore تُترجَم بلا خطأ. ❌ **لم يُتحقّق بصرياً من شاشة التهيئة** — عرضها يتطلّب حالة
«بلا أي تجزئة»، وهي لا تحدث على جهازك لأن أرقامك مُعيَّنة فعلاً.

### 🔧 إصلاح العطل ٤ (رقم الفاتورة) — 2026-09-16 · ✅ بُني واختُبر ونُشر

- `helpers.js`: أُضيفت `deviceCodeFromClientId(clientId)` — أربعة محارف base36 مشتقّة من
  معرّف الجهاز. و `generateInvoiceNumber` صارت تأخذ معاملاً رابعاً `clientId`، فتبني
  الرقم بصيغة `INV-YYYYMMDD-<رمز الجهاز>-0001` بدل رمز الكاشير، **وتحسب التسلسل ضمن
  فواتير هذا الجهاز وحده** — فلا يحتاج جهازٌ أن يعرف ماذا باع الآخر كي يرقّم فاتورته،
  وهذا ما يجعله مناعةً من تأخّر المزامنة لا مجرد تقليلاً لاحتماله.
  وأُضيف حارس `usedToday` يمنع التكرار مع الفواتير بالصيغة القديمة في يوم الانتقال.
- `AppContext.jsx:1799`: تمرير `syncEngine.clientId`.
- هوية الكاشير لم تضع: مصدرها حقل `cashierId` على الفاتورة، لا نصٌّ داخل الرقم لم يكن
  فريداً أصلاً. والمعامل `user` بقي في التوقيع للتوافق وصار غير مستعمل.

**تحقّق (2026-09-16):** `npm.cmd run build` ← **ناجح، ١٩٥٠ وحدة**، و `npm.cmd test` ←
**٣٦/٣٦** (١٧ مالية + ١٩ أداء). ❌ **لم يُتحقّق تشغيلياً بجهازين** — يحتاج تسجيل دخول.
والأثر جانبي مهم: صيغة أرقام الفواتير تتغيّر من هذا التاريخ فصاعداً، والأرقام القديمة تبقى
كما هي.

**٦ أعطال متوسطة:** لا قفل ضد الضغط المزدوج في `CheckoutModal.jsx:972` · ترحيل IndexedDB
تجميلي (٢٠١ استدعاء خام مقابل ١١ آمناً؛ الفواتير والمنتجات ما زالت في localStorage) ·
الصلاحيات تُفرَض في المتصفّح وحده (تعديل `role` في localStorage يفتح كل الشاشات) ·
`canAccessModule` تسمح عند المفتاح المجهول · **الاختبارات المالية تنسخ المنطق ولا تستورده
فلا تثبت شيئاً عن `AppContext`** · المزامنة تحتكم لساعة الجهاز لا `serverTimestamp`.

### آخر نشر — 2026-09-16
`https://flower-house-8888d.web.app` — **منشور ومحدَّث**. يحتوي، فوق نشر 2026-09-15،
إصلاحات الأعطال الحرجة الأربعة: حذف أرقام PIN الصريحة، وشاشة تهيئة أول رقم، ورقم الفاتورة
المشتقّ من `clientId`.

- `npx.cmd firebase deploy --only firestore:rules` ← **ناجح** (تُرجمت بلا خطأ؛ أبلغ فايربيس
  أن نسخة القواعد مطابقة للمنشورة فلم يُعِد الرفع، وأُطلقت). قاعدة `pos_spoilage_logs` حيّة.
- `npx.cmd firebase deploy --only hosting` ← **ناجح** (رُفع ١٣ ملفاً جديداً).

قبل النشر: `npm.cmd run build` ← **١٩٥٠ وحدة بنجاح**، و `npm.cmd test` ← **٣٦/٣٦ ناجح**.
❌ لم يُفتح التطبيق المنشور في المتصفّح للتحقّق البصري — الدخول يحتاج PIN من المستخدم.

### 🔬 فحص تشغيلي حيّ داخل التطبيق المنشور — 2026-09-16

فتح المستخدم جلسة دخول في لوحة المتصفّح، ففُحص **البرنامج المنشور نفسه** قراءةً فقط
(بلا بيع ولا كتابة أي بيان مالي). ما ثبت:

- ✅ **لا كلمة سرّ ولا رقم PIN صريح في أي مكان** — `naif_pos_v3_users` الحيّ: ستة مستخدمين،
  **لا أحد** يملك حقل `pin`، ومن يدخل يملك `pinHash` فقط. والحزمة المنشورة
  `assets/index-C32i3UUI.js` خالية من `pin:"NNNN"` (كانت تحوي ثلاثة). العطل ٢ مغلق حيّاً.
- ✅ **الحزمة المنشورة = ما بُني محلياً** — بصمة الملف واحدة، فالنشر وصل فعلاً.
- ✅ **رقم الفاتورة**: `clientId` الحيّ لهذا الجهاز `client_k2c45l1_1789526401817` ← رمز
  الجهاز `TDNU`. الفواتير الثلاث الموجودة بالصيغة القديمة (`C2`/`C4`)، والقادمة تُصدَر
  `INV-20260916-TDNU-0004`. ومحاكاة جهازين لا يرى أحدهما فواتير الآخر (أسوأ حالة تأخّر
  مزامنة) بالدالة الحقيقية المستخرَجة من `helpers.js`: ستّ فواتير **كلها فريدة**، والحارس
  `usedToday` لم يتصادم مع الصيغة القديمة. العطل ٤ مغلق منطقياً.
- ✅ **لا خطأ `permission-denied`** في الطرفية بعد الدخول والمزامنة. الخطأ ٤٠٠ الوحيد هو
  محاولة دخول أولى بكلمة مرور خاطئة من المستخدم — لا علاقة له بالبرنامج.

⚠️ **عطل مكتشف حيّاً — المدير العام لا يستطيع الدخول:** `user-2` (دور `admin`، «مدير
المتجر») و `user-4` («سعد») **بلا `pinHash` وبلا `pin`** على هذا الجهاز. وشاشة تهيئة أول
رقم لا تُنقذهما، لأن `needsPinSetup` تشترط ألّا يملك **أي** مستخدم مفعّل تجزئة — وهنا
أربعة يملكونها. فالنتيجة: أعلى دور في النظام محجوب، ولا مسار في الواجهة لتعيين رقمه إلا
من داخل شاشة المستخدمين التي تحتاج دخولاً. **الحل الآن**: ادخل بحساب «نايف» (مشرف) ←
الإعدادات ← المستخدمون ← عيّن رقماً لـ «مدير المتجر». **ولم يُصلَح في الكود بعد.**

❌ **ما لم يُفحص حيّاً**: قاعدة `pos_spoilage_logs` (فحصها يتطلّب كتابة سجل تالف حقيقي)،
وتوجيه الطابعات عبر QZ، وتبويب أداء الموظفين بصرياً.

### 🔧 إصلاح بوابة تهيئة رقم الدخول + عطل ذاكرة النشر — 2026-09-16

**(أ) المدير المحجوب — أُصلح.** `AppContext.jsx:5954` صار فيه `pinSetupTarget` بدل الشرط
القديم «لا أحد يملك تجزئة»، ويغطّي حالتين:
1. تثبيت نظيف (لا تجزئة إطلاقاً) ← يُستهدف صاحب دور `admin`.
2. **الموظفون يملكون أرقاماً والمدير وحده بلا رقم** — وهي الحالة التي وقعت فعلاً في
   المتجر. تُفتح الشاشة عندئذٍ **بشرط أن يكون بريد Firebase الداخل هو بريد المدير**
   (`getRoleByEmail(...)?.role === 'admin'`). بدون هذا الشرط تصير الشاشة تصعيدَ صلاحيات:
   جهاز كاشير داخل بحساب كاشير كان سيعيّن رقم المدير ثم يدخل به.

و `setupInitialPin` أُعيد بناء حراسها: يُعاد اشتقاق الهدف لحظة التنفيذ، ثم **حارس مستقلّ**
يرفض الكتابة فوق أي `pinHash` قائم مهما قال الاشتقاق، ثم فحص ثالث لبريد المدير. فالشاشة لا
تصلح أبداً لإعادة تعيين رقم منسيّ — ذلك من شاشة المستخدمين وحدها. وسجل التدقيق صار يذكر
اسم المستهدف ودوره وبريد Firebase المنفّذ.

`PinLockModal.jsx`: نصّ الشاشة يميّز الحالتين، وأُضيف زر **«لاحقاً — دخول بمستخدم آخر»**
يظهر فقط حين يملك غيرُ المدير أرقاماً؛ بدونه كانت شاشة تهيئة رقم المدير تحبس الكاشير عن
البيع على جهاز المدير.

**تحقّق:** ملف اختبار جديد `tests/pin_setup_gate.test.mjs` — **١٢/١٢** — يستخرج نصّ
`pinSetupTarget` من `AppContext.jsx` حرفياً وينفّذه، فلا يمكن أن ينجح على منطق منسوخ متخلّف
عن الملف. أُضيف إلى `npm test` (المجموع صار **٤٨/٤٨**). والبناء ناجح (١٩٥٠ وحدة).

**تحقّق حيّ جزئي:** على جهازك عيّن المستخدم رقم `user-2` يدوياً أثناء الجلسة، فصار كل
المستخدمين يملكون تجزئة — وشاشة التهيئة **لم تظهر**، وهو السلوك الصحيح (الحارس يعمل).
❌ لم تُرَ الشاشة نفسها معروضة، لأن عرضها يتطلّب حالة «مدير بلا رقم» التي زالت.

**(ب) عطل ذاكرة النشر — أُصلح.** `firebase.json`: قاعدة `no-cache` كانت مكتوبة لمسار
`/index.html` وحده، **والجذر `/` لا يطابقه**، فكان يُسلَّم بـ `max-age=3600`. النتيجة:
كل جهاز يبقى على النسخة القديمة حتى ساعة بعد كل نشر — وهذا ما رُصد حيّاً: بعد نشر ناجح
ظلّت اللوحة تحمّل `index-C32i3UUI.js` القديم. **الإصلاح**: `no-cache` انتقلت إلى القاعدة
العامة `**`، و `/assets/**` تبقى `immutable` لأن أسماءها مبصومة بالمحتوى.
**تحقّق حيّ:** `curl -I /` ← `no-cache, no-store, must-revalidate`، و `/assets/...js` ←
`max-age=31536000, immutable`، وبعد تجاوز الذاكرة المحلية حمّلت اللوحة الحزمة الجديدة
`index-DnROxwCG.js`.

⚠️ **ملاحظة تشغيلية**: الأجهزة التي خزّنت `/` قبل هذا الإصلاح تبقى على القديم حتى تنتهي
ساعتها. ويوجد Service Worker (PWA) يخزّن بدوره — في الواجهة زر «تحديث التطبيق فوراً وتفريغ
الذاكرة المؤقتة» وهو المخرج السريع للمستخدم.

### 🔍 فحص شامل ثانٍ — 2026-09-16 (التقرير: `تقرير_الفحص_الشامل_2026-09-16_الثاني.md`)

**النتيجة: ٧٢٪.** فُحصت القوائم والحسابات والمزامنة والمستخدمون و Firebase والإعدادات
والنسخ الاحتياطية والطباعة والتكرار. المنطق المالي الأساسي سليم؛ الخصم عن المئة في
أشياء **تنفجر لاحقاً** لا في البيع اليومي.

**أخطر خمسة:**
1. **النسخة الاحتياطية في مستند Firestore واحد بلا تجزئة** (`syncEngine.js:811`). الحدّ
   الصلب ١ م.ب؛ عند ٥٠٠–١٠٠٠ فاتورة يُرفض الكتابة ويرمي `INVALID_ARGUMENT`. وتفشل
   **صامتة** (`console.warn` فقط، ولا ختم فتُعاد المحاولة وتفشل للأبد). أخطر بند.
2. **بريد Firebase مشترك.** القواعد تعرف الهوية بالبريد وحده، ودور PIN لا علاقة له
   بالسحابة. جهاز كاشير داخل ببريد المدير = صلاحية حذف الفواتير وتعديل الأسعار
   والمستخدمين وقراءة سجل التدقيق والنسخ — من أدوات المطوّر مباشرةً.
3. **لا قفل ضد الضغط المزدوج** على «تأكيد الدفع» (`CheckoutModal.jsx:973`): لا `disabled`
   ولا حارس داخل `handleComplete`. لمسة مزدوجة = فاتورتان ومخزون مخصوم مرتين.
   (والاسترجاع محميّ بـ `refundingIdsRef` — الحماية في الأقل تكراراً وغائبة عن الأكثر.)
4. **التوجيه غائب عن ١١ مسار طباعة**: إعادة طباعة الفاتورة يدوياً، سندات القبض، كشف
   الحساب، إقرار ZATCA، تقارير الأداء والخزينة. فنفس الفاتورة تخرج من طابعة مختلفة
   حسب طريقة طباعتها. و `printRawViaQz` (TSPL) مكتوبة ولا تُستدعى.
5. **عدّادات الورديات تُزامَن كقيم مطلقة** — `user_shifts` ليست في `INCREMENT_OWNED`،
   فتحرير وردية من جهازين يُضيع نقداً (آخر كاتب يفوز).

**تكرار مالي حقيقي:** ثلاث نسخ من تصنيف وسائل الدفع بقواعد مرتجع مختلفة
(`calculateInvoicePaymentBreakdown` / `buildPaymentBreakdown` / `classifyInvoicePayments`)،
وصيغة النقدية المتوقّعة مكرّرة حرفياً في ملفين، وصيغة ثانية موازية تعتمد عدّادات الوردية
في أربعة مواضع. تتطابق اليوم بالصدفة.

**كود ميت:** `src/hooks/usePermission.js` كاملاً (لا يستورده أحد)، و`shift.drawerExpenses`
(يُقرأ ولا يُكتب)، و`generateBarcode`، و٥ من ٨ صادرات `shareHelper.js`، و٤ صادرات
`qzPrint.js`، و`HOLD_BILLS_ENABLED=false` مع كل منطقه.

**أعطال أخرى:** المشتريات النقدية لا تُنشئ سطراً في حركات الدرج · محاولات الدخول
الفاشلة لا تُسجَّل إطلاقاً · `roleLabel` يصنّف المشرف والمحاسبة «كاشير» · كائن الصلاحيات
لقطة جامدة فأي صلاحية جديدة تُمنَع صامتة عن الموظفين الحاليين · تبويب «التصفير» محميّ
بصلاحية النسخ الاحتياطي لا بصلاحية تدمير · `userReports` مدخل مكرّر لتبويب موجود.

❌ **لم تُنفَّذ عمليات بيع أو إرجاع حقيقية** للتحقّق من الأرقام تشغيلياً.

### 🎨 تحسينات واجهة الكاشير والرأس — 2026-09-16 (منشورة)

1. **دمج «تبديل المستخدم» في اسم الكاشير** — `Header.jsx`. الزر المستقل في الشريط
   العلوي كان يفتح **نفس النافذة** التي يفتحها النقر على اسم المستخدم في الشريط الذي
   تحته: زرّان لفعل واحد على شاشة لمس ضيّقة. حُذف الزر، وصار اسم الكاشير `<button>`
   بارتفاع لمس ٣٦ بكسل وعليه أيقونة `UserCheck` تدلّ أنه قابل للنقر.
2. **تصحيح مسمّى الدور** — `Header.jsx` كان `role === 'admin' ? 'المدير' : 'كاشير'`،
   فيظهر المشرف والمحاسبة «كاشير» في شريط دائم الظهور. أُضيفت `ROLE_LABEL` و
   `ROLE_EMOJI` (مدير/مشرف/محاسب/كاشير).
3. **مسطرة السحب** — `index.css`: كانت `width: 4px` تحت تعليق «أنيق للشاشات اللمسية»،
   وهي عكس ذلك: الإصبع لا يمسك أربعة بكسلات. صارت **١٤ بكسل** مع `min-height: 48px`
   للمقبض (مقبض قصير لا يُمسك مهما عرض الشريط)، ولون المتجر، وحافة شفافة تجعله يبدو
   أنحف بصرياً. و `scrollbar-none` صارت معرَّفة فعلاً لصفوف التصنيفات الأفقية.
   ⚠️ ملاحظة: `scrollbar-thin` المستعملة في عدة ملفات **صنف غير موجود** (لا إضافة
   tailwind-scrollbar في `tailwind.config.js`) — لا أثر لها إطلاقاً.
4. **أيقونة الحاسبة** — `PosRegister.jsx`: كانت `w-6 h-6` (٢٤ بكسل) داخل سطر العنوان،
   أي أقل من نصف حد اللمس (٤٤). صارت `w-11 h-11` بفاصل عن النص.

**تحقّق:** بناء ناجح (١٩٥٠ وحدة) + ٤٨/٤٨ اختباراً + نُشر. ❌ لم يُتحقّق بصرياً بعد
النشر — الجلسة عادت لشاشة PIN.

### 🐞 عطل تشغيلي خطير رُصد حيّاً — النشر أثناء العمل يكسر كل تبويب مفتوح

أثناء الفحص: التبويب كان مفتوحاً على نسخة، ثم نُشرت نسخة جديدة. الضغط على «الرئيسية»
أعطى شاشة خطأ:
`Failed to fetch dynamically imported module: .../assets/Dashboard-XXLxpLi9.js`

**السبب**: الشاشات تُحمَّل كسولاً (`React.lazy`)، وأسماء ملفاتها مبصومة بالمحتوى.
والنشر يستبدل ملفات Firebase Hosting فتختفي البصمات القديمة. فأي تبويب مفتوح قبل
النشر يطلب ملفاً لم يعد موجوداً **عند أول انتقال لشاشة لم يزرها بعد**.

**الأثر العملي**: نشر أثناء ساعات العمل = كل جهاز كاشير يتعطّل عند أول ضغطة على شاشة
جديدة حتى يُحدّث يدوياً. وسط بيعة، هذا توقّف عمل.

**تخفيف موجود**: شاشة الخطأ فيها زرّا «تحديث وتشغيل الواجهة» و«تفريغ السلة وإعادة التشغيل».
**غير محلول**: لا إعادة تحميل تلقائية عند فشل `lazy`، ولا انتظار لحظة هدوء. **الحل
المقترح**: التقاط فشل الاستيراد الكسول وإعادة تحميل الصفحة مرة واحدة تلقائياً.

### 🔍 مراجعة الشاشة الرئيسية (Dashboard) — 2026-09-16

**ما يجب أن يبقى** (صحيح ومفيد): بطاقة الوردية وحالتها · مبيعات اليوم · نقدية الصندوق ·
تنبيه الأصناف تحت الحدّ · الأكثر مبيعاً اليوم · شبكة الأقسام (تحترم `visibleModules`
و `menuItemsOrder` من الإعدادات، فالمالك يتحكّم بها).

**أعطال مكتشفة:**
- **`Dashboard.jsx:71` شرط ميت**: `effectiveShift = (شرط) ? activeShift : activeShift` —
  **الفرعان متطابقان**، فالشرط بلا أي أثر. والتعليق فوقه يقول «وردية المستخدم الحالي
  فقط — لا نتبنّى وردية كاشير آخر كأنها ورديتنا»، وهو ما **لا يفعله الكود**. فالشاشة
  الرئيسية تعرض وردية كاشير آخر كأنها ورديتك. (`Header.jsx:91` يفعلها صحيحةً — نفس
  المنطق مكتوب مرتين، مرة صحيحة ومرة معطوبة.)
- **`Dashboard.jsx:102` صيغة خامسة للنقد**: عند إغلاق الوردية تحسب
  `startCash + todayCashSales` بلا خصم المصروفات ولا المشتريات ولا `cashOut` — فترقم
  الشاشة الرئيسية أعلى من شاشة الدرج.
- **الربح**: `todayGrossProfit = مبيعات − ضريبة − تكلفة` صحيح، ويقرأ `costAtSale` المخزَّن
  في الفاتورة لا التكلفة الحالية — سليم. و `qty ?? quantity` يعالج الفواتير القديمة.

**غير ضروري / مقترح حذفه من الرئيسية:** لا شيء زائد فعلياً — الشاشة مضبوطة. الزائد هو
في القائمة الجانبية: بند «تحليلات الكاشيرات» مدخل مكرّر لتبويب موجود داخل التقارير.

### ⏸️ موقوف بانتظار المستخدم — اختبار دورة البيع الكاملة
طُلب: بيع بكل وسائل الدفع، إرجاع وأثره على المخزون، شراء من مورد وصرف، مصروف بكل الطرق،
مراجعة الخزينة والتقارير والإعدادات تشغيلياً. **الجلسة عادت لشاشة PIN ولا أستطيع إدخاله.**
قبل البدء يجب أيضاً تحويل الطباعة لوضع «يدوي» من الإعدادات، وإلا جمّدت نافذة الطباعة
اللوحة بعد أول فاتورة.

### 🔧 تصحيح ارتداد من تعديل الواجهة + بطء — 2026-09-16

**ثلاثة أخطاء ارتكبتُها في الجولة السابقة وصُحّحت:**
1. **أخفيتُ مساطر السحب.** عرّفتُ `.scrollbar-none` في `index.css` ظنّاً أنها صنف قائم
   يُراد إبقاؤه مخفياً. الحقيقة: **لا إضافة `tailwind-scrollbar` في المشروع**، فالصنف
   كان بلا معنى وتلك المساطر ظاهرة ومقصودة في ثلاثة مواضع: صف تصنيفات الكاشير
   (`PosRegister.jsx:815`)، صف تصنيفات المخزون (`ProductsScreen.jsx:546`)، صف تبويبات
   الإعدادات (`SettingsScreen.jsx:573`). حُذف التعريف فعادت الثلاثة.
2. **أبطأتُ البرنامج.** كتبتُ `* { scrollbar-color: … }` — محدّد كوني يُجبر المتصفّح
   على حساب خاصية شريط تمرير **لكل عنصر في الصفحة**. نُقلت إلى `html` وحدها.
3. **حجم المسطرة** خُفّض من ١٤ إلى **١٢ بكسل** (مقبض بحدّ أدنى ٤٤) — قابلة للإمساك
   بالإصبع بلا أن تثقل الشاشة. تحقّق حيّ: `offsetHeight − clientHeight = 15px` على صف
   التصنيفات، والمسطرة ظاهرة في لقطة الشاشة.

**اسم المستخدم انتقل للشريط الأعلى** — كان في صفّ التنقّل (الرئيسية/الكاشير/الفواتير…)
يزاحم الأزرار. صار في الشريط العلوي مكان زر «تبديل المستخدم» المحذوف، وهو نفسه زر
التبديل. صفّ التنقّل صار خالصاً للتنقّل.

### ✅ اختبار البيع الحيّ — 2026-09-16 · **نجح**

وردية `shift-1789529539783-507` (نايف، رصيد افتتاحي ٠ — **مقفل بواسطة المدير**).
بيع نقدي: جوري ×٢ @ ١٠ = ٢٠ ر.س.

| ما فُحص | النتيجة |
|---|---|
| رقم الفاتورة | **`INV-20260916-TDNU-0004`** — مطابق حرفياً لما تنبّأ به الفحص السابق. **إصلاح رقم الفاتورة يعمل حيّاً.** |
| الإجمالي والضريبة | ٢٠٫٠٠ وضريبة ٠ — صحيح، فالضريبة معطّلة في المتجر (`taxEnabled: false`) |
| المخزون | ٤ ← **٢** ✓ |
| `costAtSale` | **٢** مخزَّنة على البند (أساس حساب الربح لاحقاً) ✓ |
| الربط | `shiftId` و `cashierId` صحيحان ✓ |
| عدّاد الوردية | `cashSales = 20` ✓ |
| السحابة | ١٦ كتابة Firestore، **لا `permission-denied`** ✓ |

⚠️ **ملاحظة**: الطباعة حُوّلت إلى `autoPrintMode: 'manual'` قبل الاختبار، وإلا جمّدت
نافذة الطباعة اللوحة. وهذا يعني أن مسار «الطباعة الفورية الصامتة» لم يُختبر.

### 🐞 عطل جديد: QZ Tray لا يعمل إطلاقاً على الموقع المنشور (HTTPS)

الطرفية تمتلئ بـ:
`Mixed Content: … loaded over HTTPS, but attempted to connect to the insecure
WebSocket endpoint 'ws://localhost.qz.io:8182/'. This request has been blocked.`

الصفحة على HTTPS، و QZ يجرّب أولاً `ws://` غير الآمن **فيحظره المتصفّح حظراً كاملاً**،
ثم يجرّب `wss://` فيفشل لغياب شهادة موثوقة. النتيجة: **طبقة توجيه الطابعات لا تعمل
على `flower-house-8888d.web.app` مهما ثُبّت QZ** — تحتاج تثبيت شهادة QZ Tray الموثوقة
على كل جهاز. هذا يضاف إلى أن ١١ مسار طباعة لا تمرّر التوجيه أصلاً.

### 🐞 عطل واتساب: حلقة لا نهائية — أُصلح ونُشر (2026-09-16)

`shareHelper.js:198 openWhatsApp` كانت تستدعي **نفسها** في مسار الاحتياط:
```js
if (!handled && document.visibilityState === 'visible') {
  openWhatsApp(urls);       // ← تستدعي نفسها، والتعليق فوقها يقول «الويب احتياطاً»
}
```
على أي جهاز **بلا تطبيق واتساب مثبَّت** تدخل الدالة حلقة لا نهائية: كل ١٫٥ ثانية تعيد
محاولة البروتوكول نفسه الذي فشل، ولا تفتح واتساب ويب أبداً، وتُراكم مستمعي أحداث بلا
توقّف. فلا الفاتورة تُرسل ولا الصفحة تهدأ. **الإصلاح**: `window.open(urls.universalUrl)`
مرة واحدة فقط.

### 📱 ما يستطيعه إرسال الواتساب فعلاً — وما لا يستطيعه

**مضبوط في إعدادات المتجر بالفعل:** `defaultShareFormat: 'image'`، و
`managerPhone: '0508333996'`، و `enableImageExport: true`.

**تحويل الرقم صحيح** (تُحقّق بتشغيل الدالة الحقيقية):
`0508333996` ← `whatsapp://send?phone=966508333996&text=…` ثم `https://wa.me/966508333996`.
فالمحاولة الأولى **تطبيق واتساب** لا الويب — وهذا هو المطلوب.

⚠️ **لكن على الكمبيوتر الصورة لا تُرفَق تلقائياً.** قياس حيّ على جهازك:
`navigator.canShare({files}) === false`. أي أن مسار «نافذة المشاركة الأصلية» غير مدعوم،
فيسقط الكود إلى: نسخ الصورة للحافظة + تنزيل ملف `.jpg` + فتح محادثة واتساب بالنص.
الكاشير يلصق بـ `Ctrl+V` أو يرفق الملف المنزَّل. **على الجوال/التابلت** تعمل نافذة
المشاركة الأصلية وتذهب الصورة للتطبيق مباشرةً.

⛔ **حدّ لا يتجاوزه أي كود:** واتساب لا يسمح بإرسال رسالة برمجياً بلا WhatsApp Business
API. أقصى ما يمكن: فتح المحادثة والنص جاهزاً، ويضغط الإنسان «إرسال». هذا قيد واتساب
لا عطل في البرنامج.

### 💾 هل تُحذف البيانات المالية من السحابة؟ — نعم، مع ثلاثة استثناءات

فُحص `AppContext.jsx:6330+` و `syncEngine.js:727`. التصفير **حذف سحابي حقيقي** لا محلي:
`saveKey(key, [], true, ts, isReset=true)` ← `setResetMark` (حقيقة مشتركة تحترمها كل
الأجهزة فوراً) ثم `getDocs` على المجموعة كلها ثم `batch.delete` لكل مستند.

**يُحذف فعلاً من السحابة:** الفواتير · الفواتير المعلقة · أرصدة العملاء · أرصدة الموردين
والمشتريات · سندات القبض والصرف · الورديات والأدراج · دفتر الخزينة · المصروفات ·
سجل الورديات · السنة المالية · التصفير الكامل.

**لا يُحذف — استثناءات يجب معرفتها:**
1. **سجل التدقيق وسجل الدخول**: `firestore.rules:230` و `:240` فيهما
   `allow delete: if false` — **الحذف ممنوع بقاعدة صريحة لا يتجاوزها حتى المدير**.
   زر «مسح سجلات الدخول» في الواجهة يمسح **النسخة المحلية فقط**، والسحابة تحتفظ بها،
   وستعود عند أول مزامنة كاملة. هذا تصميم مقصود (سجل تدقيق حقيقي) لكنه غير معلن في الواجهة.
2. **النسخ الاحتياطية** (`pos_backups`): التصفير لا يمسّها. كل نسخة تحوي لقطة كاملة من
   البيانات المالية. حذفها يدوي من شاشة النسخ.
3. **التصفير يحتاج بريد المدير**: قواعد الحذف `isAdmin()`. من جهاز داخل ببريد كاشير
   يُحذف محلياً وتفشل السحابة بـ `permission-denied` — و `flushKey` يبتلع الخطأ ويسجّله
   فقط، فالشاشة تقول «تم التصفير سحابياً ومحلياً بنجاح 🌸» **وهي لم تُحذف من السحابة**.
   ⚠️ رسالة نجاح كاذبة — تستحق إصلاحاً.

**الخلاصة العملية:** نعم تستطيع حذف كل بياناتك المالية التجريبية بعد انتهاء البناء،
من جهازك (بريد المدير) عبر الإعدادات ← تصفير. ويبقى عليك بعدها: حذف النسخ الاحتياطية
يدوياً، وقبول بقاء سجلّي التدقيق والدخول (أو حذف المجموعتين من وحدة تحكّم Firebase مباشرةً).

### ✅ اختبار تشغيلي حيّ — المرتجع ووسائل الدفع والواتساب (2026-09-16)

**١. المرتجع — نجح بالكامل.** أُرجعت `INV-20260916-TDNU-0004` (٢٠ ر.س، جوري ×٢):

| ما فُحص | النتيجة |
|---|---|
| حالة الفاتورة | `refunded` ✓ |
| **المخزون** | ٢ ← **٤** (عاد كما كان تماماً) ✓ |
| `refundShiftId` | وردية **البائع** نفسها، و `refundChargedToSeller: true` — **إصلاح البند ٧ يعمل حيّاً** ✓ |
| عدّاد الوردية | `cashSales` ٢٠ ← **٠** ✓ |
| حركة الدرج | `type:'out'`, `category:'مرتجع مبيعات نقدية'`, مبلغ ٢٠ ✓ |
| `shift.cashOut` | بقي **٠** ✓ — صحيح، فالمرتجع يُخصم عبر `cashSales` لا عبر `cashOut` (تجنّب الازدواج مقصود) |

⚠️ الاسترجاع يستعمل `confirm` ثم `prompt` — يُجمّدان الواجهة، واللوحة الآلية تُلغيهما
تلقائياً. اعتُرضا للاختبار كما ينصّ §10.

**٢. البيع بالشبكة — نجح.** `INV-20260916-TDNU-0005` (١٠ ر.س):
التسلسل صحيح (٠٠٠٤←٠٠٠٥) · `paymentMethod: 'card'` · `cardSales: 10` و `cashSales: 0` —
أي **الشبكة لا تدخل نقد الدرج**، وهو الصحيح.

**٣. الواتساب — يعمل، والمسار «تطبيق لا ويب» صحيح.**
ضُغط زر «واتساب (صورة)» على الفاتورة ورُوقب ما يحدث فعلياً:
- ✅ تُلتقط صورة الفاتورة (`html2canvas` ← JPEG؛ تُحقّق أن المتصفّح يدعم `image/jpeg`).
- ✅ يسأل عن رقم العميل إن لم يكن مسجّلاً (العميل النقدي العام بلا رقم).
- ✅ يفتح **تطبيق واتساب مباشرةً**: `whatsapp://send?phone=966508333996&text=…`
  — الرقم `0508333996` تحوّل صحيحاً، والنص يحمل رقم الفاتورة والأصناف والإجمالي
  وطريقة الدفع وهاتف المتجر.
- ⚠️ **على الكمبيوتر الصورة لا تُرفَق تلقائياً**: `navigator.canShare({files}) === false`
  على جهازك، فيسقط الكود إلى نسخ الصورة للحافظة + تنزيلها + فتح المحادثة بالنص.
  اللصق `Ctrl+V` أو إرفاق الملف يدوي. على الجوال تعمل نافذة المشاركة الأصلية.
- ⛔ الإرسال نفسه يحتاج ضغطة إنسان — قيد واتساب (لا إرسال برمجي بلا Business API).

> **تصحيح لقراءة أولى خاطئة مني**: ظهر الرابط أول مرة بلا `phone=`، وظننتُه عطلاً. السبب
> أنني كنتُ قد اعترضتُ `prompt` لاختبار المرتجع فأعاد نصّاً ليس رقماً. البرنامج سليم.

**متبقٍّ من الاختبار التشغيلي:** الآجل · التقسيم · فيزا/تمارا/نينجا/تحويل ·
الشراء من مورد والصرف · المصروفات بكل الطرق · الخزينة · التقارير · الإعدادات.

### 🚨 عطل حرج أُصلح — رقم دخول من ٥ أرقام يحبس صاحبه خارج النظام (2026-09-16)

`PinLockModal.jsx:66 handleDigit` يتحقّق **تلقائياً عند الرقم الرابع**
(`if (newPin.length === 4) verifyPin(newPin)`) ولا يوجد زر «دخول» على لوحة الأرقام
اللمسية. وشاشة تهيئة أول رقم التي أُضيفت في هذه الجلسة كانت تقبل **٤ إلى ٦** أرقام
(لأن `validatePinStrength` تجيز ذلك، وهي مشتركة مع مسارات أخرى).

**النتيجة لو حدث:** مدير يعيّن رقماً من خمسة أرقام ← كل محاولة دخول تُرسَل وتفشل عند
الرقم الرابع دائماً ← **حبس دائم خارج النظام بلا أي مسار استرجاع**. وشاشة التهيئة نفسها
لا تُعرض بعدها لأن التجزئة صارت موجودة.

**الإصلاح:** الحقلان `maxLength={4}`، وزر الحفظ لا يُفعّل إلا عند `length === 4`، وحارس
`/^\d{4}$/` داخل `setupInitialPin` نفسها (لا في الواجهة وحدها). ونصّ الشاشة صار يقول
«أربعة أرقام». ⚠️ ملاحظة باقية: `UsersSettingsTab.jsx:180` يفرض ٤ أرقام بينما
`validatePinStrength` تجيز ٤–٦ — التوحيد الصحيح هو تقييد الدالة نفسها بأربعة.

**تحقّق:** بناء ناجح · ٤٨/٤٨ اختباراً · نُشر.

### 🔧 جولة إصلاحات بالأولوية — 2026-09-16 (منشورة · ٤٨/٤٨ · بناء ناجح)

أُصلحت ١١ ملاحظة من قائمة الـ٣٠، بالترتيب من الأخطر:

| # | العطل | الإصلاح |
|---|---|---|
| **٥** | **النشر يكسر كل تبويب مفتوح** (رُصد حيّاً **مرتين**) | `App.jsx`: `lazyWithReload` — يلتقط فشل الاستيراد الكسول ويعيد تحميل الصفحة **مرة واحدة** (حارس `sessionStorage` بنافذة دقيقة يمنع حلقة عند انقطاع الشبكة). ووعد غير محسوم بدل شاشة خطأ تومض |
| **٣** | **لا قفل ضد الضغط المزدوج** على «تأكيد الدفع» | `CheckoutModal.jsx`: `isCompletingRef` (في `useRef` لا `useState` — الحالة لا تُحدَّث داخل نفس دورة الأحداث فضغطتان متلاحقتان تقرآن القيمة القديمة). يُغلق بعد نجاح كل التحقّقات، ويُفتح إن لم تُنشأ فاتورة. والزر `disabled` مع نصّ «جارٍ إصدار الفاتورة…» |
| **٤** | **التصفير يعرض نجاحاً كاذباً** | `syncEngine.js`: التصفير وحده لم يعد يبتلع خطأه — يستدعي `resetErrorHandler`. و `AppContext` يعرض تنبيهاً صريحاً يذكر أن المحلي مُسح والسحابة لا، ويطلب التنفيذ من جهاز المدير، ويقيّده في سجل التدقيق |
| **٩** | **محاولات الدخول الفاشلة لا تُسجَّل** | `recordFailedLoginAttempt` جديدة، مربوطة بفشل PIN وفشل NFC. **لا تسجّل الرقم المُدخل ولا جزءاً منه** — تسجيله يحوّل السجل نفسه إلى قائمة أرقام محتملة |
| **١٠** | **شرط ميت في الرئيسية** `? activeShift : activeShift` | `Dashboard.jsx`: تُقرأ وردية المستخدم من `userShifts` أولاً، ولا يُقبل `activeShift` إلا إن كانت له. وإلا **لا وردية** — أصدق من رقم يخصّ غيره |
| **١١** | `roleLabel` يصنّف المشرف والمحاسبة «كاشير» في سجل الدخول | خريطة أدوار كاملة |
| **١٤** | `drawerExpenses` حقل ميت (لغم ازدواج) | حُذف من الموضعين مع تعليق يشرح لماذا لا يُعاد |
| **١٥** | **المشتريات النقدية بلا سطر في الدرج** | سطر `subType: 'purchase'` يُسجَّل، و**مستثنى** من جمع `cashOut` في الشاشتين لأن المبلغ يُخصم أصلاً من قائمة المشتريات |
| **١٨** | **كائن الصلاحيات لقطة جامدة** — صلاحية جديدة تُمنع صامتة عن الجميع | المفتاح **الغائب** يرجع لقالب الدور؛ والموجود يُحترم كما هو (بما فيه المنع الصريح) |
| **١٩** | `canAccessModule` تسمح عند المفتاح المجهول | صار **يمنع** ويسجّل تحذيراً — شاشة محجوبة تُكتشف في دقيقة، ومفتوحة للجميع قد لا تُكتشف أبداً |
| **٢٠** | تبويب **التصفير** محميّ بصلاحية النسخ الاحتياطي | مفتاح مستقلّ `settings_reset_accounts` (افتراضه `false` لكل الأدوار) — حفظ البيانات وإتلافها ليسا صلاحية واحدة |
| **٢١** | `validatePinStrength` تجيز ٤–٦ والنظام لا يقبل غير ٤ | وُحّدت على **أربعة أرقام** في الدالة نفسها |
| **٢٧** | `userReports` مدخل مكرّر | حُذف من القائمة، ويُرشَّح من الترتيب المحفوظ للمتاجر التي حفظته |

### ✅ اختبار التقسيم والآجل — نجحا

- **تقسيم** `INV-20260916-TDNU-0006`: ١٤٥ ر.س = **٧٢٫٥ كاش + ٧٢٫٥ شبكة**، ومجموع
  الدفعات = الإجمالي بالضبط. `cashSales: 72.5` و `cardSales: 82.5` (١٠ سابقة + ٧٢٫٥).
- **آجل** `INV-20260916-TDNU-0007`: ١٠ ر.س على «عميل اختبار كلود B2» —
  **رصيد العميل ٠ ← ١٠** ✓، و `creditSales: 10`، ولم يمسّ النقد ولا الشبكة ✓.
  وشاشة الآجل تعرض الرصيد الحالي والرصيد بعد الفاتورة قبل التأكيد — تصميم جيّد.

### 💾 النسخ الاحتياطية السحابية — إعادة بناء كاملة (2026-09-16 · منشورة)

**الاكتشاف الأهم:** `listBackups` و `getBackup` و `deleteBackup` في `syncEngine.js`
**لم يستدعها أي مكوّن في البرنامج إطلاقاً**. أي أن النسخة اليومية كانت تُرفع إلى
`pos_backups` فعلاً، ثم **لا تُعرض ولا تُسترجع ولا تُحذف**. `BackupSettingsTab` كان يعرض
`localBackups` من `localStorage` وحدها. ونسخةٌ لا تُستعاد ليست نسخة احتياطية — هي تكلفة
تخزين تُطمئن صاحبها بلا سبب.

**١. التجزئة** — `syncEngine.js`: كان هناك حارس `AUTO_BACKUP_MAX_BYTES = 800 KB` يرفض
الرفع فوقه (لتفادي حدّ مستند Firestore ١ م.ب). الحارس يمنع الانهيار لكنه يصنع ما هو أسوأ:
متجر يكبر ← النسخة تتجاوز الحدّ ← **النسخ تتوقّف نهائياً وصامتةً** بـ `console.warn` وحده،
ولا ختم يُكتب فتُعاد المحاولة وتفشل كل يوم إلى الأبد.
الآن: مستند وصفي صغير + `data` مقسّمة ٧٠٠ ك.ب لكل قطعة في **مجموعة فرعية** `chunks`.
والمجموعة الفرعية اختيار مقصود: قراءة `pos_backups` لا تجلب المجموعات الفرعية، فصارت
قائمة النسخ خفيفة فعلاً بدل أن تُنزّل كل نسخة بكامل بياناتها لعرض أسمائها.
والقطع تُكتب **قبل** المستند الوصفي، فانقطاع في المنتصف لا يُظهر في القائمة نسخةً بيانها
ناقص ويُظنّ أنها صالحة. و `getBackup` ترفض النسخة ناقصة القطع صراحةً (`incomplete`) —
نسخة مبتورة تُسترجَع أخطر من نسخة غائبة.

**٢. سياسة استبقاء** — `pruneBackups(keep = 30)` تعمل بعد كل رفع ولا تُفشله إن فشلت.

**٣. الفشل صار مسموعاً** — `runAutoCloudBackup` كان يكتفي بـ `console.warn`. الآن
`console.error` + إشعار في مركز الإشعارات + تنبيه صريح للرفع اليدوي، مع تمييز
`permission-denied` («تُرفع من جهاز المدير وحده»).

**٤. واجهة النسخ السحابية** — `CloudBackupsPanel` جديد داخل `BackupSettingsTab`:
قائمة بالتاريخ والحجم وعدد الفواتير والأصناف، وزرّا **استرجاع** و**حذف** لكل نسخة، وزر
تحديث. والاسترجاع محميّ بنفس بوابة **رمز المدير** المستعملة للاستيراد من ملف — الاثنان
يستبدلان بيانات المتجر في كل الأجهزة، فلا يصحّ أن يكون أحدهما محميّاً والآخر مفتوحاً.
ويأخذ لقطة أمان `pre_cloud_restore_safety` قبل الاستبدال.

**٥. قاعدة Firestore** — أُضيف `match /pos_backups/{doc}/chunks/{chunk}` (مدير فقط)،
وإلا فشلت كتابة القطع كلها. **نُشرت** وتُرجمت بلا خطأ.

**حالة التحقّق:** بناء ناجح · **٤٨/٤٨ اختباراً** · القواعد منشورة · الاستضافة منشورة.
❌ **لم يُتحقّق تشغيلياً**: رفع نسخة مجزّأة فعلية، وعرضها في القائمة، واسترجاعها —
عادت لوحة المتصفّح إلى **شاشة دخول Firebase** (لا شاشة PIN فقط) ولا أستطيع إدخال البريد
وكلمة المرور. هذا أول ما يجب فحصه في الجلسة القادمة: الإعدادات ← النسخ ← هل تظهر نسخة؟

### 🔧 جولة إصلاحات ثانية — 2026-09-16 (منشورة · **٦٠/٦٠ اختباراً**)

| # | العطل | الإصلاح |
|---|---|---|
| **٦** | **١١ مسار طباعة بلا توجيه** — الفاتورة اليدوية وسندات القبض وكشف الحساب وإقرار ZATCA والتسوية وتقارير الأداء و Z وتقرير الخزينة والطباعة التجريبية | مُرِّر `{ purpose, storeInfo }` في **١١/١١**. `purpose: 'invoice'` للفواتير والسندات، و `'report'` للتقارير وكشف الحساب. والطباعة التجريبية في الإعدادات تُوجَّه من **`formData`** لا `storeInfo` — لأنها تجرّب إعداداً قيد التعديل لم يُحفظ بعد |
| **١٢/١٣** | **خمس صيغ للنقدية المتوقّعة** تتطابق بالصدفة | `useShiftMetrics.js`: `computeExpectedCash` + `sumDrawerCashIn` + `sumDrawerCashOut` — مصدر واحد. و `ShiftHeaderModal` و `CurrentShiftDrawerTab` صارا يستدعيانها بدل نسختين حرفيتين |
| **٢٦** | كود ميت | حُذف `src/hooks/usePermission.js` كاملاً (١٢٦ سطراً لا يستوردها أحد)، وحُذفت `generateBarcode` (المولّد العشوائي القديم) — إبقاء مولّد عشوائي بجانب تسلسلي دعوةٌ لأن يُستعمل الخطأ منهما |

**اختبار جديد:** `tests/expected_cash.test.mjs` — **١٢/١٢** — يستخرج الدوال الثلاث من
`useShiftMetrics.js` حرفياً وينفّذها. يغطّي: الصيغة الأساسية · المرتجع · المصروف
والمشتريات · **حالة الوردية الحقيقية المختبَرة حيّاً** (بيع ٢٠ + إرجاعه + تقسيم كاش
٧٢٫٥ ← ٧٢٫٥) · السالب لا يُقصّ (حقيقة محاسبية لا خطأ يُخفى) · القيم غير الرقمية ·
**استثناءات منع الخصم المزدوج** في `sumDrawerCashOut` (مصروف/مشتريات/مرتجع) و
`sumDrawerCashIn` (العهدة الافتتاحية والحركات المعلّقة).

**المجموع الآن: ٦٠/٦٠** (١٧ مالية + ١٩ أداء + ١٢ بوابة PIN + ١٢ نقدية متوقّعة).
بناء ناجح · منشور. ❌ لم يُتحقّق تشغيلياً من التوجيه (يحتاج QZ مثبّتاً وشهادة موثوقة).

### 🏷️ طباعة الباركود بـ TSPL خام — جوهر شكوى «غير احترافية» (2026-09-16 · منشورة)

**لماذا لم يكن ضبط CSS يكفي:** المتصفّح يطبع **صفحةً لا ملصقاً**. مهما ضُبط `@page` تبقى
هوامش المتصفّح مفروضة، والتحجيم يختلف بين جهاز وآخر، والباركود يُرسم **صورةً** فتتغيّر
سماكة خطوطه مع الدقّة فيصعب مسحه. TSPL لغة طابعات الملصقات نفسها: المقاس بالملّيمتر
صراحةً، والباركود يُولَّد **داخل الطابعة بخطوطها** لا كصورة.

**`src/utils/tsplLabel.js` (جديد، بلا استيرادات ليكون قابلاً للاختبار):**
- `buildTsplLabel` — `SIZE`/`GAP` بالملّيمتر، `CLS`، `BARCODE`، `TEXT`، `PRINT n,1`.
- `resolveSymbology` — `EAN13` و `UPCA` يشترطان الطول والأرقام، وإلا **رجوع آمن إلى
  CODE128**؛ فملصق بترميز خاطئ تُخرجه الطابعة فارغاً بلا سبب ظاهر.
- `mmToDots(mm, dpi)` — يدعم ٢٠٣ و٣٠٠ نقطة/بوصة.
- `isPrintableAscii` — ⛔ **TSPL لا يدعم العربية**: خطوط الطابعة الداخلية بلا محارف
  عربية ولا اتصال حروف. فالنص العربي **يُحذف من الملصق** بدل أن يُطبع رموزاً مشوّهة.
  الباركود والسعر يُطبعان دائماً. **قرار صريح لا نقص**، ومكتوب في الواجهة للمستخدم.
- `canUseTspl` — يشترط QZ مفعّلاً **و** `useTspl: true` **و** وجود باركود.

**الربط:** `printHelper.js printThermalBarcodeLabels` صارت `async`: تجرّب TSPL أولاً، وإن
لم يُعالَج **ترتدّ صامتةً إلى HTML** ولا تتعطّل. ومفتاح التشغيل في تبويب الباركود
(معطّل افتراضياً) مع تحذير صريح بالمتطلّبات وحدّ العربية.

⚠️ **عطل فرعي أُصلح أثناء الربط:** المستدعيان (`ProductsScreen` و `BarcodeSettingsTab`)
كانا يلفّان الاستدعاء بـ `try/catch` **بلا `await`** — وهذا لا يلتقط رفض الوعد إطلاقاً
بعد أن صارت الدالة غير متزامنة، فكان أي فشل يمرّ صامتاً ولا يصل حتى إلى الارتداد.
صارا `async/await` مع رسالة خطأ ظاهرة بدل `window.print()` الذي كان يطبع الصفحة كلها.

**تحقّق:** `tests/tspl_label.test.mjs` — **٣٩/٣٩** — يستورد الوحدة الحقيقية. يغطّي:
تحويل المقاس (٥٠مم@203 = ٤٠٠ نقطة، و@300 = ٥٩١) · الرجوع الآمن للترميز (٦ حالات) ·
كشف العربية والمختلط · تهريب الاقتباس · ملصق كامل (المقاس والفجوة والباركود والسعر
وعدد النسخ) · **لا حرف عربي في الخرج إطلاقاً** · حدود النسخ (٠ و−٥ ← ١، و٩٩٩٩٩ ← ٩٩٩) ·
بوابة التشغيل (٥ حالات، والارتداد هو الافتراض).

**المجموع: ٩٩/٩٩ اختباراً** (١٧ مالية + ١٩ أداء + ١٢ بوابة PIN + ١٢ نقدية + ٣٩ TSPL).
بناء ناجح · منشور. ❌ **لم يُختبر على طابعة حقيقية** — يحتاج QZ مثبَّتاً وشهادته موثوقة
وطابعة ملصقات موصولة.

### ⏸️ مؤجّل بقرار واعٍ — عدّادات الورديات المتزامنة كقيم مطلقة

`user_shifts` أفضل مما قُدّر في التقرير: **مستند لكل مستخدم** في مجموعة (لا خريطة في
مستند واحد)، وعليه حارس `updatedAt` (§5.4). فالخطر ضيّق: جهازان يكتبان **نفس** مستند
الوردية (المدير يغلق وردية كاشير بينما الكاشير يبيع) — عندها تفوز الكتابة الأحدث ختماً
حتى لو حُسبت من أساس قديم، فيضيع فرق الآخر.

الإصلاح الصحيح هو ضمّ `user_shifts` إلى `INCREMENT_OWNED` وتحويل **كل** كتّاب
`cashSales`/`cashIn`/`cashOut` إلى `adjustFields` — refactor واسع في مسار نقدي
**لا يمكن التحقّق منه حيّاً بلا تسجيل دخول وجهازين**. تنفيذ تغيير كهذا بلا تحقّق أخطر من
العطل نفسه، فأُجّل عمداً. ويخفّف من أثره أن `computeExpectedCash` صارت تُعيد الحساب من
السجلات الأصلية لا من العدّادات.

### 🔧 جولة إصلاحات ثالثة — 2026-09-16 (منشورة · ٩٩/٩٩)

| # | العطل | الإصلاح |
|---|---|---|
| **١٧** | **مؤقّت تأجيل واحد لكل المفاتيح** | `syncEngine.js`: كان مؤقّت واحد يُلغيه كل حفظ جديد ويبدأه من الصفر. فمتجر مزدحم (بيع ← فاتورة + منتجات + وردية + حركة درج، ثم بيعة تالية قبل مرور ٤٠٠ مللي ثانية) **لا يُفرَّغ فيه شيء إطلاقاً** ما دامت الحركة مستمرة — أي أن أكثر اللحظات حاجةً للمزامنة أقلّها مزامنةً. الآن **مؤقّت لكل مفتاح** + **سقف تأجيل ثانيتين** يضمن الإرسال ولو استمرّ الضغط |
| **٣٠** | **الواجهة تَعِد بمسح لا يحدث** | `ResetAccountsTab.jsx`: زر «تصفير سجلات الدخول» كان يقول «تفريغ السجل بالكامل»، بينما `firestore.rules` تمنع حذف `pos_login_logs` و `pos_audit_logs` بـ `allow delete: if false` — لا يتجاوزها حتى المدير (وهذا مقصود). فالسجلات تعود عند أول مزامنة ويظنّ المالك أن المسح فشل عشوائياً. صار النصّ يُفصح صراحةً أن المسح **محلي فقط** ويشير لوحدة تحكّم Firebase. **وعدٌ لا يُنفَّذ أسوأ من منعٍ معلن** |
| صغيرة | نصّ زر الباركود «عشوائي» | صار «توليد باركود تسلسلي فريد (الرقم التالي في التسلسل)» — كان يصف سلوكاً تغيّر |
| **٢٦** | كود ميت في `qzPrint.js` | حُذفت `isQzConnected` و `disconnectQz` و `getDefaultPrinter` (مصدَّرة ولا يستدعيها أحد **لا داخل الملف ولا خارجه**)، و `PRINT_PURPOSES`. دوالٌّ معطّلة بجانب عاملة تُوهم القارئ بمسار يعمل وهو لا يعمل |

**تصحيح لقراءة سابقة مني:** ما وصفتُه بـ«٥ من ٨ صادرات `shareHelper.js` ميتة» **غير
دقيق** — الخمس مستعملة **داخل الملف نفسه** (`shareDocument` تستدعيها)، وفحصي الأول كان
يستثني الملف من البحث. الميت فعلاً في `shareHelper.js`: **لا شيء**. وكذلك
`security.js: sha256` و `audit.js: AUDIT_LABELS` و `idbStorage.js: openIDB` — كلها
مستعملة داخلياً. الميت المؤكَّد كان في `qzPrint.js` وحده (وحُذف)، ويبقى
`idbStorage: idbRemove/idbGetAllKeys` و `themes: FONT_SIZES` (تُركت: غير ضارّة وقد تُستعمل).

### 📚 توثيق مُصحَّح — §9 و§11

- **§9 جدول الاختبارات** كان يذكر ملفين فقط. حُدِّث إلى الخمسة (**٩٩ اختباراً**) مع
  توثيق **النمطين** المستعملين: الاستيراد المباشر للوحدات الخالية من الاستيرادات، و
  **استخراج النصّ حرفياً + `new Function`** للمنطق المدفون في ملفات ضخمة — وميزته أن
  الاختبار لا يمكن أن ينجح على منطق منسوخ متخلّف عن المصدر (وهو عيب
  `financial_calculations.test.cjs` المعروف).
- **«لم يبدأ»** كان ما زال يعدّ TSPL غير منجَز بعد شحنه — صُحِّح.
- **«قيد العمل الآن»** أُعيدت كتابته ليقول بدقّة ما ينتظر المستخدم: التحقّق البصري من
  شاشة النسخ السحابية، ومفتاح TSPL، وتبويب الأداء، والدورة التشغيلية المتبقّية.
- أُضيف قسم **«قرار بانتظار المستخدم — بريد Firebase لكل جهاز»** بأثره الجانبي المقصود.

### 🔧 جولة إصلاحات رابعة — 2026-09-16 (منشورة · ٩٩/٩٩)

**١. `firestore.rules`: الأدوار مفصولة أخيراً** (القواعد منشورة وتُرجمت بلا خطأ)

أُضيفت `isSupervisor()` و `isAccountant()` و `isCashier()` مطابقةً لـ `authUsers.js`،
وفوقها بوابتان بالمعنى لا بالبريد:
- `canSeeFinancials()` = مدير + محاسب + مشرف (لا الكاشير)
- `canManageInventory()` = مدير + مشرف

وربُطت بالمجموعات الحسّاسة:
| المجموعة | قبل | بعد | لماذا |
|---|---|---|---|
| `pos_products` تعديل | `isAdmin()` | `canManageInventory()` | المشرف مسؤول المخزون فعلاً؛ وكان مضطراً لبريد المدير |
| `pos_treasury_ledger` قراءة | `isStaff()` | `canSeeFinancials()` | دفتر الخزينة يكشف حركة نقد المتجر كلها — لا شأن للكاشير به |
| `pos_purchases` قراءة | `isStaff()` | `canSeeFinancials()` | فاتورة الشراء تحمل **سعر التكلفة**، وهو ما نمنع الكاشير من رؤيته |

⚠️ **هذه الكتلة بلا أثر عملي اليوم**: كل الأجهزة تدخل ببريد المدير فتمرّ من `isAdmin()`
على أي حال. كُتبت الآن كي لا يبقى تقسيم الحسابات معلّقاً على تعديل قواعد لاحق يُنسى —
فبمجرد إنشائك `f1`…`f4` تصير الحماية فعّالة بلا عمل إضافي.

**٢. تناقض حقيقي في تبديل المستخدم — أُصلح.** `AppContext.jsx`:
تبديل المستخدم **بالرقم** يُفرّغ السلة (لأن تعليق الفواتير موقوف بقرار إدارة:
`HOLD_BILLS_ENABLED = false`)، بينما التبديل **بالبطاقة NFC** كان يُنشئ **فاتورة معلّقة**.
طريقان للفعل نفسه بسلوكين متناقضين — ومن يبدّل ببطاقته كان يُراكم فواتير معلّقة **لا
يستطيع أحد إكمالها ولا حذفها**، وهي المشكلة التي أُوقف التعليق بسببها أصلاً.
وأسوأ من ذلك أن الرسالة كانت تقول «تم حفظ فاتورة… معلقة بأمان 📥» — وعدٌ يجعل الكاشير
يبحث عنها ولا يجدها. الآن المساران واحد، والرسالة تقول ما حدث فعلاً: «فُرّغت السلة — لم
تكن فاتورة مسجّلة».

**٣. `.scrollbar-thin` عُرّف أخيراً** — `index.css`. كان مستعملاً في `PosRegister.jsx`
(لوحة الفاتورة وقائمة السلة) **وهو غير معرَّف إطلاقاً** فبلا أثر. صار ٨ بكسل: أنحف من
الشريط العام (١٢) لأن العمود عرضه ٣٨٠ بكسل، وما زال يُمسك بالإصبع (المقبض ٤٤ طولاً).

**تحقّق:** بناء ناجح · ٩٩/٩٩ · القواعد والاستضافة منشورتان.
❌ لم يُتحقّق بصرياً (يحتاج دخول)، ولم تُختبر الأدوار الجديدة عملياً (تحتاج حسابات `f1`…`f4`).

### 🚨 فحص شامل ثالث — 2026-09-16 · **٨١٪** (التقرير: `تقرير_الفحص_الشامل_الثالث_2026-09-16.md`)

فحص جديد كامل لا إعادة تدوير: تحقّق **مستقلّ** من وجود كل إصلاح سابق في الكود (**١٢/١٢
موجودة**)، ثم مسوحات جديدة (شروط ميتة، وعود غير منتظَرة، ابتلاع صامت، تسرّب مؤقّتات
ومستمعين، طبقات التخزين، إشارات القيود). **البناء ١٩٥١ وحدة · الاختبارات ١١١/١١١.**

#### 🔴 عطل حرج جديد — التصفير كان يُحيي المحذوف (أُصلح ونُشر)

**يُبطل جوابي السابق عن حذف البيانات المالية.** السلسلة:
1. `saveAndSync` تكتب في **IndexedDB ثم localStorage** (`AppContext.jsx:1224`) — صحيح.
2. لكن **دوال التصفير التسع عشرة تكتب في localStorage وحده**؛ صفر `idbSet` داخلها.
3. وعند الإقلاع `hydrateLargeCollections` كانت تقرّر بشرط واحد:
   `idbInvoices.length > prev.length` — أي **«الأطول هو الأصحّ»**. و localStorage صار
   `[]` بينما IndexedDB فيها المئات.
4. واللقطة السحابية الفارغة لا تُنقذ: `syncEngine.js:258` تعود مبكراً عند `snap.empty`
   عمداً وبحقّ (§5.3).

**النتيجة**: مالكٌ يُصفّر حساباته ثم يُعيد التحميل **فيجد الفواتير كما كانت**، والتقارير
المحلية و Z-Report تُحسب على بيانات ظنّها محذوفة. **السحابة كانت تُمسح فعلاً — والجهاز لا.**

**الإصلاح:**
- `purgeLocalKey(key, emptyValue, stamp)` — تمسح **localStorage و IndexedDB معاً** وتكتب
  ختم التصفير في الاثنين. أُدرجت في **٢١ موضع تصفير**. «المسح من مكان واحد لا يكفي حين
  يُقرأ من مكانين».
- الاسترجاع صار يحترم ختم التصفير: نسخةٌ ختمها `<=` آخر تصفير **تُهمَل وتُمسح** فلا
  تتكرّر المحاولة كل إقلاع. والتعادل يفوز فيه التصفير. الطول لم يعد دليلاً على شيء.

**تحقّق:** `tests/reset_purge.test.mjs` — **١٢/١٢** — يستخرج `purgeLocalKey` من
`AppContext.jsx` حرفياً وينفّذها على مخزنين وهميين، ويغطّي **السيناريو الذي كان يُحيي
المحذوف** نصّاً، والبيع بعد التصفير، والخرائط (`{}` لا `[]`)، وتعادل الختم.
**المجموع صار ١١١/١١١.**

#### 🟡 أعطال أخرى بخطورة مُقاسة لا مبالَغ فيها

- **٢٢ استدعاء طباعة بلا `await`**، ٧ منها داخل `try/catch` لا يلتقط شيئاً.
  **لكن** `printHtmlDirectly` **لا ترفض إطلاقاً** (تلتقط داخلياً و`resolve(false)`)،
  فالمسار الاحتياطي **كود ميت يُعطي طمأنينة كاذبة** لا عطل يقع. **العطل الحقيقي**:
  تُرجع `false` عند الفشل و**لا أحد يفحصها** — فالطباعة تفشل صامتة. **خطورة متوسطة.**
- **IndexedDB تُقرأ لمفتاحين من ستة عشر** — `getSaved` يقرأ localStorage وحده.
  فتعليق «Relying on IndexedDB storage» عند امتلاء الحصة **غير دقيق**.
  **خطورة منخفضة** لأن السحابة مرجع عند الإقلاع، إلا لجهاز بلا إنترنت لحظة الامتلاء.
  ⚠️ **تصحيح لتقريري الأول**: وصفتُ الترحيل بأنه «تجميلي» — الأدقّ: **الكتابة سليمة
  ومزدوجة، والقراءة ناقصة**.
- **خزينة المدير تُخفي الدرج السالب** — `AppContext.jsx:5180` فيها `Math.max(0, …)`،
  فمجموع نقد الكاشيرين **يزيد عن الحقيقة**. ويناقض قرار `computeExpectedCash` في نفس
  الجلسة («السالب حقيقة محاسبية لا خطأ يُخفى»). **لم يُصلَح عمداً — قرار محاسبي للمالك.**

#### ✅ فُحص ووُجد سليماً
قيود الخزينة (المصروف `-numAmount` فـ `Σ amount` صحيح) · لا تسرّب مؤقّتات ولا مستمعين ·
لا شروط ميتة باقية · لا أسرار في حزمة الإنتاج · الحسابات الأساسية تحقّقت حيّاً.

### 🔧 جولة إصلاحات خامسة — البنود «العالية» (2026-09-16 · منشورة · ١١١/١١١)

#### ٢. صيغة سادسة للنقد في الشاشة الرئيسية — أُصلحت
`Dashboard.jsx:115` كانت تحسب:
```js
startCash + shiftCashSales + shift.cashIn − shift.cashOut
```
**بلا خصم المصروفات ولا المشتريات**، بينما شاشة الدرج تخصمهما. فالمالك يرى **رقمين
مختلفين لنفس الدرج في شاشتين** ولا يعرف أيّهما يصدّق — وهذا أسوأ من رقم خاطئ معروف.
ولها عيب ثانٍ: كانت تقرأ عدّادات الوردية المتسابقة.

**الإصلاح:** `drawerCash` تستدعي `computeExpectedCash` المشتركة، وتُعيد حساب كل مُدخلاتها
من **السجلات الأصلية** (الفواتير، حركات الدرج، المصروفات، المشتريات) بنفس قواعد شاشة
الدرج. وأُضيفت `purchases` و `drawerTransactions` إلى ما تقرأه الشاشة من السياق.

#### ٣. عدّادات الورديات المتسابقة — أُزيل أثرها العملي
كان البند مؤجَّلاً لأن إصلاحه الجذري (ضمّ `user_shifts` إلى `INCREMENT_OWNED` وتحويل كل
كتّابها إلى `adjustFields`) refactor نقدي لا يمكن التحقّق منه حيّاً. **فاتُّخذ طريق آخر
أكثر أماناً وأقوى أثراً: إخراج الأرقام من دائرة السباق بدل إصلاح السباق.**

فُحص كل مستهلكي `shift.cashSales`/`cashIn`/`cashOut`، فتبيّن أن الأحياء منهم **اثنان فقط**
(الباقي لقطات ورديات **مغلقة**، وهي صحيحة تاريخياً ولا يجوز إعادة حسابها):
- `AppContext.jsx:5180` — `openShiftsCashTotal` في ملخّص الخزينة.
- `ManagerTreasuryTab.jsx:1581` — «درج حالي» في قائمة تغذية الدرج.

**الإصلاح:** دالة واحدة مصدَّرة `computeOpenShiftCash(shift)` تُعيد الحساب من السجلات،
ويستعملها الموضعان. فيرى المدير **نفس الرقم الذي يراه الكاشير في درجه** — لا رقماً ثالثاً
مشتقّاً من عدّادات تتسابق بين الأجهزة. والسجلات لا تتسابق: كلٌّ منها مستند مستقل.

> ⚠️ العدّادات نفسها ما زالت تُزامَن كقيم مطلقة — لكنها لم تعد مصدراً لأي رقم مالي حيّ.

#### ٤. ساعة الجهاز تحسم التعارضات — صار العطل **مرئياً**
المزامنة تحسم كل تعارض بـ `updatedAt` المكتوب من `Date.now()`. فجهاز ساعته متقدّمة نصف
ساعة **يفوز بكل تعارض** ولو كانت نسخته أقدم فعلاً، ومتأخّر يخسر تعديلاته الصحيحة —
ورقمان مختلفان على جهازين **بلا سبب ظاهر في أي سجل**.

الإصلاح الجذري (`serverTimestamp` في كل كتابة) تغييرٌ واسع في دلالات الوقت عبر المحرّك
كله، لا يصحّ بلا بيئة اختبار بجهازين. **فالخطوة الآمنة المتاحة: قياس الانحراف وإظهاره.**

`src/utils/clockSkew.js` (جديد): يقيس فرق ساعة الجهاز عن ساعة خادم Google من ترويسة
`Date` في استجابة `HEAD` — **بلا أي كتابة**. ويطرح نصف زمن الرحلة فلا يُحسب بطء الشبكة
انحرافاً في الساعة. وفوق دقيقتين يُطلق إشعاراً بالعربية: «ساعة هذا الجهاز متقدّمة/متأخّرة
كذا… صحّح وقت الجهاز، وإلا اختلطت أسبقية التعديلات وظهرت أرقام متضاربة».
يُفحص **مرة واحدة عند الإقلاع** بعد ٨ ثوانٍ (لا يزاحم أول مزامنة)، وغياب القياس (بلا
إنترنت) لا يُعدّ انحرافاً.

**تحقّق:** بناء ناجح · **١١١/١١١** · منشور.
❌ لم يُتحقّق بصرياً من أي من الثلاثة (يحتاج تسجيل دخول)، ولم يُختبر إنذار الساعة على
جهاز ساعته منحرفة فعلاً.

**الباقي من قائمة الـ١١:** البنود المتوسطة (الطباعة تفشل صامتة · ٤٢ `confirm`/`prompt`
مجمِّدة · اختبار مالي ينسخ ولا يستورد) والمنخفضة (خزينة تُخفي الدرج السالب — **قرارك** ·
IndexedDB تُقرأ لمفتاحين · QZ محجوب على HTTPS · لا git) وبريد Firebase المشترك — **قرارك**.

### 🔧 جولة سادسة — البنود «المتوسطة» (2026-09-16 · تُسجَّل تباعاً)

#### ✅ (١/٣) الطباعة كانت تفشل صامتة — أُصلح ونُشر

`printHtmlDirectly` كانت تُرجع `false` عند الفشل، و**لا أحد من مستدعيها الاثنين والعشرين
يفحص القيمة المُرجَعة**. فالكاشير يضغط «طباعة»، لا يخرج شيء، ولا تظهر رسالة — فيظنّ
الطابعة معطّلة ويعيد المحاولة، أو الأسوأ: **يسلّم العميل بضاعة بلا فاتورة ظنّاً أنها طُبعت**.

**الإصلاح في نقطة الطباعة الوحيدة لا في ٢٢ موضعاً** (وهذا معنى §5.6): الفشل يُعلن من
داخل `printHtmlDirectly` نفسها برسالة تذكر السبب وتقول صراحةً «المستند لم يُطبع».
فيغطّي المستدعين كلهم دفعةً واحدة.

**و`window.print()` حُذف من خمسة مسارات «احتياطية»** — كان يطبع **الصفحة كلها** لا
المستند، فيُهدر ورقاً حرارياً ويُخرج شيئاً لا يشبه الإيصال. وهو **كود ميت أصلاً**: دوال
الطباعة غير متزامنة ولا ترفض، فالـ `catch` الذي يلفّها لا يعمل أبداً.

**وحالتان «لا مستند لنطبعه»** (`CurrentShiftDrawerTab` و `StaffAndShiftsTab`) كانتا
تطبعان الصفحة عند غياب التقرير — صارتا رسالة صريحة تقول ما ينقص وكيف يُجهَّز.

**ما بقي مقصوداً:** زر «مستند PDF 📄» في `ReceiptModal:615` يستدعي `window.print()` عمداً —
وهو يعمل مع قواعد `@media print` في `index.css` التي تُخفي الأزرار والترويسة. **تُرك.**

**تحقّق:** بناء ناجح · ١١١/١١١ · منشور. ❌ لم يُتحقّق بصرياً (يحتاج دخول وطابعة).

#### ✅ (٢/٣) الاختبار المالي الزائف — استُبدل بحقيقي، **وكشف عطلاً فورياً**

`financial_calculations.test.cjs` كان **ينسخ** الدوال بدل أن يستوردها. فسبعة عشر اختباراً
تمرّ خضراء على نسخةٍ من المنطق قد تكون متخلّفة عن الملف الحقيقي بأشهر — **اختبارٌ يطمئن
ولا يحمي، وهو أسوأ من غياب الاختبار لأن غيابه معروف**.

**البديل:** `tests/financial_real.test.mjs` — **٤٠ اختباراً على الكود الحيّ** بنمطين:
- **استيراد مباشر** لـ `security.js` (بلا استيرادات): التجزئة، التحقّق، الملح العشوائي،
  قوة الرمز (أربعة أرقام، رفض المكرّر والشائع).
- **استخراج النصّ حرفياً** لـ `getCartTotals` من `AppContext.jsx` و `generateZatcaTLV`
  من `helpers.js`: الضريبة الشاملة وغير الشاملة والمعطّلة · **الخصم النسبي يُحسب بعد
  خصومات الأصناف لا قبلها** (١٨ لا ٢٠) · الخصم لا يتجاوز السلة · وسوم ZATCA الخمسة ·
  **قصّ اسم المتجر الطويل** (الحرف العربي بايتان، و٢٠٠ حرف = ٤٠٠ بايت > حدّ ٢٥٥).

🔴 **وفي أول تشغيل كشف عطلاً حيّاً لم يره أي فحص سابق:**
`getCartTotals` كانت تكتب `Number(item.unitPrice ?? …)` **بلا حارس**. و`Number('abc')`
تساوي `NaN`، و`NaN` يعدي كل ما يُجمع معه — فصنف واحد بسعر غير رقمي (استيراد إكسل بخلية
نصية، أو حقل أُفرغ ثم حُفظ) كان يجعل **إجمالي الفاتورة كلها `NaN`**: الشاشة تعرض
«NaN ر.س»، والفاتورة تُحفظ بإجمالي تالف، وتقارير اليوم تنهار معها.
**أُصلح** بـ `safeNum` تقصر الضرر على الصنف التالف وحده (والخصم العام كذلك).
**عطلٌ عاش تحت سبعة عشر اختباراً «ناجحاً» — وسقط في أول تشغيل على الكود الحقيقي.**

الملف القديم **أُخرج من `npm test`** وبقي في المستودع بترويسة تشرح لماذا (حالات ديون
FIFO فيه لم تُنقل بعد).

**المجموع صار ١٣٤/١٣٤** (٤٠ مالية حقيقية + ١٩ أداء + ١٢ بوابة PIN + ١٢ نقدية + ٣٩ TSPL
+ ١٢ تصفير). بناء ناجح · منشور.

#### ⏳ (٣/٣) ٤٢ نافذة `confirm`/`prompt` مجمِّدة — لم يبدأ

### ✅ مكتمل ومُتحقَّق منه

**1. لوحة المفاتيح الافتراضية** — `common/VirtualKeyboardWrapper.jsx`
- عطل أول: بعد كل ضغطة يُعاد تركيز الحقل فيُطلق `handleFocus` وتُطبَّق قاعدة تبديل اللغة،
  فترتدّ من «أرقام» إلى العربية. الحل: `isSameField` — اللغة تتغيّر فقط عند الانتقال لحقل آخر
  (`:189` و `:216`).
- عطل ثانٍ اكتُشف أثناء الاختبار: **أزرار الشريط كلها ميتة** — `startDrag` مربوط بـ
  `onPointerDown` ويستدعي `preventDefault()` فيُلغي حدث `click`. مجموعة الأزرار كانت توقف
  `mousedown` فقط وهو يأتي بعده. الحل: إيقاف `pointerdown` و `touchstart` عند الأزرار (`:326`).
- **تحقّق تشغيلي**: ٦ أرقام متتابعة كُتبت `505123` واللغة بقيت «أرقام»؛ والانتقال لحقل آخر
  يرجعها «عربي». ✅ منشور.

**2. الباركود المتسلسل** — `helpers.js:154`
- ٩ حالات ناجحة عبر استيراد الوحدة الحقيقية + إنشاء صنف فعلي في التطبيق حصل على `0000001`.
- باركود المصنع `6282001` (٧ أرقام لكن > 999999) لا يكسر التسلسل. ✅

**3. تنبيه نفاد المخزون** — `AppContext.jsx:615`
- تحقّق تشغيلي: بيع خفض المخزون ٤←٣ (= `minStock`) فظهر التنبيه بنصّه الصحيح. ✅

**4. ضياع خصم البيع عند تعديل متزامن** — `AppContext.jsx` `updateProduct`
- **العطل**: النموذج فُتح والمخزون ٣، ثم بيعت وحدة على جهاز آخر فصار الحيّ ٢، والنموذج ما زال
  ٣. عند الحفظ حُسب الفرق `3 − 2 = +1` وأُرسل `increment(+1)` **فأعاد الوحدة المباعة**.
  والمستخدم لم يلمس حقل الكمية. وسجّل أيضاً **قيد تسوية وهمياً بلا سبب** في سجل التدقيق.
- **الإصلاح**: `updateProduct(id, data, reason, stockBaseline)` — الفرق يُقاس على ما رآه
  المستخدم؛ والحالة المحلية تطبّق نفس الفرق على الحيّ؛ ونموذج التعديل لا يرسل `stock` إن لم
  يُمسّ؛ وأزرار +/− تمرّر الكمية المعروضة.
- **تحقّق تشغيلي بجهازين**: قبل الإصلاح السحابة ٣ (ضاع البيع) — بعده **٢** مع وصول تعديل B. ✅

**5. رصيد العميل عند التعديل المتزامن**
- `CustomersScreen.jsx:84` كان محميّاً أصلاً (يجرّد `balance` من حمولة التعديل).
- **تحقّق تشغيلي**: سند قبض على A خفض الرصيد ١٠←٠ ونموذج B يحمل ١٠؛ بعد حفظ B بقي **٠**
  ووصل اسم B. ✅

**6. النسخة الاحتياطية السحابية التلقائية** — `AppContext.jsx:5284`
- **العطل**: لم تعمل **ولا مرة**. العلم يُرفع قبل عمل المؤقّت، والتنظيف يُلغي المؤقّت في كل
  إعادة تصيير، و `products` في مصفوفة الاعتماد وتتغيّر مع كل نبضة مزامنة. فأي تغيّر خلال
  ١٥ ثانية يُلغي المؤقّت ثم يخرج المؤثّر لأن العلم مرفوع → لا يُعاد جدولته أبداً.
- **الإصلاح**: المؤقّت في `useRef`، التنظيف عند إزالة المزوّد فقط، واستدعاء عبر مرجع متجدّد
  لالتقاط بيانات مكتملة، وفحص أخير للختم قبل الرفع (منع التكرار بين التبويبات).
- **تحقّق تشغيلي**: رُفعت نسخة فعلية ٥٠ ك.ب (٧١ صنفاً، ٥ فواتير) إلى `pos_backups`. ✅

**7. تضارب نسبة المرتجع** — `AppContext.jsx` `refundInvoice`
- **العطل**: `refundShiftId` كان يُكتب بوردية **المنفّذ** بينما المال يخرج من وردية **البائع**.
  فأي تقرير يعتمد عليه ينظر للوردية الخطأ.
- **الإصلاح**: تحديد الوردية مرة واحدة **قبل** ختم الفاتورة واستعمالها في الختم والخصم وحركة
  الدرج. وحقول جديدة: `refundChargedUserId`, `refundChargedToSeller`, `refundShiftFallback`.
  والحالة الصامتة (وردية البائع مغلقة) صارت تُسجَّل في سجل التدقيق بدل `console.warn`.
- **الحالة**: بُني بنجاح — ❌ **لم يُتحقّق تشغيلياً** (يحتاج كاشيرين).

**8. طبقة توجيه الطابعات (QZ Tray)** — جديدة
- `qzPrint.js`: اتصال، اكتشاف الطابعات، الطابعة الافتراضية، طباعة HTML وخام، اختبار طباعة.
- `printHelper.js:33`: التوجيه أُدخل في `printHtmlDirectly` وحدها فورثه كل نوع طباعة.
- `PrintersSettingsPanel.jsx`: ثلاث وجهات، اكتشاف حقيقي للطابعات، دعم IP، زر اختبار لكل وجهة.
- `initialData.js`: أُفرغ `printerName` و `barcodePrinterName` (كانا **لا يُقرآن في أي مكان**).
- **تحقّق تشغيلي جزئي**: الشاشة تُعرض، تبديل الوضع إلى «شبكة» يُظهر خانتي IP والمنفذ ويحدّث
  «الوجهة الفعلية» فوراً. ❌ لم يُختبر مع QZ مثبَّت فعلياً.

**9. محرّك أداء الموظفين والبونص** — `staffPerformance.js` (جديد)
- ثلاثة أخطاء حسابية في `StaffAndShiftsTab.jsx` صُحّحت:
  - **الالتزام**: `hasDiscrepancy` قيمة منطقية واحدة، فمن عنده ٨ ورديات مختلّة من ١٠ تظهر
    نسبته **٩٠٪**. الآن تُعدّ الورديات المختلّة فعلاً → **٢٠٪**.
  - **أدقّ درج**: كان يرتّب بـ `Math.abs(مجموع الفروقات)` فيتصدّر من نقص ٥٠ وزاد ٥٠
    (المحصّلة صفر) على من نقص ريالاً. الآن **مجموع القيم المطلقة**، والعجز منفصل عن الزيادة.
  - **المرتجعات**: كانت تُنسب بتاريخ الفاتورة. الآن كل قيد يدخل فترة وقوعه: يناير `+100`،
    فبراير `−100`، والمجموع صفر — بلا عقاب مزدوج وبلا إعادة كتابة فترة صُرف راتبها.
- أُضيف **مجمل الربح** من `costAtSale` المخزَّن في الفواتير.
- نظام بونص: نسبة / مبلغ لكل فاتورة / **شرائح تصاعدية**، مع حدّ أدنى وسقف وخصم العجز النقدي.
- **تحقّق**: **١٩/١٩ اختباراً ناجحاً** يستورد الملف الحقيقي. ✅ مربوط بالواجهة (بند ١١).

**10. شاشة ضبط بونص الموظف** — `settings/BonusRuleEditor.jsx` (جديد)
- مكوّن مستقل مربوط في `UsersSettingsTab.jsx` داخل نموذج المستخدم قبل قسم الصلاحيات.
- يُحفظ في `user.bonusRule`، ويُحمَّل عند التعديل، ويُصفَّر عند إضافة موظف جديد.
- قاعدة الاحتساب (صافي المبيعات / مجمل الربح / عدد الفواتير) × الطريقة (نسبة / مبلغ لكل
  فاتورة / شرائح) + حدّ أدنى + سقف + خصم العجز النقدي.
- **معاينة حيّة** على مثال ثابت (مبيعات ٣٠٠٠٠، ربح ٦٠٠٠، ١٥٠ فاتورة، عجز ٤٠) تُظهر مبلغ
  البونص فور تغيير أي حقل — لأن قاعدة بلا رقم أمام العين تُضبط بالحدس وتُكتشف آخر الشهر.
- **الحالة**: بُني بنجاح (١٩٤٧ وحدة). ❌ **لم يُتحقّق بصرياً** — انتهت جلسة Firebase
  وعادت شاشة الدخول، والدخول يحتاج المستخدم. منطق الحساب نفسه مغطّى بـ ١٩ اختباراً.

**11. إعادة بناء تبويب أداء الموظفين** — `reports/tabs/StaffAndShiftsTab.jsx` (2026-09-15)
- أُعيد بناء الملف بالكامل ليستهلك `staffPerformance.js` بدل الحسابات المضمّنة المعطوبة.
- **ما تغيّر**:
  - أُزيل `useMemo` القديم (٣٠٠ سطر حسابات مضمّنة بخمسة أخطاء) واستُبدل باستدعاء
    `computeStaffPerformance()` ثم `attachBonuses()`.
  - أُضيفت بطاقتان جديدتان في الملف الفردي للكاشير: **مجمل الربح** (مع هامش الربحية)
    و**البونص المستحق** (مع تفاصيل القاعدة والخصومات).
  - تُحدّثت بطاقة انضباط الصندوق: العجز والزيادة منفصلان، نسبة المطابقة حقيقية
    (`cleanShifts / shiftsCount`)، ومتوسط الانحراف لكل وردية.
  - تُحدّثت بطاقات القيادة في المقارنة الشاملة: أُضيف «أعلى ربح»، واستُبدل ترتيب الدقة
    بمجموع القيم المطلقة.
  - أُضيفت أعمدة الربح والمرتجع والبونص في جدول المقارنة.
  - `salesPerHour` يعرض `—` عند عمل أقل من ١٥ دقيقة (بدل رقم مضخّم).
  - تُحدّثت دوال الطباعة والمشاركة عبر واتساب بالحقول الجديدة.
- **ما لم يتغيّر**: تبويب Z-Report، تبويب سجل الورديات، هيكل الشاشة العام.
- **تحقّق**: `npm.cmd test` ← **٣٦/٣٦ ناجح**، `npm.cmd run build` ← **١٩٤٧ وحدة بنجاح**.
  ❌ **لم يُتحقّق بصرياً** — يحتاج تسجيل دخول.

### 🔄 قيد العمل الآن — ما ينتظر المستخدم تحديداً

كل ما بُني منشور. **الناقص كله تحقّق بصري يحتاج تسجيل دخول** (لا أستطيع إدخال البريد
ولا رقم PIN):

1. **شاشة النسخ السحابية الجديدة** — الإعدادات ← النسخ. يُفترض ظهور قسم أعلى الصفحة فيه
   قائمة النسخ وزرّا استرجاع وحذف، وحقل رمز المدير لتفعيل الاسترجاع.
2. **مفتاح TSPL** — الإعدادات ← الباركود ← «طباعة خام بلغة الطابعة».
3. **تبويب أداء الموظفين** وشاشة البونص — مبنيّان ولم يُريا.
4. **دورة تشغيلية متبقّية**: شراء من مورد وصرف · مصروف بكل الطرق · الخزينة · التقارير.

### 🔐 قرار بانتظار المستخدم — بريد Firebase لكل جهاز (أكبر ثغرة أمنية باقية)
القواعد تعرف الهوية **بالبريد وحده**، ودور PIN لا علاقة له بالسحابة. فجهاز كاشير داخل
ببريد المدير يستطيع من أدوات المطوّر: حذف الفواتير، تعديل الأسعار والتكلفة، تعديل
المستخدمين، قراءة سجل التدقيق والنسخ الاحتياطية.
**المطلوب منك**: إنشاء `f1`…`f4` في Firebase Authentication (موجودة في `authUsers.js`)
وتسجيل كل جهاز ببريده. ⚠️ **أثر جانبي مقصود**: `pos_users` و `pos_backups` كتابتهما
`isAdmin()` فقط — فبعد التحويل لن تُرفع النسخ الاحتياطية إلا من جهازك.

### 🗂️ نظام النسخ الاحتياطي للكود (Git)
- ❌ **لا يوجد git على الجهاز إطلاقاً** (`git --version` يفشل) ولا مستودع في المجلّد.
  النسخة الوحيدة `FL-HO2018.rar` (٢٣٠ ميجا) لقطة قديمة جامدة. **خطر ضياع كامل.**
- ✅ حُدّث `.gitignore` ليستثني `.env` و `*.rar` والسجلات ومخرجات البناء.
- ✅ أُنشئ `.env.example` بأسماء المفاتيح بلا قيم (المفاتيح لا تنتقل مع git).
- ⏸️ **بانتظار المستخدم**: تثبيت git — افتح PowerShell **كمسؤول** وشغّل `choco install git -y`
  ثم افتح طرفية جديدة. بعدها: `git init` + أول إيداع + مستودع GitHub خاص.

### ⏸️ مؤجّل بقرار المستخدم
- **اختبار الكاشيرين والتقسيم**: بيع عند كاشير ١ وإرجاع عند كاشير ٢ — يكشف صحّة إصلاح
  النسبة (بند ٧). **معطّل**: رمز PIN الكاشير غير معروف؛ يُضبط من الإعدادات ← المستخدمين.
- **تنظيف بيانات الاختبار**: ألغاه المستخدم («البرنامج تحت الإنشاء»). الباقي في المتجر:
  صنف `prod-1789321089586-11970`، عميل `cust-1789324914305`، ٤ فواتير × ١٠ ر.س،
  سند قبض ١٠، و٣ نسخ احتياطية. الوردية المفتوحة تقول `165 ر.س` بدل `125`.

### 📋 لم يبدأ
- ~~طباعة الباركود بـ TSPL خام~~ ✅ **أُنجزت ونُشرت** — انظر قسم TSPL أعلاه.
- **إعادة تصميم قالب الفاتورة** (80مم و A4) — `SettingsScreen.jsx:1492` كتلة ~١٠٠٠ سطر.
- **طباعة باركود احترافية**: طباعة صنف/أصناف + دمج باركود المصنع.
- **متغيّرات المنتج (Product Variants)**: نوع/حجم/لون بسعر ومخزون وباركود لكل متغيّر.
  **الأكبر والأعقد — يحتاج تخطيطاً قبل الكتابة.**

### 🐞 ملاحظات صغيرة معلّقة
- ~~زر توليد الباركود عنوانه «عشوائي»~~ ✅ صار «توليد باركود تسلسلي فريد».
- نافذة الطباعة بعد كل فاتورة تُجمّد الواجهة؛ يمكن جعلها اختيارية أو صامتة عبر QZ.
- `window.confirm`/`prompt` في المرتجع وتسوية الكمية يُجمّدان اللوحة — يُستحسن استبدالهما
  بنوافذ داخل التطبيق (لم يُنفَّذ بعد).

---

## 12. سجل القرارات

| التاريخ | القرار | السبب |
|---|---|---|
| 2026-09-13 | **QZ Tray** لا Electron | الملصقات تحتاج TSPL لمقاس مضبوط؛ Electron يطبع HTML فقط |
| 2026-09-13 | لا تنظيف لبيانات الاختبار | البرنامج تحت الإنشاء |
| 2026-09-13 | لا تغيير للوردية التي يُخصم منها المرتجع | تعديل تقرير Z مُصدَر قرار محاسبي للمالك وحده |
| 2026-09-13 | البونص يدعم **مجمل الربح** كقاعدة | البونص على الإيراد وحده يُغري ببيع الرخيص وبالخصومات |

---

## 13. تفضيلات المستخدم

- يتواصل **بالعربية**؛ أجبه بالعربية.
- يريد **عملاً منفّذاً** لا خططاً مطوّلة.
- يقدّر **الصراحة**: قل ما لم يُختبر ولا تُجمّل.
- يعمل في **جلسة واحدة فقط** على هذا المشروع في كل مرة.
- «احترافي» عنده تعني: أرقام صحيحة يصحّ الحكم بها، ومطبوعات بمقاسات مضبوطة.

---

## 14. بدء جلسة جديدة — قائمة تحقّق

1. اقرأ هذا الملف كاملاً.
2. تحقّق من البناء: `cd D:\FL-HO2018; npm.cmd run build`
3. شغّل الاختبارات: `npm.cmd test`
4. راجع **§11 الوضع الحالي** لمعرفة أين توقّف العمل.
5. عند تشغيل خادم تطوير استعمل **المنفذ 3000** حصراً.
6. **حدّث §11 بعد كل خطوة — إلزامي.**
